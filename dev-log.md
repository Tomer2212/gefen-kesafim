# Dev Log

## 2026-09-23 — ניהול וניתוק חיבורים מרוחקים (חיבורים פעילים)
- טבלה חדשה `user_sessions` + שדות `session_auto_disconnect_hours` / `session_auto_disconnect_next_run_at` ב-`organizations` (מיגרציה רצה ואומתה מול Supabase)
- `backend/auth.py`: כל בקשה נבדקת מול כותרת `X-Device-Id` — חיבור שנותק מרחוק נחסם תוך שניות (`_is_device_revoked`, מטמון 5 שניות)
- `backend/routers/sessions_router.py` חדש: רישום חיבור + אכיפת מגבלת 3 חיבורים, רשימת/ניתוק חיבורים אישיים ומנהליים, קביעת חיבור ראשי, הגדרת תדירות ניתוק אוטומטי ברמת ארגון, ו-endpoint cron לניתוק בפועל (`POST /sessions/auto-disconnect`, מאובטח ב-`X-Cron-Secret` כמו תזכורות הפגישות)
- פרונטאנד: `X-Device-Id` קבוע לכל דפדפן (`lib/deviceSession.js`), טאב "חיבורים" חדש באזור האישי (`ConnectionsTab.jsx`) ובאזור הניהול (`AdminConnectionsTab.jsx`), מסך חסימה כשמגיעים ל-3 חיבורים (`ConnectionsLimitPage.jsx`), והודעה בעמוד ההתחברות כשחיבור נותק מרחוק
- נוסף `/sessions` ל-proxy allowlist ב-`vite.config.js`
- workflow חדש `.github/workflows/sessions-auto-disconnect.yml` שקורא ל-endpoint הניתוק האוטומטי כל שעה (כמו ה-workflow הקיים לתזכורות פגישות)

## 2026-09-23 — תיקון: רישום חיבור נכשל עם 503
- הסיבה: `.maybe_single()` בגרסת ה-postgrest-py המותקנת זורקת שגיאה כשאין תוצאה (0 שורות) במקום להחזיר `data=None` — קרה בכל ניסיון רישום ראשון של חיבור חדש. תוקן ב-`auth.py` ובכל `sessions_router.py` (helper חדש `_fetch_one`)
- נוסף טור "חיבור ראשי" לטבלת הניהול בטאב "חיבורים", כדי לראות בלי לפתוח מודל אילו משתמשים כבר מוגדר להם חיבור ראשי
- טאב "חיבורים" באזור הניהול הוזז למיקום בין "ביצועים" ל"שעון נוכחות"

## 2026-09-23 — תיקון נוסף: התנגשות כשאותו דפדפן משמש כמה משתמשים
- הסיבה: מזהה הדפדפן (`device_id`) שימש גם כמפתח הראשי של שורת החיבור בטבלה — כשאותו דפדפן/מחשב שימש שני משתמשים שונים (למשל שני יועצים שבודקים על אותו מחשב), הרישום של המשתמש השני נכשל עם שגיאת "duplicate key"
- תוקן: הוספת עמודה `device_id` נפרדת מהמזהה של השורה עצמה (`id`, שנוצר אוטומטית), עם אילוץ ייחודיות על הצירוף (device_id, user_id) — כך שאותו דפדפן פיזי יכול להופיע כשורה נפרדת לכל משתמש שהתחבר בו, בלי להתנגש
- נוספה כתובת IP לתצוגה בכל חיבור (גם באזור האישי וגם בטאב הניהול) — עוזר להבחין בין כמה מחשבים ששמם מוצג אותו הדבר (למשל שני מחשבי "Chrome · Windows")

## 2026-09-23 — טבלת הניהול של חיבורים: מיון, סינון, וכתובת IP בעמודה
- עמודת "חיבור ראשי" בטבלה הראשית (לא רק במודל) מציגה עכשיו גם כתובת IP, לא רק סוג דפדפן/מערכת הפעלה
- כותרות הטבלה ניתנות למיון וסינון בסגנון Excel מלא (אייקון ▽ בכל עמודה: מיון עולה/יורד, חיפוש חופשי, רשימת כל הערכים הקיימים עם אפשרות לסמן/להוריד סימון, "בחר הכל") — רכיב חדש ועצמאי `components/ColumnFilterMenu.jsx`, באותו סגנון בדיוק כמו טבלת בתי הספר הראשית (בלי לגעת ב-`DashboardPage.jsx` עצמו כדי לא לסכן את המסך הראשי)
- מיון ברירת המחדל הוא לפי תפקיד: בעלים → מנהל → יועץ
- תוקן יישור הטבלה למרכז (הייתה חסרה `mx-auto` על העטיפה הפנימית, מה שגרם לה להידבק לימין בתוך אזור רחב יותר)
- כתובת ה-IP בעמודת "חיבור ראשי" מתויגת עכשיו במפורש כ-IP (לא רק מספר גולמי)

## 2026-09-23 — תיקוני סינון/מיון + הרשאות חיבור ראשי
- תוקן: כפתורי המיון בעמודה המספרית ("חיבורים פעילים") הציגו "מא'-ת'" — עכשיו מציגים "מהקטן לגדול"/"מהגדול לקטן" לעמודות מספריות
- תוקן באג אמיתי בסינון: הקלדת טקסט וסימון תיבות ב-`ColumnFilterMenu` לא היה משפיע בפועל בלחיצה על "אישור" — חסרה הייתה התנהגות ה"הצמדה" של הבחירה לתוצאות החיפוש (בסגנון Excel), אותה תבנית בדיוק כמו בטבלה הראשית
- משתמש לא יכול יותר לנתק בעצמו את "החיבור הראשי" שלו (גם בשרת וגם בממשק) — רק בעלים/מנהל יכולים להסיר חיבור ראשי
- ניתן לסמן עד 2 חיבורים ראשיים למשתמש (היה מוגבל ל-1) — `PATCH /sessions/users/{id}/{sid}/primary` הפך מ"בלעדי" (קובע ומבטל את כל האחרים) ל"מתג" (`{is_primary: true/false}`) עם אכיפת מגבלה של 2

## 2026-09-23 — עוד 4 תיקונים בטבלת הניהול
- תוקן: `GET /sessions/users/counts` היה מחזיר רק חיבור ראשי אחד למשתמש (דורס את הקודם) — עכשיו מחזיר רשימה, כך שכששניים מסומנים כראשיים שניהם מוצגים
- תוקן סדר תצוגת IP שהתערבב (RTL/LTR) — עכשיו "IP <מספר> · <סוג>" בעטיפת `dir="ltr"` אחת, בטבלת הניהול, במודל, ובאזור האישי
- ברירת המחדל (מיון לפי תפקיד) כבר לא מסומנת ויזואלית ככה "פעיל" בכותרת העמודה — רק מיון/סינון שהמשתמש בחר בפועל מודגש
- אייקון הסינון/מיון (המשולש ▽) מתמלא בצבע מלא כשהעמודה מסוננת/ממוינת, בלי צורך בחץ נוסף ליד הכותרת
- תוקן: קביעה/הסרה של חיבור ראשי מהמודל לא רעננה את טבלת הניהול הראשית (רק "נתק" עשה זאת) — עכשיו שתי הפעולות מרעננות

## 2026-09-23 — תיקון א-סימטריה בסרגל הטאבים של אזור הניהול
- תיבת הכותרת/הטאבים הייתה קבועה על `max-w-4xl` (~896px) ללא תלות בכמות הטאבים — עם 11 טאבים כעת, השורה חורגת מהתיבה, וב-RTL זה גולש רק מצד שמאל (הימין נשאר צמוד לגבול התיבה) → א-סימטריה
- תוקן ל-`max-w-[100rem]` (אותה רוחב שכבר משמש את הטאבים ה"רחבים" למטה). הממורכז (`mx-auto`) כבר היה שם, כך שהתיקון עובד אוטומטית גם במצב סיידבר מורחב/מצומצם
- ניסיון ראשון עם `overflow-x-auto` (גלילה אופקית) העביר את הא-סימטריה לצד השני, כי דפדפנים לא עקביים לגבי נקודת העיגון של גלילה ב-RTL. הוחלף ב-`flex-wrap` — הטאבים עוברים לשורה שנייה כשאין מקום, כך שכולם תמיד נראים והתיבה תמיד ממורכזת
- הכותרת "פאנל ניהול" ושורת הטאבים עצמן מיושרות עכשיו למרכז התיבה (לא רק התיבה עצמה ממורכזת בעמוד)
- לפי בקשה: הכותרת "פאנל ניהול" חזרה להיות מיושרת לימין (מעל "בתי ספר", לא עד הסוף) והכותרת המשנית "ניהול בתי ספר, חטיבות ומשתמשים" הוסרה — הטאבים עצמם נשארו ממורכזים
- ניסיון עם `w-fit` (הסתמכות על CSS "שתתאים את עצמה לתוכן") לא עבד באמינות — כנראה בגלל אופן שדפדפנים מחשבים רוחב "טבעי" לשורת flex עם flex-wrap. הוחלף בגישה אמינה יותר: מדידה בפועל של המיקום של הטאב הראשון ("בתי ספר") מול קצה התיבה עם ResizeObserver, והזזת הכותרת בדיוק לפי המרחק שנמדד — כך שה-"פ" וה-"ב" תמיד על אותו קו אנכי, גם כשמצמצמים/מרחיבים את הסיידבר
