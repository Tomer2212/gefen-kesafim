# Dev Log

## 2026-08-05 — מחיקת ארגון מתוך "ניהול מערכת"
- נוסף כפתור שלוש נקודות ליד כל כרטיס בקשת הרשמה בעמוד "ניהול מערכת" (SuperAdminPage), עם אפשרות "מחק ארגון".
- לחיצה פותחת חלון אישור; אישור מוחק לצמיתות: אם לבקשה יש ארגון מאושר — את הארגון, כל בתי הספר שלו, כל ההיסטוריה, וכל המשתמשים (כולל חשבונות ההתחברות); אם אין ארגון (בקשה ממתינה/נדחתה) — רק את שורת הבקשה.
- Backend: endpoint חדש `DELETE /signup/requests/{req_id}` (סופר-אדמין בלבד) ב-`signup_router.py`.

## 2026-08-04 — טאב "גבייה" חדש בניהול
- נוסף טאב "גבייה" בין "שיחות" ל"הרשאות" בפאנל הניהול (owner/manager בלבד), עם טבלת בתי ספר בעיצוב זהה לטאב "בתי ספר".
- 7 עמודות placeholder: גפן מסודר, שעות גפן מדווחות, סכום ששולם, נותר לתשלום, אמצעי תשלום, מס' חשבונית עסקה, תאריך הפקדה (ללא חיבור ל-DB עדיין).
- כפתור "עמודות לתצוגה" (הצגה/הסתרה של עמודות) וכפתור סינון מרובה-בחירה לפי "סטטוס לקוח" (ברירת מחדל: פעיל בלבד).
