# Dev Log

## 2026-09-16 — שיפור בחירת ערך ו"יחס" לשדות טקסט חופשי במסך יצירת משימה
- frontend/PersonTaskCreateWizard.jsx: חוברה טעינת רשימת בתי הספר המלאה (`allSchools`) והועברה ל-ConditionGroupsEditor בשני מקומות השימוש (audience filter + מדד הצלחה) — היה חסר לעומת TaskCreateWizard.jsx, ולכן שדות טקסט חופשי (סמל מוסד/עיר/רשות/בעלות) לא קיבלו הצעות אוטומטיות בכלל.
- frontend/ConditionGroupsEditor.jsx: `TypeaheadValueInput` שודרג מרשימה מוגבלת ל-8 תוצאות ללא גלילה, לרשימה גוללת ומווירטואלית (`@tanstack/react-virtual`) שמציגה את כל הערכים הקיימים ומסננת בזמן הקלדה — משפיע גם על TaskCreateWizard.jsx (רכיב משותף).
- frontend/ConditionGroupsEditor.jsx: בורר "יחס" לשדות טקסט חופשי — ברירת המחדל שונתה מ-"שווה" ל"מכיל", והתוויות הוחלפו מסמלים (=/≠) למילים ("שווה ל"/"שונה מ"/"מכיל"). שדות select לא השתנו מבחינת ברירת מחדל, רק שינוי תווית עיצובי (=/≠ → שווה ל/שונה מ).

## 2026-09-16 — תיקון תנאי "יעד" בסינון קהל יעד + קיצור ניסוח יעדים
- backend/task_logic.py: `_eval_goal_condition` מקבלת פרמטר `combine_divisions` — כשמסננים "לא עומד ביעד" בסינון קהל יעד (audience) לבית ספר עם כמה חטיבות, מספיק שחטיבה אחת לא עומדת ביעד (OR). הערכת "מדד הצלחה" של משימה נשארה AND בין כל החטיבות כפי שהייתה (לא נגעתי כדי לא לשנות בשקט התנהגות משימות קיימות).
- frontend/ConditionGroupsEditor.jsx: לתנאי "יעד" נוסף prop חדש `goalValueContext` — כשמשתמשים בו לסינון קהל יעד (audience) מוצג "מצב נוכחי" עם אפשרויות "עומד ביעד/לא עומד ביעד/טרם הוגדר" במקום "ערך מדד הצלחה"/"כן/לא/טרם הוגדר" (שנשאר ללא שינוי כשמדובר בהגדרת מדד הצלחה). חובר בשלושת מסכי סינון הקהל (PersonTaskCreateWizard.jsx, TaskCreateWizard.jsx פעמיים).
- frontend/taskShared.js: נוספה פונקציה משותפת `goalShortLabel` שמפיקה ניסוח קצר ("תכנון 40%"/"דיווח 70%") במקום המשפט הארוך מ-GOAL_DEFINITIONS ("יעד תכנון: לפחות 40% מהתקציב."). מוחל עכשיו בכל מקום שמציג/מסנן יעדים: FieldPickerButton.jsx (בורר השדות, כולל התיקון לכפילות "יעד: יעד..."), taskShared.js's describeCondition (תיאורי תנאים בפרטי משימה/audiences שמורים), ו-FieldMetricEditor.jsx (הוסר שכפול קוד, אותה תוצאה כמו קודם).

## 2026-09-16 — בחירה מרובה (OR) לשדות select בבונה התנאים
- backend/task_logic.py: `_compare` תומכת עכשיו בערך שהוא רשימה ("אחד מרשימה") לכל שדה select — נדרש כדי שסינון עם כמה ערכים מסומנים (למשל תוכנת כספים = פייסקול או סקולקאש) יעבוד. תאימות לאחור מלאה לתנאים ישנים עם ערך יחיד (סקלרי).
- backend/task_logic.py: תנאי "יעד" — גם `budget_names` וגם `value` (מצב נוכחי/מדד הצלחה) עברו מ-AND ל-OR פנימי (בחירת כמה תקציבים/ערכים = "אחד מהם מספיק"). בוצע בלי חשש לתאימות לאחור כי אין עדיין משימות קיימות עם תנאי יעד.
- frontend/ConditionGroupsEditor.jsx: נוסף רכיב `MultiSelectChips` משותף - כל שדה select (סוג שירות, תוכנת כספים, מחוז, סטטוס לקוח, סטטוס מכתב בקרה, מצב יעד וכו') הפך מ-`<select>` בודד לבחירת צ'יפים מרובה, ובוטלה שורת "יחס" (=/≠) לשדות האלה לגמרי — הבחירה המרובה היא כל הסמנטיקה, בלי היפוך "שונה מ".
- frontend/taskShared.js + FieldMetricEditor.jsx + personTaskColumns.js: עודכנו כל מקומות התצוגה (תיאורי תנאים, עמודת "אחראי ביצוע") לתמוך בערך שהוא מערך, עם תאימות לאחור לתנאים ישנים עם ערך יחיד.
- frontend/PersonTaskCreateWizard.jsx: תוקן חישוב "האם נבחר ערך" לשדה חובה (סטטוס לקוח/סוג שירות) — מערך ריק `[]` הוא truthy ב-JS, אז הבדיקה עודכנה לבדוק אורך המערך ולא רק truthiness (אחרת האזהרה "שדה חובה" הייתה מפסיקה להופיע).
- frontend/ConditionGroupsEditor.jsx: רקע כרטיסיית כל תנאי בודד (השורה עם שדה/יחס/ערך) הוכהה מ-`bg-slate-50` ל-`bg-slate-100` (גם המסגרת מ-slate-100 ל-slate-200 כדי שתישאר נראית על הרקע הכהה יותר) — כדי שהתיבות הלבנות (השדות עצמם) יבלטו יותר על הרקע, בלי לשנות את השדות עצמם.
