# Dev Log

## 2026-09-06 — תמיכה בפורמט החדש של קובץ כספים2000
- `backend/logic/kesafim_processor.py` `_parse_tsv`: מיפוי עמודות לפי שם-כותרת במקום אינדקס קבוע, עם נפילה-לאחור לאינדקסים הישנים (`_LEGACY_IDX` / `_HEADER_TO_FIELD`).
- מתקן בדיקת השוואה כספים↔גפן שיצאה שגויה לחלוטין על הפורמט החדש (כספים2000 הוסיפו 3 עמודות: "סוג מסמך", "מס. הקצאה", "מס.תוכנית", ושינו סדר עמודות — הסכום זז מ-idx 10 ל-13).
- נבדק: קובץ ישן — פלט זהה (אפס רגרסיה); קובץ חדש — סכום/סטטוס/ichud תקינים.

## 2026-09-06 — פייסקול: התאמת מספר חשבונית עם מספר עסקה בסוגריים
- פייסקול מציג לעיתים `מספר חשבונית` כ-`<חדש> (<מקורי>)` (למשל `27203 (3646)`) בעוד דיווח ביצוע מחזיק רק את המספר המקורי → אי-התאמה שקרית בשני הצדדים.
- `backend/logic/payscool_processor.py`: נוספה `canonical_payscool_invoice()` (regex מחמיר `^(.+?)\s*\((\d+)\)\s*$`) — מחלצת את המספר שבסוגריים לצורך בניית ה-`ichud` בלבד. עמודת התצוגה לא משתנה.
- שולב ב-3 אתרי בניית `ichud` של פייסקול: `load_payscool`, ו-`analyze_router.py` `_load_payscool_sheet_df` + `_build_finance_ichud_budget_map`.
- התראה/עקיבות: `summary.finance_file.paren_invoice_normalized` (ספירה) + `logger.info`.
- לא נגעו: צד גפן, `reconciler.py`, כספים2000/סקולקאש. שאר שדות ה-`ichud` (ח.פ/קוד/סכום) עדיין משווים רגיל.
- נבדק על הקבצים לדוגמה: אי-התאמות 27/27 → 9/9 (18 נפתרו סימטרית); קבצי פייסקול ללא התבנית — `ichud` זהה בית-בבית (אפס רגרסיה).
