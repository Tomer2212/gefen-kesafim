# Dev Log

## 2026-09-01 — תיקון גלישת שדות ב"יועצים מלווים" (עריכה, כרטיס בית ספר)
- `SchoolPage.jsx`: במצב עריכה, שלושת עמודות "יועצים מלווים" (גפן/שוטף/מחוז) היו צרות מדי ל-layout של תווית+שדה זה לצד זה, והשדות (בעיקר "זמן לפגישה") גלשו אל מחוץ לכרטיס. הוחלף ל-layout מוערם (תווית מעל השדה) עם `min-w-0` בכל תא; נוסף `stackedLabelCls`, הוסרו `editColGridStyle`/`editLabelCls` שאינם בשימוש
- `HourMinuteInput.jsx`: (הוחזר) ללא `flex-wrap` — "שעות" ו"דקות" נשארים באותה שורה
- `SchoolPage.jsx`: ב"זמן לפגישה" בעריכת "יועצים מלווים" מועבר `inputClassName` צר (`w-11`, `px-0.5`) כדי ששני שדות השעות/דקות + התוויות ייכנסו לשורה אחת בתוך העמודה הצרה

## 2026-09-01 — הבהרה למשתמש חסום: tooltip + טקסט מסך "אין הרשאה"
- `DashboardPage.jsx`: רכיב `LockedCardTooltip` (רקע צהוב "בננה" `#FEF08A`, portal, כמו `CheckLinkTooltip`); ריחוף על אייקון המנעול ליד שם בית הספר מציג "אין הרשאות לצפייה בכרטיס בית הספר."
- `SchoolPage.jsx`: טקסט מסך ה-403 עודכן — מדגיש "זו אינה תקלה", ומסביר לפנות למנהל/בעלים כדי להפעיל את ההרשאה (ניהול → הרשאות). מסך זה מכסה גם את שאר דרכי הכניסה לכרטיס (תזכורות פגישה/משימה, עמוד התראות, שורת פגישה) שכולן מנתבות אליו
