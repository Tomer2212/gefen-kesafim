# Dev Log

## 2026-07-17 — Feat: סנכרון הפוך מ-Outlook + תיקוני סנכרון + הדרכת חיבור מלאה
- **הקשר:** המשך ישיר לעבודת אינטגרציית Outlook. לאחר בדיקות חיות נוספות, נמצאו 4 פערים אמיתיים בסנכרון (לא רק "לבדוק" — קוד חסר בפועל), נבנה פיצ'ר סנכרון הפוך (Outlook → המערכת), ונבנתה הדרכת חיבור מלאה עם צילומי מסך אמיתיים.

### 4 תיקוני סנכרון (נבדקו כולם חי מול Graph/Outlook אמיתיים)
1. **חיבור אנשי קשר ב"אזור אישי"/פאנל ניהול** — התברר שכבר תוקן על ידי המשתמש בעצמו (`buildSchoolContacts` משותף + `contactsFor`) — אומת שעובד נכון, לא נדרש תיקון.
2. **שיוך מחדש של פגישה לבית ספר אחר (`reassign-school`)** — לא הפעיל סנכרון מחדש כלל, השאיר את הכותרת הישנה ב-Outlook. תוקן: מנקה `participants`/`primary_contact_key` (שייכים לבית הספר הישן) ומסנכרן מחדש עם הכותרת הנכונה.
3. **מחיקה/שחזור בית ספר (`delete_school`/`restore_school`)** — פגישות עתידיות לא בוטלו מ-Outlook במחיקה, ולא נוצרו מחדש בשחזור. תוקן ואומת: מחיקה מבטלת בפועל את האירועים (404 מאומת), שחזור יוצר אותם מחדש.
4. **חלונית "מה עושים עם הפגישות" בעת מחיקת משתמש** — `backend`: 3 endpoints חדשים (`GET .../future-meetings`, `POST .../meetings/transfer`, `POST .../meetings/cancel-future`), עם סינון מדויק ל-`status="scheduled"` בלבד (לא `completed`). `frontend`: `UserMeetingsConflictModal.jsx` חדש — טבלת פגישות עתידיות, העברה למשתמש אחר (עם זיהוי התנגשויות בסיכום), הורדת PDF (עם ה-endpoint הגנרי הקיים `/schools/export-pdf`), וביטול מלא (מוחק גם מהמערכת וגם מה-Outlook). מחובר לזרימת מחיקת המשתמש הקיימת ב-`AdminPage.jsx` — אם יש פגישות עתידיות, זו קופצת לפני חלונית האישור הרגילה.

### סנכרון הפוך: Outlook → המערכת (חדש)
- **החלטת ארכיטקטורה:** נבחר **Polling כל 15 דקות** (cron, GitHub Actions) על פני Webhooks — נימוק: webhooks דורשים URL ציבורי (לא ניתן לבדיקה מקומית), subscription שפג תוקף כל ~3 ימים עם חידוש אוטומטי, ותהליך אימות handshake — מורכבות תפעולית לא מוצדקת מול יתרון ה"זמן אמת" שלא קריטי לתיאום פגישות.
- **היקף מכוון (לפי בקשת המשתמש):** רק שינוי תאריך/שעה וביטול/מחיקה מסונכרנים בחזרה. **כותרת האירוע לעולם לא נמשכת בחזרה** — הלוגיקה שלנו (בית ספר/עיר/איש קשר) נשארת הסמכות היחידה לשם.
- **Backend:** `graph_client.get_event()` חדש — שליפת אירוע בודד עם הבחנה קפדנית בין "נמחק בוודאות" (404 או `isCancelled`, `None`) לבין "הבדיקה נכשלה" (`EVENT_CHECK_SKIPPED` — לעולם לא מטופל כמחיקה, רק מדולג לסבב הבא). `POST /schools/meetings/poll-outlook-changes` (חדש, מוגן ב-`X-Cron-Secret` כמו `send-due-reminders` הקיים) — עובר על כל הפגישות העתידיות המסונכרנות, בודק כל אירוע מול Outlook: אם השעה/תאריך השתנו — מעדכן אצלנו; אם האירוע נמחק **עבור כל היועצים המסונכרנים** (לא רק אחד, בפגישות רב-יועציות) — מסמן `status="cancelled"` ומנקה `calendar_sync`. workflow חדש `.github/workflows/outlook-poll.yml` (כל 15 דקות, אותו דפוס בדיוק כמו `meeting-reminders.yml` הקיים).
- **בדיקות שבוצעו (חי, מול Graph אמיתי):** נוצרה פגישת בדיקה מסונכרנת; שונה הזמן שלה **ישירות ב-Outlook** (לא דרך המערכת) — הרצת ה-endpoint אימתה `updated:1` ועדכון נכון בפועל ב-DB; האירוע נמחק **ישירות ב-Outlook** — הרצה נוספת אימתה `cancelled:1` ו-`status="cancelled"` נכון ב-DB. אומת גם ש-401 מוחזר בלי הסוד הנכון. **הערה חשובה:** ה-cron החדש (`outlook-poll.yml`) לא יתחיל לרוץ בפועל עד שהוא נדחף ל-GitHub (main) — כרגע קיים רק מקומית, כמו כל שאר העבודה בסשן הזה.

### הדרכת חיבור מלאה (חדש)
- **חלונית צף גלובלית** (`GuideContext`/`GuideWindow.jsx`) — נשארת פתוחה/ממוזערת גם במעבר בין עמודים, **ניתנת לגרירה ולשינוי גודל** (הועתק במדויק מהמנגנון הקיים של `CompareResultsWindow.jsx`), עם כפתור "הדרכה" חדש ליד כפתור החיבור ב"אינטגרציות".
- **תוכן ההדרכה** (`guideContent.js`, מבנה גנרי לפי מפתח אינטגרציה, מוכן לאינטגרציות עתידיות) — נכתב מחדש ברמת דיוק גבוהה במיוחד (כל שדה/כפתור/ניסוח בדיוק כפי שמופיע במסך האמיתי), כולל **5 צילומי מסך אמיתיים** שהמשתמש צילם תוך כדי ביצוע התהליך בפועל (כולל ניתוק זמני ו-revoke consent אמיתי ב-Azure כדי לקבל מסך הרשאות "טרי" לצילום). הערת MFA מפורטת (כולל הנחיה להתקין Authenticator ולסרוק QR) — הובהר למשתמש שזה שלב שרלוונטי רק אם *ארגון הלקוח* אוכף אותו, לא חלק מהתהליך שלנו.
- **הורדת PDF אמיתית** — משתמשת ב-html2canvas-pro+jsPDF (כמו ב"השוואה בין בדיקות"), מצלמת בפועל את תוכן החלונית כולל התמונות — לא טבלה גנרית.
- **תיקון נלווה:** ה-redirect בסיום תהליך האישור (`calendar_router.py`) לא כלל `tab=integrations`, כך שהמשתמש נחת בטאב "בתי ספר" במקום "אינטגרציות" ולא ראה את הבאנר אוטומטית (רק בלחיצה ידנית על הטאב הבאנר כן הופיע — התנהגות הבאנר עצמה הייתה תקינה). תוקן בשלושת ה-redirects (הצלחה + 2 שגיאה).

### בדיקות ושינויים כלליים
- `python -m py_compile` עבר על כל קבצי ה-backend. `npx vite build` עובר ב-0 שגיאות לאורך כל הסשן. כל נתוני הבדיקה (בתי ספר/פגישות/משתמשים זמניים) נוקו מה-DB ומ-Outlook בסיום כל בדיקה.
- **לא בוצעה בדיקה ידנית מלאה בדפדפן** של: טאב הפגישות בפאנל הניהול מול Outlook, פגישה עם 2+ יועצים, מחיקה מרובה (bulk delete), חיבור כמשתמש בתפקיד "יועץ" (לא owner/manager) — אלה ברשימת הבדיקות הבאות שהוצעו למשתמש.

## 2026-07-17 — Fix: קישורי הורדת קבצים לא עבדו + עמודות "אסמכתאות שנדחו"/"ללא PDF" הציגו "—" מטעה
- **הקשר:** המשך ישיר לתיקון הקודם היום. שני באגים נוספים שהתגלו בבדיקה חיה.
- **באג 1 — לחיצה על שם קובץ ב-tooltip לא הורידה כלום (וגם ה-tooltip נסגר מהר מדי):** ה-tooltip ממוקם *מעל* אייקון ה-✓ עם רווח (`mb-2`) בין הכפתור לחלונית. כש-`hovered` היה נסגר מיידית ב-`onMouseLeave`, האלמנט של ה-tooltip (כולל כפתורי ההורדה בתוכו) נעלם מה-DOM לפני שהעכבר הספיק לחצות את הרווח ולהגיע אליו — כך שגם החלונית "נסגרת מהר מדי" וגם לחיצה על קישור בתוכה לא הייתה יכולה לעבוד (הכפתור כבר לא היה קיים). **תוקן:** ב-`FileCheckCell` וב-`NotCheckedBadge` (`SchoolPage.jsx`) — `onMouseLeave` כעת מתזמן סגירה בעיכוב של 500ms (`setTimeout`) במקום סגירה מיידית; `onMouseEnter` מבטל כל טיימר ממתין. גם נוסף `console.error` על כשל הורדה (היה `catch` שקט) כדי שכשל אמיתי בשרת (למשל שרת `uvicorn` ישן שלא נטען מחדש) יהיה גלוי ב-devtools ולא ייעלם בשקט.
- **באג 2 — עמודות "אסמכתאות שנדחו" ו"ללא PDF" מציגות "—" תמיד בבתי ספר עם ריבוי תקציבים, גם כשהבדיקה בפועל רצה ומצאה 0 ליקויים:** שורש הבאג ב-`analyze_router.py` — בשלושה מקומות (הפונקציה הראשית ושני מסלולי "classify"/"classify2" לעדכון בדיקה קיימת), הקוד היה: `if per_bud_rej: tikhnun["per_budget_rejected"] = per_bud_rej`. כש**כל** התקציבים נקיים לגמרי (0 אסמכתאות שנדחו בכל בית הספר — המקרה הטוב והנפוץ ביותר), `_build_rejected_from_results_clean` מחזירה `{}` (dict ריק) — ש-Python מתייחס אליו כ-falsy, כך שהשדה **מעולם לא נשמר בכלל**. בפרונטאנד (`renderCheckLogCellForBudget`) אין שום דרך להבדיל בין "השדה חסר כי לא בוצע חישוב" לבין "השדה חסר כי הוא היה ריק" — שתי האפשרויות מציגות "—" זהה. **תוקן:** הוסרה בדיקת ה-truthy בשלושת המקומות — השדה נשמר תמיד (גם `{}`), כך שההבדל האמיתי הוא בין `undefined` (חישוב נכשל בפועל, exception) לבין dict (גם ריק) שמייצג "נבדק, 0 נמצאו". בפרונטאנד, `"rejected"`/`"rejected_sum"`/`"no_pdf"`/`"no_pdf_sum"` מציגים כעת `0` אמיתי כשבאמת נבדק ונמצא נקי, ו-`NotCheckedBadge` (✕ עם הסבר) רק כשהחישוב אכן נכשל.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. `python -m py_compile` עבר על `analyze_router.py`. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לוודא לפני העלאה: (א) ריחוף על אייקון ✓ ולחיצה על שם קובץ מורידה אותו בפועל, גם כשלוקח רגע להגיע עם העכבר לחלונית; (ב) בית ספר עם ריבוי תקציבים ו-0 אסמכתאות שנדחו/ללא PDF מציג "0" ולא "—".

## 2026-07-17 — Fix: תיקון אמיתי לבאג "0/+ במקום ✕" בטבלת "בדיקות" (התיקון הקודם היה שגוי חלקית) + קישורי הורדה לקבצים
- **הקשר:** בבדיקה חיה של המשתמש התברר שהתיקון הקודם (מוקדם יותר היום) לא באמת פתר את הבעיה — עמודות "קיים בכספים/גפן" הציגו "—" במקום "0" (שיפור חלקי), אבל עדיין לא את ה-✕ האדום עם ה-tooltip הצהוב שהוגדר; ועמודת "קבצים שזוהו – כספים" עדיין הציגה "+" במקום ✕ כשלא הועלה קובץ כספים כלל.
- **שורש הבאג (`SchoolPage.jsx`, `renderCheckLogCellForBudget`):** התיקון הקודם כלל תנאי `!log.summary?.gefen_only && !log.finance_file_name` — אבל `gefen_only=True` הוא **בדיוק** המקרה של "לא הועלה קובץ כספים בכלל" (מוגדר כך ב-`analyze_router.py`), כך שהתנאי בפועל **החריג** את המקרה שהיה אמור לתפוס, ונפל תמיד ל-"—". **תוקן:** הוסרה בדיקת ה-`gefen_only` — נשארה רק `!log.finance_file_name`, שמזהה נכון כל מקרה של קובץ כספים חסר ומציגה תמיד `NotCheckedBadge` (✕ + tooltip).
- **שורש הבאג השני (`getFileState`):** עמודת "כספים" ב"קבצים שזוהו" נפלה תמיד ל-fallback הכללי `fc.kasafim ? "present" : "absent"` שמחזיר "+" (אייקון "הוספה אופציונלית") כשאין קובץ כספים — במקום "✕" (בדיקה לא הושלמה). **תוקן:** נבדל בבירור בין "תכנון" (`tikhnun`) שהוא באמת אופציונלי (בדיקת גפן-כספים יכולה לרוץ בלעדיו, אז "+" נכון) לבין "כספים" (`kasafim`) שהעדרו תמיד אומר שההשוואה לא בוצעה בכלל — כעת תמיד `"not_checked"` (✕) כשאין קובץ כספים, בלי קשר לתקציב נבחר.
- **הבהרה למשתמש (לא נדרש שינוי קוד):** נבדק ספציפית אם אותה בעיה חוזרת בעמודות "תכנון"/"דיווח ביצוע" — לא, כי "דיווח ביצוע" (doch) הוא תמיד חובה לכל בדיקה קיימת (בלעדיו אין בכלל log), ו"תכנון" (tikhnun) הוא לגיטימית אופציונלי (בדיקת גפן-כספים לא תלויה בו) — "+" שם נכון ומכוון, לא באג.
- **פיצ'ר נוסף שהתבקש: הורדת קבצים מקוריים מתוך ה-tooltip:** שמות הקבצים שמוצגים ב-hover על אייקון ✓ ב"קבצים שזוהו" הפכו ללינקים לחיצים. **Backend:** endpoint חדש `GET /analyze/logs/{log_id}/source-file?name=...` (`analyze_router.py`) — משתמש באותו דפוס בדיוק כמו `add_file_to_check` הקיים לאיתור הקובץ בתוך `summary.stored_file_paths`, ומוריד מ-Supabase Storage; קידוד שם קובץ עברי ב-`Content-Disposition` לפי RFC 5987 (אותו תיקון בדיוק שכבר קיים ועובד ב-`meeting_upload_router.py`, בעקבות הבאג שתועד קודם עם שמות קבצים עבריים). **Frontend:** `downloadLogSourceFile()` חדש ב-`SchoolPage.jsx` (axios blob download, אותו דפוס בדיוק כמו `DownloadButton.jsx` הקיים) — שם קובץ ברשימת ה-tooltip הוא כעת כפתור לחיץ (כחול, עם underline ב-hover) שמוריד את הקובץ; פעיל רק בשורות היסטוריה אמיתיות (`log.id` קיים) — בשורת ה"בדיקה שזה עתה הסתיימה" (pending run, לפני שמירה) השם עדיין טקסט רגיל, כי אין עדיין log id לשלוף לפיו.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. `python -m py_compile` עבר על `analyze_router.py`. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לוודא לפני העלאה: (א) בית ספר עם תכנון+דיווח ביצוע בלי קובץ כספים מציג עכשיו ✕ אדום עם tooltip צהוב (לא "—" ולא "0") גם בעמודות ההשוואה וגם בעמודת "קבצים שזוהו – כספים"; (ב) לחיצה על שם קובץ בתוך ה-tooltip (hover על ✓) אכן מורידה את הקובץ המקורי הנכון.

## 2026-07-17 — Feat: פיצול טבלת "תקציב קבוע" לפי סוג תקציב במצב ריבוי תקציבים
- **הקשר:** בטאב "תקציב קבוע" (`KvuaTab`, `ResultsView.jsx`), כשמזוהים כמה סוגי תקציב (גפן/דוקאטי/תנופה/וכו') כל השורות הוצגו יחד בטבלה אחת עם עמודת "סוג תקציב" נוספת — ביקשת שיוצגו פילים לבחירת סוג תקציב (כמו בטאבים "יוזמות וצרכים"/"דיווח חסר") וטבלה נפרדת לכל סוג.
- **בעיית ההתאמה:** לכל שורת `kvua_rows` (שמגיעה מ-`backend/logic/tikhnun_processor.py` — קובץ מוגן, לא נגעתי בו) יש `budget_type` בתור המחרוזת הגולמית מעמודה A בגיליון "הכל" (למשל `תקציב גפ"ן הכללי`), בעוד רשימת הפילים (`tikhnun.budgets[].name`) משתמשת בשם **מנורמל** (`גפן`). לא ניתן להשוות ביניהם ישירות בבטחה (הבדלי גרשיים/רווחים).
- **תוקן ב-`analyze_router.py`** (לא בתיקיית `logic/`, מותר לעריכה): בלולאה הקיימת שכבר מחשבת `kvua_by_budget` עם `normalize_budget_name`, נוסף תיוג `kr["budget_norm"] = nb` על כל שורת kvua — כעת יש לכל שורה שם מנורמל שמתאים בדיוק ל-`budgets[].name`.
- **`ResultsView.jsx` — `KvuaTab`:** נוספו פילי בחירת תקציב (אותו סגנון/מבנה בדיוק כמו ב-`PartialTab` הקיים) כשיש יותר מתקציב אחד; הטבלה מסננת לפי `row.budget_norm === selectedBudget` ומציגה רק את השורות של התקציב הנבחר, בלי עמודת "סוג תקציב" (הפכה למיותרת כי כל טבלה כבר ספציפית לתקציב אחד). עובד גם בבתי ספר שש-שנתיים (שני `KvuaTab` נפרדים, אחד לכל חלוקה, כל אחד עם הפילים שלו).
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. `python -m py_compile` עבר על `analyze_router.py`. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לוודא מול בית ספר עם ריבוי תקציבים אמיתי שהפילים מוצגים, שכל טבלה מציגה רק את השורות הנכונות, ושה"סה"כ" בתחתית כל טבלה מחושב נכון per-budget.

## 2026-07-17 — Fix: 3 באגים בטבלת "בדיקות" (שורת סיכום מטעה, אייקוני קבצים, פיל "כולם")
- **הסיבה המשותפת לכל 3 הבאגים (`SchoolPage.jsx`):** הטבלה מכילה שתי לוגיקות רינדור מקבילות — אחת "חדשה" ומודעת ל-`per_combo_results`/`gefen_only` (`renderCheckLogCellForBudget`, `getFileState`), ואחת "ישנה" (`renderCheckLogCell`, וקוד ידני נפרד בשורת ה"בדיקה שזה עתה הסתיימה"/pending run) שלא מכירה את המושג "לא בוצעה בדיקה בגלל קובץ חסר" ופשוט מציגה את מה שה-backend שמר.
- **באג 1 — עמודות השוואת גפן-כספים מציגות "0" במקום ✕ כשלא הועלה קובץ כספים:** ב-`_save_check_log` (backend, `analyze_router.py`) `in_finance_not_gefen_count`/`in_gefen_not_finance_count` נשמרים כברירת מחדל `0` כשה-summary לא כולל את השדות בכלל (מצב `gefen_only` — אין קובץ כספים). ב-frontend, `renderCheckLogCellForBudget` היה מנתב ישירות ל-`renderCheckLogCell` הישן (שמציג את ה-`0` הגולמי כפי שהוא) בכל פעם שלא נבחר תקציב ספציפי — כלומר תמיד בבתי ספר עם תקציב יחיד, ותמיד כשלא סומן פיל תקציב. **תוקן:** הלוגיקה של עמודות "קיים בכספים/גפן" עוברת כעת דרך אותה בדיקת `per_combo_results`/`gefen_only`/`finance_file_name` גם ללא תקציב נבחר — קובץ כספים חסר תמיד מציג ✕ עם הסבר, לעולם לא "0".
- **באג 2 — עמודות "קבצים שזוהו" מציגות "+" (הוסף קובץ) במקום ✕ (בדיקה לא בוצעה):** שורת ה"בדיקה שזה עתה הסתיימה" (pending run, לפני שהיא הופכת לשורת היסטוריה שמורה) בנתה בעצמה עותק פשוט של לוגיקת האייקונים (✓/"—" בלבד, בלי מודעות ל-`per_combo_results`) במקום להשתמש ב-`FileCheckCell`/`getLogFileCols` הקיימים ששורות ההיסטוריה השמורות כבר משתמשות בהן. **תוקן:** חולצו `getFileState`/`getFileNotCheckedReason` לפונקציות משותפות ברמת הקובץ, ושורת ה-pending run משתמשת כעת באותן פונקציות ובאותה קומפוננטת `FileCheckCell` בדיוק כמו שורות ההיסטוריה — אין יותר שתי לוגיקות נפרדות.
- **באג 3 — פיל "כולם" מיותר מעל טבלת הסיכום במצב ריבוי תקציבים:** הוסר הפיל הראשון (היה `null` עם התווית "כולם") מרשימת הפילים; מוצגים רק שמות התקציבים שזוהו בפועל. נוסף `useEffect` שמוודא ש-`selectedHistBudget` תמיד מצביע על תקציב אמיתי (התקציב הראשון כברירת מחדל) ברגע שיש יותר מתקציב אחד, כדי שלא יישאר במצב "לא נבחר כלום" שלא מתאים לאף פיל.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק לפני העלאה: (א) בית ספר עם קובץ תכנון+דיווח ביצוע אבל בלי קובץ כספים מציג ✕ (לא 0) בעמודות ההשוואה ו-✕ (לא +) בעמודת "קבצים שזוהו – כספים", גם בשורת ה-pending run וגם אחרי רענון; (ב) בית ספר עם ריבוי תקציבים לא מציג יותר פיל "כולם" והפיל הראשון נבחר אוטומטית.

## 2026-07-17 — Fix: שעת סיום/התחלה נשמרת לפעמים ללא דקות (למשל "12" במקום "12:00")
- **הסיבה (באג-שורש משותף, `MeetingRow.jsx`):** `TimeInput` מתקן את הערך בעצמו ב-`onBlur` (למשל "12"→"12:00"), אבל אירוע ה-blur הזה מבעבע גם ל-`<tr onBlur={handleRowBlur}>` שמבצע את השמירה בפועל — ושני האירועים קורים באותה מחזור-אירוע לפני שה-state מתעדכן, כך ש-`handleRowBlur` שומר את הערך הישן (הלא-מתוקן) ולא את זה שה-`TimeInput` תיקן רגע קודם.
- **החמרה נקודתית באזור האישי:** ב-`SchoolPage.jsx` וב-`AdminMeetingsTab.jsx` יש שכבת הגנה שנייה שמתקנת שוב את השעה ממש לפני השליחה לשרת (`normalizeTime` על ה-payload), אבל ב-`PersonalMeetingsTab.jsx` שכבת ההגנה הזו לא הייתה קיימת כלל — הערך הגולמי (הלא-מתוקן) נשלח ונשמר ב-DB כפי שהוא, לצמיתות.
- **התיקון:** `TimeInput.jsx` — לוגיקת התיקון חולצה לפונקציה משותפת מיוצאת (`normalizeTimeValue`). `MeetingRow.jsx` — `handleRowBlur` מתקן כעת בעצמו את `start_time`/`end_time` לפני ההשוואה/השמירה (לא תלוי בתזמון ה-blur הפנימי של `TimeInput`) — מתקן את הבאג בכל שלושת המקומות בבת אחת כי כולם עוברים דרך אותה קומפוננטה. `PersonalMeetingsTab.jsx` — נוספה אותה שכבת הגנה שנייה שכבר הייתה קיימת ב-2 המקומות האחרים.
- **תיקון נתונים:** אותרה ב-DB פגישה אחת שכבר נפגעה (בית ספר אור יהודה, 23.07.2026) עם `end_time="9"` — תוקנה ל-`"09:00"` לאחר אישור המשתמש.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן.**

## 2026-07-17 — Fix: "אין אנשי קשר מוגדרים" מוצג תמיד באזור אישי ובעמוד כל הפגישות
- **הסיבה:** ב-`MeetingsTable.jsx`, ה-`contacts` שמוצג בבורר המשתתפים (`ParticipantsSelector`) הוא פרופ סטטי יחיד שמוזרם לכל השורות בטבלה, ללא תלות בבית הספר של כל פגישה. זה תקין ב-`SchoolPage.jsx` כי הטבלה שם מוגבלת לבית ספר אחד, אבל ב-`PersonalMeetingsTab.jsx` וב-`AdminMeetingsTab.jsx` (שמציגות פגישות של הרבה בתי ספר יחד) הפרופ הוזן כ-`contacts={[]}` קבוע — כלומר תמיד ריק, בלי קשר לבית הספר בפועל. בנוסף, ב-`AdminMeetingsTab.jsx` רשימת ה-`schools` שנטענה נחתכה מראש ל-`{id, name, symbol, city}` בלבד (שדות אנשי הקשר לא נשמרו כלל שם).
- **התיקון:** `MeetingsTable.jsx` מקבל כעת גם `contactsFor` (פונקציה לפי שורה, בדומה לתבנית הקיימת של `schoolLabelFor`) — אם סופק, נעשה בו שימוש per-row במקום ה-`contacts` הסטטי. חדש: `frontend/src/components/meetings/schoolContacts.js` (`buildSchoolContacts(school)`, אותה לוגיקה כמו `getSchoolContacts()` הקיים ב-`SchoolPage.jsx`). `PersonalMeetingsTab.jsx` ו-`AdminMeetingsTab.jsx` עברו מ-`contacts={[]}` ל-`contactsFor={m => buildSchoolContacts(schools.find(s => s.id === m.school_id))}`; `AdminMeetingsTab.jsx` גם הפסיק לחתוך את שדות בית הספר בטעינה (`setSchools(res.data || [])` במקום מיפוי חלקי).
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לוודא שבורר המשתתפים מציג את אנשי הקשר הנכונים לכל שורה, גם כשיש בטבלה פגישות של כמה בתי ספר שונים בו-זמנית.

## 2026-07-17 — Feat: חסימת ניווט מפגישה חדשה שטרם הושלמה
- **הקשר:** הוספת פגישה יוצרת מיידית שורה אמיתית ב-DB (POST עם ברירות מחדל) עוד לפני מילוי תאריך/שעות/מוסד/יועץ. עזיבת העמוד/הטאב באמצע השאירה פגישה "יתומה" עם נתונים חסרים.
- **קבצים חדשים:** `frontend/src/components/meetings/meetingCompleteness.js` (בדיקת שדות קריטיים חסרים: תאריך/שעת התחלה/שעת סיום תמיד, מוסד/יועץ לפי הקשר), `frontend/src/components/meetings/MeetingNavigationGuardModal.jsx` (מודאל 3 כפתורים: הישאר בעמוד/שמור ועבור/עבור בכל זאת-מחיקה).
- **`SchoolPage.jsx`:** מעקב אחרי פגישות שנוצרו בהפעלה הנוכחית (`sessionCreatedMeetingIdsRef`); שולב לתוך ה-`useBlocker`/`isDirty` הקיים לניווט אמיתי, ועם guard נפרד ב-`onClick` של כפתורי הטאבים הפנימיים (מעבר טאב הוא `useState` מקומי, לא ניווט, אז `useBlocker` לא תופס אותו).
- **`PersonalMeetingsTab.jsx`:** נוסף `useBlocker` חדש (לא היה בשימוש שם) עם אותה תבנית מעקב.
- **`AdminMeetingsTab.jsx`:** הפך ל-`forwardRef` וחושף `getMissingFields`/`discardIncompleteMeetings`; מדווח מצב "יש פגישה לא-שלמה" להורה דרך `onIncompleteChange`.
- **`AdminPage.jsx`:** שולב המצב החדש לתוך ה-`isDirty`/`useBlocker` הקיים (מעבר הטאבים שם כבר עובר דרך `navigate()` אמיתי, אז נתפס אוטומטית); מודאל חדש מוצג לצד ה-`UnsavedChangesModal` הקיים לפי הסיבה.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק את כל 3 הכפתורים בשלושת המקומות (כרטיס בית ספר כולל מעבר טאב פנימי, אזור אישי, עמוד כל הפגישות) לפני העלאה.

## 2026-07-16 — Feat: אינטגרציית יומן Outlook ארגוני (סנכרון פגישות + Free/Busy)
- **הקשר:** בעקבות שיחת אפיון עם לקוח (חברת ליווי כלכלי עם 25 יועצים) — היום כל פגישה מוזנת ידנית גם ביומן Outlook הארגוני וגם במערכת שלנו. המטרה: פגישה שנקבעת/מתעדכנת/מתבטלת במערכת משתקפת אוטומטית ביומן ה-Outlook של כל יועץ רלוונטי, וזמינות היועץ (Free/Busy) מוצגת בתוך המערכת.
- **החלטת ארכיטקטורה מרכזית:** נבחר **App-Only Permissions + Admin Consent חד-פעמי ברמת הארגון** (Microsoft Graph API, client-credentials flow) במקום OAuth דלגייטד per-user — כי המזכירות (לא כל יועץ בנפרד) הן שקובעות את רוב הפגישות. נשמרת גם תשתית ל-OAuth דלגייטד per-user כ-fallback ליועץ עצמאי בלי טננט ארגוני. סנכרון **חד-כיווני בלבד** (מערכת שלנו → Outlook) — לא קוראים בחזרה אירועים שנוצרו ישירות ב-Outlook, רק Free/Busy.
- **DB (Supabase):** טבלה חדשה `calendar_connections` (org-scope ו-user-scope, provider='microsoft'; GRANT ל-service_role בלבד, RLS מופעל). עמודה חדשה `profiles.calendar_sync_email` (override לכתובת הסנכרון, ברירת מחדל = מייל המשתמש). עמודה חדשה `meetings.calendar_sync` (JSONB, מפה per-advisor: `external_event_id`/`status`/`updated_at`) — פגישה עם כמה יועצים יוצרת אירוע נפרד בכל יומן, לא הזמנה משותפת.
- **Backend (`graph_client.py`, חדש):** מודול עצמאי (מחוץ ל-`backend/logic/`) עם `httpx` (בהתאם לדפוס הקיים כבר ב-`email_resend.py`, אין SDK של מיקרוסופט בפרויקט) — client-credentials token caching (מיפוי ישיר של תבנית `run_states`), יצירה/עדכון/מחיקה של אירועים per-advisor, Free/Busy. הצפנת טוקנים ב-Fernet (`CALENDAR_TOKEN_ENCRYPTION_KEY`). כל הפונקציות non-fatal כלפי הקורא — כישלון בגרף לא מפיל את שמירת הפגישה.
- **Backend (`routers/calendar_router.py`, חדש):** `GET /calendar/connection`, `GET /calendar/connect/microsoft/admin-consent-url` (owner/manager), `GET .../callback` (מקבל redirect דפדפני ממיקרוסופט — משתמש ב-state token מוצפן Fernet במקום JWT כי אין Authorization header בניווט דפדפן), מסלול OAuth דלגייטד אישי (`/connect/microsoft/personal/*`), `GET/PATCH /calendar/sync-email`, `GET /calendar/freebusy`.
- **Backend (`schools_router.py`):** חוברו קריאות סנכרון (non-fatal, try/except נפרד) בארבע נקודות: `create_meeting`, `update_meeting` (PUT), `patch_meeting` (PATCH — כולל ביטול פגישה שמוחק את האירועים החיצוניים), `delete_meeting` (מבטל אירועים לפני מחיקת השורה).
- **Frontend:** טאב חדש "אינטגרציות" ב-`AdminPage.jsx` (owner/manager, קומפוננטה נפרדת `AdminIntegrationsTab.jsx`) עם כפתור "חבר יומן ארגוני". שדה `calendar_sync_email` נוסף ל-`SettingsModal` הקיים ב-`NotificationsPage.jsx`. תג סטטוס סנכרון-יומן ב-`MeetingRow.jsx` (`CalendarSyncBadge`, נשאב מ-`meeting.calendar_sync` בלי קריאת API נוספת). תצוגת Free/Busy ב-`DatePickerPopover.jsx` — נקודה כתומה על ימים שהיועץ תפוס בהם ביומן (על בסיס `advisor_ids[0]` של הפגישה).
- **בדיקות שבוצעו:** כל שלוש המיגרציות אומתו ישירות מול Supabase (עמודות, GRANT, RLS, policy). `python -m py_compile` עבר על כל קבצי ה-backend החדשים/שהשתנו. `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה מקצה-לקצה מול Microsoft Graph אמיתי** — `MS_CLIENT_ID`/`MS_CLIENT_SECRET` עדיין placeholder ב-`.env`. **דורש לפני שימוש בפועל:** רישום אפליקציה ב-Azure AD (מומלץ קודם בטננט חינמי של Microsoft 365 Developer Program לבדיקה, לפני חיבור לטננט אמיתי של לקוח), עדכון `MS_CLIENT_ID`/`MS_CLIENT_SECRET`/`MS_REDIRECT_URI` ב-`.env` (וב-Render בזמן ההעלאה), ומתן Admin Consent על ידי Global Admin של הארגון. בדיקה ידנית מלאה בדפדפן (יצירה/עדכון/ביטול פגישה + תצוגת Free/Busy) טרם בוצעה.

## 2026-07-14 — Fix: 3 תיקונים נוספים לפורטל העלאת קבצים, בעקבות בדיקה חיה שלישית
- **Frontend (`MeetingUploadPage.jsx`):** תאריך הפגישה בדף ההעלאה הציבורי הוצג בפורמט ISO (`2026-07-15`) — שונה לפורמט `DD/MM/YYYY`.
- **Frontend (`NotificationsPage.jsx` — `MeetingFilesArrivedDetail`):** הוחלף סדר שני כפתורי הפעולה ("בצע בדיקה" עכשיו ראשון/ימני), צבע "בצע בדיקה" שונה לירוק כהה (`bg-green-700`), טקסט "שלח בקשה למנהלנית" עודכן ל"שלח בקשה מחודשת למנהלנית".
- **Fix התנהגותי — יעד ניווט שגוי אחרי "בצע בדיקה":** הכפתור ניווט בעבר ל-`/check?run_id=...` (מסך ההעלאה/תוצאה החד-פעמי הישן), לא למה שהתבקש. תוקן לניווט ל-`/school/{schoolId}?tab=checks` (טאב "בדיקות" של בית הספר הרלוונטי) — הבדיקה עצמה ממשיכה לרוץ ברקע בדיוק כמו קודם, רק יעד הניווט השתנה. נדרש שינוי נלווה ב-`SchoolPage.jsx`: נוסף תמיכה בפרמטר `?tab=` לבחירת טאב פתיחה (`info`/`meetings`/`goals`/`checks`), באותו דפוס בדיוק כמו התמיכה הקיימת ב-`?meeting=`.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** של הניווט ל-`?tab=checks` בפועל — מומלץ לוודא בבדיקה החיה שהבדיקה החדשה אכן מופיעה בטאב "בדיקות" לאחר שהיא מסתיימת ברקע.

## 2026-07-14 — Fix: 5 תיקוני UX נוספים לפורטל העלאת קבצים, בעקבות בדיקה חיה שנייה
- **Fix קריטי — הורדת קובץ עם שם עברי נכשלה בשקט (`meeting_upload_router.py`):** `Content-Disposition` בנה כותרת HTTP עם שם קובץ עברי גולמי — כותרות HTTP חייבות להיות ASCII/Latin-1, ולכן זה קרס עם `UnicodeEncodeError` בכל פעם שהשם המקורי הכיל עברית (אומת ישירות: זו הייתה בדיוק השגיאה). קבצים עם שם אנגלי בלבד עבדו במקרה. תוקן לפי RFC 5987 (`filename` בסיסי ASCII + `filename*=UTF-8''...` מקודד) — אומת שהבנייה כבר לא נכשלת.
- **Frontend (`MeetingUploadPage.jsx`):** כותרת הדף שונתה מ"העלאת קבצים לפגישה בבית הספר X" ל"העלאת קבצים לקראת הפגישה" (עם שם בית הספר+תאריך בשורת המשנה). זרימת ההעלאה שונתה מ"מעלה מיד עם הבחירה" ל**"בחירה → סקירה → שליחה"**: קבצים שנבחרו/נגררו מוצגים כרשימה עם כפתור הסרה (✕) לכל קובץ, וההעלאה בפועל קורית רק בלחיצה על "שליחת קבצים". הודעת "עדיין חסר..." (ותודה בסיום) הוחלפו מבאנר קטן לחלונית מודאלית בולטת (`UploadResultModal`, עם `useFocusTrap`).
- **Frontend (`NotificationsPage.jsx` — `MeetingFilesArrivedDetail`):** שני כפתורי הפעולה ("שלח בקשה למנהלנית" ו"בצע בדיקה") מוצגים כעת **יחד תמיד** (זה לצד זה, גודל/עיצוב זהה) במקום רק אחד מותנה — "שלח בקשה" מוסתר רק כשאין מה לבקש (הכל התקבל), "בצע בדיקה" זמין תמיד כשיש לפחות קובץ אחד, כדי שההחלטה (לחכות לחסרים או להריץ עכשיו עם מה שיש) תהיה תמיד בשיקול דעת המשתמש.
- **הבהרה (לא נדרש שינוי קוד):** אומת שההשוואה ו"בצע בדיקה" תמיד מסתכלים על **כלל** הקבצים שהועלו אי-פעם לפגישה (לא רק העלאה אחרונה) — צבירה נכונה כבר קיימת מעצם המבנה (כל השאילתות מסננות לפי `meeting_id` בלבד, בלי הגבלת חלון זמן/session).
- **בדיקות שבוצעו:** אומת ישירות (השוואת קוד ישן מול חדש) שבניית ה-Response עם שם קובץ עברי גולמי אכן זורקת `UnicodeEncodeError` (משחזר בדיוק את התקלה שדווחה), ושהתיקון לפי RFC 5987 בונה בהצלחה. `npx vite build` עובר ב-0 שגיאות. **לא בוצעה עדיין בדיקה ידנית מלאה בדפדפן של זרימת "בחירה→סקירה→שליחה"** — מומלץ לבדוק גרירת כמה קבצים, הסרת קובץ מהרשימה, ולחיצה על "שליחת קבצים" בפועל לפני שמסתמכים על זה מול לקוח אמיתי.

## 2026-07-14 — Feat: שיפורי UX לפורטל העלאת קבצים (שלב ב'), בעקבות בדיקה חיה
- **הקשר:** אחרי בדיקה חיה של המשתמש בתפקיד "מנהל"/"מנהלנית"/יועץ, עלו 4 בעיות UX שתוקנו.
- **ניסוח מייל המנהלנית (`_build_secretary_upload_email_html`):** עודכן פעמיים לפי משוב — כולל שם היועץ המבצע ("עם X"), שעת הפגישה, ותאריך; משתמש עכשיו ב-`when_bet` (בלי ל'-prefix, "שתתקיים מחר"/"שתתקיים ביום ראשון") במקום `when_lamed`.
- **בדיקה בזמן אמת בעת העלאה:** `GET /public/meeting-upload/{token}` ו-`POST .../files` מחזירים כעת `items` עם דגל `received` לכל פריט (לא רק רשימה סטטית) — דף ההעלאה מציג ✓/○ חי, ואחרי כל העלאה מוצגת הודעה מדויקת "תודה על המאמץ שלך, אבל אנחנו צריכים שתעלי גם את X" עם שמות הפריטים החסרים בפועל, בלולאה עד שהכל מתקבל.
- **כותרת התראה מפורטת:** `_notify_advisors_files_arrived` מקבל כעת שם המנהלנית/אחראית הכספים (מזוהה מ-`participants`), שם בית הספר, ותאריך הפגישה — הכותרת עכשיו "התקבלו קבצים מ{שם} לקראת הפגישה עם בית הספר {שם} ב{תאריך}" במקום כותרת גנרית.
- **הרחבה במקום (לא ניווט):** נוסף `ref_id=meeting_id` לשורת ההתראה; `NotificationsPage.jsx` מטפל בסוג `meeting_files_arrived` באותו דפוס בדיוק כמו `update_request_submitted` (לחיצה מרחיבה במקום, לא מנווטת). קומפוננטה חדשה `MeetingFilesArrivedDetail` מציגה טבלת קבצים (שם קובץ כלינק הורדה אמיתי + סוג קובץ), רשימת ✓/✗, וכפתור מותנה — "שלח למנהלנית בקשה לקבצים חסרים" או "בצע בדיקה" (מריץ את אותו מסלול `run-check-from-uploads` הקיים). נוספו endpoints חדשים ב-`meeting_upload_router.py`: `GET .../uploaded-files` (עם `file_type_label` חדש ב-`meeting_upload_logic.py`) ו-`GET .../uploaded-files/{id}/download` (proxy מאומת ל-Storage, אותו דפוס כמו הורדות קיימות ב-`analyze_router.py`).
- **בדיקות שבוצעו:** מקצה-לקצה מקומית מול ה-DB האמיתי — checklist חי אומת עם קבצים אמיתיים (העלאה חלקית → הודעת חוסרים מדויקת → העלאת השאר → עדכון נכון), כותרת ההתראה אומתה במלואה עם שם+בית ספר+תאריך, `file_type_label` אומת נכון לשלושת סוגי הקבצים (גפן/תכנון/כספים), ושני הניסוחים החדשים של המייל (מחר/יום אחר) נבדקו ישירות מול הפונקציה. `npx vite build` עובר ב-0 שגיאות. נוקו כל נתוני הבדיקה (DB + Storage).

## 2026-07-12 — Feat: יומן עדכונים לכל שורה בטבלת "דיווח חסר"
- **הקשר:** בטאב "דיווח חסר" (תוך תוצאות בדיקת תקציב תקנוני) לא הייתה שום דרך לתעד הערות על מענה ספציפי בזמן פגישה עם בית הספר. נוספה עמודת "עדכונים" חדשה שמאפשרת ליועץ לתעד, לכל מענה בנפרד, מה ההסבר של בית הספר ומה צריך לתקן.
- **מודל הנתונים:** ההערות **עוקבות אחרי המענה עצמו** (לפי `row.key` היציב שכבר קיים בחישוב השרת, בשילוב בית ספר+חלוקה+תקציב) ולא אחרי בדיקה בודדת — כך שכשמריצים בדיקה חדשה על אותה חלוקה, כל היסטוריית העדכונים על אותו מענה עדיין מופיעה. לכל מענה יש "יומן" של רשומות (groups); כל רשומה יכולה לצבור כמה "תוספות" (segments) מכמה משתמשים שונים לאורך זמן — לחיצה על "+ הוספת עדכון חדש" יוצרת רשומה חדשה, לחיצה על רשומה קיימת מוסיפה תוכן לאותה רשומה בלי למחוק את הקיים.
- **הרשאות:** יועץ יכול לערוך רק תוכן שהוא כתב בעצמו (לא של משתמש אחר), אך יכול תמיד להוסיף תוספת חדשה לרשומה של מישהו אחר. owner/manager יכולים לערוך או למחוק כל תוספת.
- **DB (Supabase):** טבלה חדשה `partial_row_updates` (שורה = תוספת בודדת; `group_id` מקבץ תוספות לאותה רשומה חזותית) — נגישה רק ל-`service_role`, RLS מופעל, בלי GRANT ל-`authenticated` (הפרונטאנד לא פונה אליה ישירות).
- **Backend (`schools_router.py`):** endpoints חדשים תחת `/schools/{school_id}/partial-updates` — `POST .../batch` (שליפה מרוכזת של כל השורות המוצגות בטאב בשאילתה אחת), `POST` (יצירת רשומה חדשה), `POST .../{group_id}/segments` (הוספת תוספת לרשומה קיימת), `PATCH .../segments/{id}` ו-`DELETE .../segments/{id}` (עריכה/מחיקה עם בדיקת הרשאה).
- **Frontend:** קומפוננטה חדשה `PartialRowUpdatesModal.jsx` (חלונית עם טבלת תאריך/משתמש/תוכן, צביעה פסטלית לפי כותב) + עמודת "עדכונים" חדשה ב-`PartialTab` (`ResultsView.jsx`) שמציגה את התוספת האחרונה ומאפשרת פתיחת החלונית המלאה.
- **בדיקות שבוצעו:** מיגרציית ה-DB אומתה בפועל (עמודות, RLS, הרשאות). `backend/main.py` נבדק שנטען בלי שגיאות ייבוא. השרת הופעל זמנית מקומית וה-endpoints החדשים נבדקו שמגיבים כראוי (403 ללא אימות, לא קריסה) ואז נסגר. `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית מלאה בדפדפן** (דורשת קבצי גפן/תכנון אמיתיים ובית ספר עם "דיווח חסר" בפועל) — מומלץ לבדוק בדפדפן לפני העלאה לאתר, כולל תרחיש ה"הוספה לרשומה קיימת" ע"י שני משתמשים שונים.
- **🐛 באג קיים-מזמן שהתגלה ותוקן תוך כדי בדיקה ידנית (לא נגרם ע"י השינוי הזה — אומת ב-`git diff`/`git show HEAD`):** בבתי ספר שש-שנתיים (דו-חלוקתיים, `isDualTikhnun`), `PartialTab` בטאב "דיווח חסר" קיבל `activeBudgetIdx`/`setActiveBudgetIdx` כ-props חובה בלי נפילה-חזרה למצב פנימי — וב-`ResultsView.jsx` הקריאה במצב דו-חלוקתי (`<PartialTab tikhnun={tikhnunTikkon} />`) מעולם לא העבירה את ה-props האלה. התוצאה: כפתורי בחירת תקציב (גפן/תקומה/וכו') לא הוצגו בכלל בטאב הזה בבתי ספר דו-חלוקתיים, והתצוגה נשארה תקועה על התקציב הראשון ברשימה — כך שאם דווקא לתקציב הראשון אין ליקויים בבדיקה נתונה, הטאב הציג "כל התוכניות דווחו במלואן" למרות שתקציבים אחרים כן הכילו דיווח חסר (המידע לא אבד, פשוט לא היה נגיש). **תוקן** ע"י החלת אותה תבנית fallback שכבר קיימת ועובדת ב-`SikarTab` (props אופציונליים + `useState` פנימי כברירת מחדל) — עכשיו הכפתורים עובדים גם בבתי ספר דו-חלוקתיים, בלי לשנות שום קריאה קיימת (`npx vite build` עובר ב-0 שגיאות).
- **תיקונים נוספים לפי בדיקה ידנית של המשתמש (יומן העדכונים):**
  - **צביעה:** קודם כל תוספת נצבעה מיד (גם כשהיה כותב יחיד לתוכנית), מה שיצר צבע מיותר. **תוקן:** צביעה מופיעה רק כשיש 2+ כותבים שונים באותו יומן (`row_key`) — כותב יחיד נשאר ללא צבע; ברגע שמצטרף כותב שני, כל התוספות בכל היומן (כולל הישנות) מקבלות צבע לפי כותב.
  - **הרשאות עריכה/מחיקה — עודכן להיררכיית תפקידים:** בעדכון קודם owner/manager יכלו לערוך/למחוק תוספת של **כל** משתמש אחר, כולל אחד עם תפקיד גבוה יותר. המשתמש ציין בבדיקה שזה שגוי — manager לא אמור להיות מסוגל לערוך/למחוק הערה של owner. **תוקן** (גם בצד השרת וגם בצד הלקוח): עריכה/מחיקה של תוספת של משתמש אחר מותרת רק אם לפעיל תפקיד **גבוה ממש** מתפקיד הכותב (owner>manager>advisor) — למשל manager יכול לפעול על תוספת של advisor אך לא של owner; owner יכול לפעול על מנהל/יועץ. הבדיקה השרתית (`schools_router.py`) שולפת את תפקיד הכותב מ-`profiles` ונכשלת-סגורה (מסרבת) אם התפקיד לא ידוע, כדי שלא תתאפשר עריכה לא-מורשית במקרה של תקלה בשליפה.
- **בקשת המשתמש להסרת "הוספה לרשומה קיימת" (פישוט):** לאחר בדיקה נוספת, המשתמש ביקש לבטל לגמרי את האפשרות להוסיף תוכן לרשומה קיימת של מישהו אחר — כל עדכון חדש (כולל מאותו משתמש) חייב ליצור רשומה **עצמאית** חדשה משלו דרך "+ הוספת עדכון חדש" בלבד. **בוצע:** הוסר ה-endpoint `POST /partial-updates/{group_id}/segments` מה-backend (`schools_router.py`) והוסרה לגמרי ההתנהגות המתאימה מה-`PartialRowUpdatesModal.jsx` (לחיצה על רשומה קיימת כבר לא פותחת שדה הוספה). הצביעה נשארה בדיוק כמו שהייתה (רק כשיש 2+ כותבים שונים באותו יומן) וגם הרשאות העריכה/מחיקה נשארו ללא שינוי נוסף — רק ההוספה-לרשומה-קיימת בוטלה. `npx vite build` עובר ב-0 שגיאות.

## 2026-07-10 — Feat: מעבר הצ'אטבוט לחיוב ישיר מול Google (במקום OpenRouter)
- **הקשר:** גילינו ש-OpenRouter לא מוסיף markup על מחיר הטוקנים עצמו (זהה לגוגל), אבל גובה עמלת טעינת קרדיט של כ-5.5%. עברנו לחיוב ישיר מול Google כדי לחסוך את העמלה הזו, בלי לשנות שום דבר אחר בחוויית המשתמש.
- **ממצא קריטי שהתגלה תוך כדי בדיקה:** ה-caching המפורש (`cache_control`) שהיה בשימוש מול OpenRouter **לא רלוונטי** מול ה-API הישיר של גוגל — שם צריך ליצור "אובייקט cache" נפרד עם עלות אחסון שוטפת ($1 למיליון טוקן לשעה, ללא קשר לשימוש בפועל). עם המסמך הגדול שלנו (128K טוקן), cache שנשאר "חם" 24/7 היה עולה כ-90$/חודש — יותר מכל מה שחסכנו. **בדקנו בפועל** (לא הנחנו) אם ה-caching האוטומטי (implicit) של גוגל עובד כשפונים ישירות בלי OpenRouter באמצע — **והוא עובד מצוין**: קריאה שנייה עם אותו system prefix הראתה `cached_tokens: 122,847` מתוך 127,882 (כ-96% hit), בלי שום קוד caching בכלל. המסקנה: הכישלון הקודם (שראינו מול OpenRouter) היה ספציפי לניתוב/proxy של OpenRouter, לא לגוגל עצמה — כך שהמימוש הסופי פשוט בהרבה ממה שתוכנן במקור (בלי ניהול מחזור-חיים של cache).
- **בחירת מודל:** `gemini-2.5-flash` (ששימש עד כה) התגלה כ**חסום לגמרי** למפתחות/פרויקטים חדשים בגוגל (גם דרך ה-API הישיר וגם דרך OpenRouter בעתיד). מתוך המודלים הזמינים בפועל למפתח החדש, הושוו `gemini-flash-latest` (=3.5-flash כרגע) מול `gemini-3.1-flash-lite` — נמצא הפרש תמחור של **פי 6** בכל המימדים (קלט/פלט/cache). נבחר **`gemini-3.1-flash-lite`**, כי המשימה (מענה מבוסס מסמך נתון) לא דורשת יכולת reasoning מתקדמת, ועם caching בפועל העלות ירדה לכ-**0.46 סנט להודעה** (זול אפילו יותר מהפתרון הקודם מול OpenRouter).
- **Backend (`chatbot_config.py`):** נוספו `GOOGLE_API_KEY`, `GOOGLE_URL`, `GOOGLE_MODEL = "gemini-3.1-flash-lite"`, ומתג `CHATBOT_PROVIDER` (`google`/`openrouter`, ברירת מחדל `google`). קבועי `OPENROUTER_*` **נשארו ללא שינוי** — לפי בקשת המשתמש, OpenRouter נשאר כגיבוי לא-פעיל; חזרה אליו = שינוי משתנה סביבה אחד, לא שינוי קוד.
- **Backend (`chatbot_router.py`):** נוספה `_stream_google` (אותו מבנה בדיוק כמו `_stream_openrouter` — SSE passthrough, טיפול שגיאות זהה), ו-`ask()` בוחר בין שני הספקים לפי `CHATBOT_PROVIDER`. לוגיקת המכסה היומית (`_check_quota`/`_record_usage`) לא תלויה בספק ונשארה בדיוק כמו שהיא.
- **🐛 באג שהתגלה ותוקן תוך כדי בדיקה:** ה-endpoint של גוגל שולח את שדה ה-`usage` על **מספר chunks בזרימה** (5 בבדיקה שבוצעה), לא רק בצ'אנק האחרון כמו OpenRouter — הקוד המקורי היה מריץ את `_record_usage` (הגברת המכסה) בכל פעם שמזהה `"usage"` בשורה, מה שגרם ל**ספירת יתר של פי 5** על כל הודעה אמיתית אחת! תוקן ע"י דגל `usage_recorded` בתוך שתי פונקציות ה-streaming (גם ב-`_stream_openrouter`, כאמצעי זהירות) — מבטיח הגברה של **פעם אחת בדיוק** לכל בקשה, בלי קשר למספר ה-chunks שמדווחים usage.
- **בדיקות שבוצעו (מקצה-לקצה, מול Google API ו-Supabase האמיתיים, לא הנחות):** קריאה בסיסית ישירה אימתה תשובה עברית תקינה; שתי קריאות רצופות עם אותו system prefix אימתו caching אוטומטי בפועל (`cached_tokens` עלה מ-0 ל-122,847); streaming (SSE) נבדק ואומת תואם לפורמט שהפרונטאנד הקיים כבר יודע לפרסר; זרימת HTTP מלאה נבדקה עם `TestClient` על ה-endpoint האמיתי `/chatbot/ask` — **לפני** התיקון הראתה `message_count=7` על הודעה אחת (הבאג), **אחרי** התיקון הראתה נכון `message_count=1`; מסלול הגיבוי (`CHATBOT_PROVIDER=openrouter`) נבדק בנפרד ואומת שעדיין עובד בשלמותו. `npx vite build` לא נדרש — **אין שום שינוי בפרונטאנד**, המעבר כולו בצד השרת. כל נתוני הבדיקה נוקו מ-Supabase בסיום.
- **דורש פעולה לפני העלאה לאתר:** להוסיף `GOOGLE_API_KEY` ו-`CHATBOT_PROVIDER=google` גם למשתני הסביבה ב-Render (לא רק ל-`.env` המקומי).

## 2026-07-10 — Feat: כפתור "הורד PDF" בחלון השוואה בין בדיקות
- ב-`CompareResultsWindow.jsx` נוסף כפתור "הורד PDF" בקצה השמאלי של שורת התאריכים/שמות הבדיקות המושוות, בצבע אפור (תואם לגוון הכותרת של החלון) במקום כתום.
- הכפתור מצלם את ה-DOM בפועל של האזור המוצג (כותרת, תאריכים, פילות תקציב אם יש, ותוכן "כללי"+"מענים" של התקציב הפעיל) וממיר אותו ל-PDF (`jspdf`) — נאמנות מלאה למה שמוצג על המסך (צבעים, מבנה), כולל תוכן שגולש מעבר לגובה הנראה של החלון (עוקף זמנית את ה-`overflow-auto`). כפתורי סגירה/מזעור אינם נכללים בצילום (`data-html2canvas-ignore`).
- **Fix:** ההורדה נכשלה בשקט (בלי שגיאה גלויה) כי Tailwind v4 מייצר צבעים בפורמט `oklch` שהספרייה המקורית `html2canvas` לא יודעת לפרסר (זורקת "Attempting to parse an unsupported color function") — הוחלף ל-`html2canvas-pro` (פורק מתוחזק שתומך ב-oklch/lab/color-mix), ונוסף טיפול שגיאה גלוי למשתמש (כפתור אדום "שגיאה, נסה שוב" + `console.error`) במקום בליעה שקטה של החריגה.
- תלויות בפרונטאנד: `html2canvas-pro`, `jspdf`. `npx vite build` עובר ב-0 שגיאות.
- **Fix:** ה-PDF שירד הציג רק את מה שנראה בפריים הראשון (בלי גלילה) — כי לחלון עצמו יש גובה קבוע + `overflow-hidden` (מעבר לגלילה הפנימית של גוף התוכן), כך שרק ביטול הגלילה של גוף התוכן לא הספיק. עכשיו לפני הצילום מבטלים זמנית גם את החיתוך של החלון החיצוני (`height: auto`, `overflow: visible`) בנוסף לגוף הפנימי, וממתינים ל-2 פריימים (`requestAnimationFrame`) שהדפדפן יישם את הפריסה החדשה לפני הקריאה ל-`html2canvas` — כך כל התוכן (כולל שורות שדרשו גלילה) נכלל בקובץ שיורד.

## 2026-07-09 — Feat: הגבלת שימוש יומית (Rate Limiting) לצ'אטבוט AI + מסך נראות למנהלים
- **הקשר:** לצ'אטבוט (שכבר עבד) לא הייתה שום הגנה מפני שימוש לא הוגן (לולאת באג, טוקן שדלף, ניצול לרעה) — כל בקשה עולה כסף אמיתי מול OpenRouter. נוספה מכסה יומית + מסך נראות למנהלים, לפי בקשת המשתמש.
- **DB (Supabase):** טבלה חדשה `chatbot_usage_daily` (`user_id` + `usage_date` כמפתח מורכב, `message_count`) — נגישה רק ל-`service_role` (אין GRANT ל-`authenticated`, הפרונטאנד לא פונה אליה ישירות), RLS מופעל + מדיניות deny-all מפורשת. פונקציית RPC אטומית `increment_chatbot_usage` (`INSERT ... ON CONFLICT ... DO UPDATE`) — נדרשת כי השרת רץ על 4 workers ב-Render, וקריאה-ואז-כתיבה רגילה מ-Python הייתה יוצרת מרוץ (lost update) בדיוק בתרחיש שהפיצ'ר אמור לתפוס (הרבה בקשות בו-זמנית).
- **Backend (`chatbot_router.py`):** `_check_quota()` — בדיקה (SELECT פשוט, לא ה-RPC) לפני פתיחת הזרימה ב-`/chatbot/ask`; אם המכסה האישית (10/יום) או הגלובלית (100/יום, ניתנות לכוונון דרך `.env`) עברה — מוחזר `429` מיידי עם `{"detail": {"reason": "user_limit"|"global_limit"}}`, בלי לקרוא בכלל ל-OpenRouter (אין עלות). ההגברה בפועל (`_record_usage`, קורא ל-RPC) קורית רק **אחרי** תשובה מוצלחת (בתוך ה-branch הקיים שמזהה את ה-`usage` chunk האחרון מ-OpenRouter) — לא לפני הקריאה — כך שבקשה שנכשלת לא "צורכת" מהמכסה של המשתמש. יש חלון מרוץ קטן ומקובל בין הבדיקה להגברה (כמה בקשות בו-זמניות עלולות לחרוג קצת מהמכסה) — מתקן את עצמו בבקשה הבאה, הוחלט שזה עדיף על חיוב מכסה על בקשות שנכשלו.
- **Backend — endpoint חדש `GET /chatbot/usage-today`:** מוגבל ל-owner/manager (403 לאחרים), מחזיר רשימת משתמשים + כמות הודעות היום לכל אחד (עם שמות מ-`profiles`, לפי כלל ה-two-step PostgREST של הפרויקט, עטוף ב-try/except לא-קריטי) + הסכום הגלובלי + שתי המכסות הנוכחיות.
- **Frontend (`ChatWidget.jsx`):** טיפול ייעודי בתשובת 429 — הודעת עברית שונה למכסה אישית ("הגעת למכסת השאלות היומית... נסה שוב מחר") לעומת גלובלית ("העוזר עמוס... נסו שוב מאוחר יותר"), לפני הבדיקה הכללית הקיימת של שגיאות (כי 429 מגיע כ-JSON רגיל, לא כ-SSE).
- **Frontend (`AdminPage.jsx`):** כרטיס "שימוש בעוזר ה-AI היום" בתוך טאב "משתמשים" הקיים (לא טאב חדש) — טבלה של שם משתמש + מספר הודעות מתוך המכסה האישית, ושורת סיכום עם הסכום הגלובלי מתוך המכסה הגלובלית. נטען פעם אחת בכניסה לטאב (owner/manager בלבד), אותו דפוס כמו טעינת המשתמשים הקיימת.
- **אבטחה:** `OPENROUTER_API_KEY` נשאר אך ורק בצד השרת (`.env`, ב-`.gitignore`) — אומת שהוא לא נחשף בשום תשובת API. שני ה-endpoints (`/ask`, `/usage-today`) דורשים אימות; `/usage-today` דורש גם הרשאת owner/manager.
- **בדיקות שבוצעו (מקצה-לקצה, מול Supabase ו-FastAPI האמיתיים):** RPC נבדק ישירות (3 קריאות רצופות → ספירה נכונה 1,2,3, בלי איבוד עדכון). `_check_quota` נבדק ישירות — מתחת למכסה עובר בלי חריגה, בדיוק במכסה זורק `429` עם `{"reason": "user_limit"}`. זרימת HTTP מלאה נבדקה עם `TestClient` (dependency override על `get_current_user`, בלי לדלג על שום לוגיקה אמיתית): `/chatbot/usage-today` על משתמש ריק מחזיר מבנה תקין עם המכסות (10/100); לאחר דחיפת המשתמש למכסה — `/chatbot/ask` מחזיר בדיוק `429` עם ה-JSON הצפוי, ו-`/chatbot/usage-today` משקף נכון `message_count=10`. כל נתוני הבדיקה נוקו מהטבלה בסיום. `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** של הודעות ה-429/מסך המנהל — מומלץ לבדוק לפני העלאה לאתר.

## 2026-07-06 — Feat: פורטל העלאת קבצים למזכירה/אחראית כספים לפני פגישה (שלב ב')
- **הקשר:** המשך ישיר לשלב א' (תזכורות מייל). עכשיו, כשיש בפגישה איש קשר מסוג "מנהלנית"/"אחראית כספים" (`participants` עם `key` בערך `secretary`/`finance`), אותו `reminder_enabled` הקיים גם שולח לה מייל משולב (תזכורת + checklist מדויק + קישור להעלאת קבצים) — בלי שום כפתור נוסף שהיועץ צריך ללחוץ.
- **DB (Supabase):** 3 טבלאות חדשות — `meeting_upload_tokens` (טוקן לשימוש חוזר, תוקף = מועד הפגישה + 3 ימי חסד), `meeting_upload_files` (קובץ שהועלה + סיווג: `identified_type`/`division_type`/`budgets`), `meeting_upload_followups` (יומן שליחת מייל מעקב חוסרים). כולן GRANT ל-service_role בלבד + RLS + policy יחיד, כמו `meeting_reminders`.
- **Backend (`meeting_upload_logic.py`, חדש):** `build_upload_checklist()` — בונה checklist לפי `check_metrics` **מסונן לפי שנת הלימודים הנוכחית בלבד** (לא שנים קודמות, כי סוגי תקציבים יכולים להשתנות); בית ספר ללא נתוני בדיקה לשנה הנוכחית מקבל checklist גנרי + דגל `no_baseline_this_year`. `compute_upload_comparison()` — דיף בין הצפוי למה שהתקבל בפועל (לא מריץ שום התאמה אמיתית).
- **Backend (`routers/meeting_upload_router.py`, חדש):** שני endpoints ציבוריים ללא אימות (`GET/POST /public/meeting-upload/{token}`) — המזכירה גוררת קבצים, כל קובץ מסווג באמצעות `identify_file()` הקיים + `_detect_gefen_division()`/`extract_plan_roster()` הקיימות (שימוש חוזר מלא, ללא נגיעה ב-`backend/logic/`), נשמר ב-Storage תחת `meeting-uploads/{meeting_id}/...`, ונוצרת התראה ליועצים דרך `_create_notifications` הקיים. שני endpoints מאומתים: `GET .../upload-comparison` (חישוב הדיף) ו-`POST .../request-missing-files` (הכפתור היחיד בפיצ'ר — שולח מייל ממוקד רק על מה שחסר בפועל).
- **Backend (`analyze_router.py`):** נוסף `POST /analyze/meetings/{meeting_id}/run-check-from-uploads` — משכפל בדיוק את דפוס ה-Storage-download הקיים של `/add-file/{log_id}` כדי להריץ בדיקה אמיתית מהקבצים שכבר הועלו, בלי להעלות שוב.
- **Backend (`email_resend.py`, חדש):** אינטגרציית Resend דרך `httpx` (כבר תלות קיימת בפרויקט) — עבור שני סוגי המייל החדשים בלבד. שלב א' (התזכורת הבסיסית) נשאר Gmail SMTP ללא שינוי, בהחלטה מפורשת.
- **Frontend:** עמוד ציבורי חדש `/upload/:token` (`MeetingUploadPage.jsx`, ללא התחברות, dropzone יחיד + checklist) ומודל `MeetingUploadComparisonModal.jsx` (נפתח דרך deeplink מההתראה, `?meeting=`, ב-`SchoolPage.jsx`).
- **בדיקות שבוצעו (מקצה-לקצה, מול הפרודקשן האמיתי של Supabase Storage/DB עם קבצי אקסל אמיתיים):** checklist אומת מול בית ספר עם היסטוריית בדיקות אמיתית (זיהה נכון "גפן"+"תקומה"+"payscool"); הועלו 3 קבצים אמיתיים (גפן/תכנון/כספים) — כולם סווגו נכון (`gefen`→חטיבה, `tikhnun`→רשימת תקציבים, `payscool`); ה-diff חושב נכון מול הצפוי; אומת שכל 3 הקבצים ניתנים להורדה חזרה מ-Storage (הבסיס ל-`run-check-from-uploads`); אומת שהנתיב החדש (מזכירה) ב-`send_due_reminders` נכשל *בחינניות* (ללא קריסה) כל עוד `RESEND_API_KEY` לא הוגדר — מסומן כ-`status=failed` עם הודעת שגיאה ברורה, בלי להשפיע על נתיב הגמייל הרגיל של שאר המשתתפים. `npx vite build` עובר ב-0 שגיאות. כל נתוני הבדיקה (DB + Storage) נוקו בסיום.
- **דורש פעולה ידנית לפני שהמיילים החדשים באמת ייצאו:** הרשמה ל-Resend, אימות דומיין `gefenai.co.il` (רשומות DNS), והוספת `RESEND_API_KEY` ל-`.env` המקומי (כבר קיים ריק) ול-Render.
- **⏸️ ממתין בכוונה, לא נכלל בהעלאה של 2026-07-08:** לפי בקשת המשתמש — ממתינים לוודא שהחיבור ל-LIVEDNS ול-Resend עובד תקין לפני שהפיצ'ר הזה עולה. **הערה טכנית חשובה:** כדי לאפשר להעלות את שאר השינויים (שלא קשורים לפיצ'ר הזה) בלי לגרור אותו איתם בטעות, חיווט הרישום שלו הוסר זמנית מ-`backend/main.py` (השורות `from routers.meeting_upload_router import ...` ו-`app.include_router(meeting_upload_router)`) ומ-`frontend/src/App.jsx` (הימפורט של `MeetingUploadPage` והנתיב `/upload/:token`) — **בכוונה, לא בטעות**. שאר הקבצים של הפיצ'ר (`meeting_upload_logic.py`, `routers/meeting_upload_router.py`, `email_resend.py`, `MeetingUploadPage.jsx`, `MeetingUploadComparisonModal.jsx`, השינויים ב-`schools_router.py`/`analyze_router.py`/`SchoolPage.jsx`, והתלות `httpx` ב-`requirements.txt`) נשארו בלי שינוי בעבודה המקומית. **לפני ההעלאה הבאה של הפיצ'ר הזה — יש להחזיר את שתי שורות החיווט האלה** ב-`main.py` וב-`App.jsx`.

## 2026-07-08 — Fix: הסרת סעיף "יעדים" מ"סינון מתקדם" בדף הבית
- הוסרה לגמרי הסקציה המתקפלת "יעדים" שהופיעה מתחת ל"ראשי" בפאנל "סינון מתקדם" ב-`DashboardPage.jsx` — מיותרת כי הנתונים כבר מוצגים דרך עמודות היעדים בטבלה. `npx vite build` עובר ב-0 שגיאות.
