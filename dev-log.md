# Dev Log

## 2026-09-02 — תיקון: ייבוא בתי ספר — רשימת המשתמשים לא נטענה לפני הזיהוי
- באג: `users` (רשימת משתמשי הארגון) נטענת רק בכניסה לטאב "משתמשים"/"חיובים". מי שנכנס ישר ל"בתי ספר" → "ייבוא מאקסל" קיבל `users = []`, ולכן **כל** תא של יועץ מלווה (שם/מייל/טלפון) לא זוהה והשורה נפלה ל"בעיות".
- תיקון: `confirmImport` כעת async — אם `users` ריק, מושך `/schools/users/all` לפני בניית מדד הזיהוי (`buildUserMatchIndex`).
- שיפור נלווה: בחלונית, אם תא "מתאם פגישות" הכיל שם רגיל (לא מייל/טלפון) שלא זוהה — השם ממולא מראש, המשתמש רק בוחר תפקיד.
