# Dev Log

## 2026-07-06 — Feat: פורטל העלאת קבצים למזכירה/אחראית כספים לפני פגישה (שלב ב')
- **הקשר:** המשך ישיר לשלב א' (תזכורות מייל). עכשיו, כשיש בפגישה איש קשר מסוג "מנהלנית"/"אחראית כספים" (`participants` עם `key` בערך `secretary`/`finance`), אותו `reminder_enabled` הקיים גם שולח לה מייל משולב (תזכורת + checklist מדויק + קישור להעלאת קבצים) — בלי שום כפתור נוסף שהיועץ צריך ללחוץ.
- **DB (Supabase):** 3 טבלאות חדשות — `meeting_upload_tokens` (טוקן לשימוש חוזר, תוקף = מועד הפגישה + 3 ימי חסד), `meeting_upload_files` (קובץ שהועלה + סיווג: `identified_type`/`division_type`/`budgets`), `meeting_upload_followups` (יומן שליחת מייל מעקב חוסרים). כולן GRANT ל-service_role בלבד + RLS + policy יחיד, כמו `meeting_reminders`.
- **Backend (`meeting_upload_logic.py`, חדש):** `build_upload_checklist()` — בונה checklist לפי `check_metrics` **מסונן לפי שנת הלימודים הנוכחית בלבד** (לא שנים קודמות, כי סוגי תקציבים יכולים להשתנות); בית ספר ללא נתוני בדיקה לשנה הנוכחית מקבל checklist גנרי + דגל `no_baseline_this_year`. `compute_upload_comparison()` — דיף בין הצפוי למה שהתקבל בפועל (לא מריץ שום התאמה אמיתית).
- **Backend (`routers/meeting_upload_router.py`, חדש):** שני endpoints ציבוריים ללא אימות (`GET/POST /public/meeting-upload/{token}`) — המזכירה גוררת קבצים, כל קובץ מסווג באמצעות `identify_file()` הקיים + `_detect_gefen_division()`/`extract_plan_roster()` הקיימות (שימוש חוזר מלא, ללא נגיעה ב-`backend/logic/`), נשמר ב-Storage תחת `meeting-uploads/{meeting_id}/...`, ונוצרת התראה ליועצים דרך `_create_notifications` הקיים. שני endpoints מאומתים: `GET .../upload-comparison` (חישוב הדיף) ו-`POST .../request-missing-files` (הכפתור היחיד בפיצ'ר — שולח מייל ממוקד רק על מה שחסר בפועל).
- **Backend (`analyze_router.py`):** נוסף `POST /analyze/meetings/{meeting_id}/run-check-from-uploads` — משכפל בדיוק את דפוס ה-Storage-download הקיים של `/add-file/{log_id}` כדי להריץ בדיקה אמיתית מהקבצים שכבר הועלו, בלי להעלות שוב.
- **Backend (`email_resend.py`, חדש):** אינטגרציית Resend דרך `httpx` (כבר תלות קיימת בפרויקט) — עבור שני סוגי המייל החדשים בלבד. שלב א' (התזכורת הבסיסית) נשאר Gmail SMTP ללא שינוי, בהחלטה מפורשת.
- **Frontend:** עמוד ציבורי חדש `/upload/:token` (`MeetingUploadPage.jsx`, ללא התחברות, dropzone יחיד + checklist) ומודל `MeetingUploadComparisonModal.jsx` (נפתח דרך deeplink מההתראה, `?meeting=`, ב-`SchoolPage.jsx`).
- **בדיקות שבוצעו (מקצה-לקצה, מול הפרודקשן האמיתי של Supabase Storage/DB עם קבצי אקסל אמיתיים):** checklist אומת מול בית ספר עם היסטוריית בדיקות אמיתית (זיהה נכון "גפן"+"תקומה"+"payscool"); הועלו 3 קבצים אמיתיים (גפן/תכנון/כספים) — כולם סווגו נכון (`gefen`→חטיבה, `tikhnun`→רשימת תקציבים, `payscool`); ה-diff חושב נכון מול הצפוי; אומת שכל 3 הקבצים ניתנים להורדה חזרה מ-Storage (הבסיס ל-`run-check-from-uploads`); אומת שהנתיב החדש (מזכירה) ב-`send_due_reminders` נכשל *בחינניות* (ללא קריסה) כל עוד `RESEND_API_KEY` לא הוגדר — מסומן כ-`status=failed` עם הודעת שגיאה ברורה, בלי להשפיע על נתיב הגמייל הרגיל של שאר המשתתפים. `npx vite build` עובר ב-0 שגיאות. כל נתוני הבדיקה (DB + Storage) נוקו בסיום.
- **דורש פעולה ידנית לפני שהמיילים החדשים באמת ייצאו:** הרשמה ל-Resend, אימות דומיין `gefenai.co.il` (רשומות DNS), והוספת `RESEND_API_KEY` ל-`.env` המקומי (כבר קיים ריק) ול-Render.
- **⏸️ ממתין בכוונה, לא נכלל בהעלאה של 2026-07-08:** לפי בקשת המשתמש — ממתינים לוודא שהחיבור ל-LIVEDNS ול-Resend עובד תקין לפני שהפיצ'ר הזה עולה. **הערה טכנית חשובה:** כדי לאפשר להעלות את שאר השינויים (שלא קשורים לפיצ'ר הזה) בלי לגרור אותו איתם בטעות, חיווט הרישום שלו הוסר זמנית מ-`backend/main.py` (השורות `from routers.meeting_upload_router import ...` ו-`app.include_router(meeting_upload_router)`) ומ-`frontend/src/App.jsx` (הימפורט של `MeetingUploadPage` והנתיב `/upload/:token`) — **בכוונה, לא בטעות**. שאר הקבצים של הפיצ'ר (`meeting_upload_logic.py`, `routers/meeting_upload_router.py`, `email_resend.py`, `MeetingUploadPage.jsx`, `MeetingUploadComparisonModal.jsx`, השינויים ב-`schools_router.py`/`analyze_router.py`/`SchoolPage.jsx`, והתלות `httpx` ב-`requirements.txt`) נשארו בלי שינוי בעבודה המקומית. **לפני ההעלאה הבאה של הפיצ'ר הזה — יש להחזיר את שתי שורות החיווט האלה** ב-`main.py` וב-`App.jsx`.
