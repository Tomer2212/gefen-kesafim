# Dev Log

## 2026-09-25 — אחידות תצוגתית ל"שלב חינוך" (יסודי/חטיבת ביניים) בין כל אזורי הבדיקה
- כשקודי הדיווח שהועלו לא מספיקים כדי להבחין בוודאות בין יסודי לחטיבת ביניים, כל אזורי התצוגה (קבצי דיווח ביצוע, קבצים מתוכנת הכספים, קבצי תכנון, טבלת "תקציב קבוע") מציגים כעת ניסוח אחיד: "יסודי/חטיבת ביניים".
- אם לחשבון הגפן הספציפי שנבדק (ואם לא — לכרטיס בית הספר הכללי) יש קביעה חד־משמעית של יסודי או חטיבת ביניים, היא מוצגת בכל האזורים הללו במקום הניסוח המעורפל.
- תיקון תצוגתי בלבד — לא בוצע שינוי בקבצי `backend/logic/` ולא בלוגיקת ההתאמה/החישוב עצמה. נוסף שדה חדש `stage_override` ל-`analyze_router.py` (נקרא רק לתצוגה), ולוגיקת ההצגה עודכנה ב-`ResultsView.jsx`.
- תוקן באג המשך: `stage_override` חושב נכון בבדיקה חיה אך "נפל" ולא נשמר בהיסטוריה (`check_logs`), כך שצפייה בבדיקה שמורה עדיין הציגה את הניסוח המעורפל. נוסף לשמירה ב-`_save_check_log` (`analyze_router.py`) ולשחזור מהיסטוריה ב-`logToResult` (`SchoolPage.jsx`). בדיקות שנשמרו *לפני* התיקון הזה עדיין יציגו את הניסוח הישן — יש להריץ אותן מחדש כדי לראות את התיקון.

## 2026-09-25 — תיקון אייקוני "דיווח חסר" בבדיקת תכנון בלבד
- כשלא הועלה קובץ דיווח ביצוע (גפן), עמודת "דיווח" בטבלת ההיסטוריה מציגה כעת X אדום ("לא בוצעה בדיקה") במקום "+" מטעה, בדיוק כמו עמודת "כספים".
- תוקן באג שגרם לעמודות "דיווח חסר" (כמות/סכום) להמשיך להציג "0" מטעה כשאין בחירת תקציב ספציפי (מסלול רינדור נפרד, `renderCheckLogCell`, שלא בדק אם קובץ הדיווח הועלה).
- כעת גם העמודות "אחוז דיווח למודל תמרוץ", "אסמכתאות שנדחו" ו"ללא PDF" (כמות/סכום) מציגות X אדום לחיץ במקום "–" כשלא הועלה קובץ דיווח ביצוע — אחידות מלאה מול שאר העמודות.
- אוחד ניסוח ההערות בכל המקרים: בעמודות "קבצים שהועלו" (דיווח/כספים) — "לא הועלה קובץ [דיווח ביצוע/כספים] [סוג תקציב אם נבחר] [שלב לימוד אם שש-שנתי] — חלק מהבדיקות לא בוצעו."; בשאר העמודות — "הבדיקה לא בוצעה משום שלא הועלה קובץ [דיווח ביצוע/כספים] [סוג תקציב] [שלב לימוד]." שם השלב (חט"ע/חט"ב) מופיע רק בבתי ספר שש-שנתיים, בהתאם ללשונית הפעילה.
- טקסט ההנחיה בכל ה-X-ים האדומים הלחיצים שונה ל"לחץ על ה-✕ להוספת קובץ ועדכון הבדיקה."

## 2026-09-25 — איחוד עמודות-אב בבורר "עמודות להצגה" (בדיקות)
- בכפתור "עמודות להצגה" בלשונית "בדיקות", עמודות עם תתי-עמודות (למשל "נותר לתכנון" עם קבוע/גמיש) מוצגות כעת כאפשרות סימון אחת בלבד, במקום שורה נפרדת לכל תת-עמודה.
- סימון/ביטול סימון האפשרות המאוחדת מציג/מסתיר את כל תתי-העמודות שלה יחד — לא ניתן יותר להציג תת-עמודה אחת בלי האחרות מאותה עמודת אב.

## 2026-09-25 — עדכון כותרות עמודות ההשוואה בלשונית "בדיקות"
- "קיים בכספים, לא בגפן" → "דווחו בכספים, לא שויכו בגפן".
- "בגפן, לא בכספים" → "שויכו בגפן, לא קיימות בכספים".
- הטבלה משתמשת ב-table-layout רגיל (לא fixed) עם whitespace-nowrap על הכותרות, כך שרוחב עמודות "כמות"/"סכום" מתרחב אוטומטית להתאמת הכותרת הארוכה יותר, בלי לגרום לשבירת שורה או הגדלת גובה הכותרת.
