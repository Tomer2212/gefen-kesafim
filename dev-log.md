# Dev Log

## 2026-07-30 — העברת מנגנון טיקטים והסכמות ל-Supabase
- הוספו טבלאות `contact_tickets` (עם `ticket_number` רץ אוטומטי, `GENERATED ALWAYS AS IDENTITY`) ו-`user_consents` (FK ל-contact_tickets) ב-Supabase, כולל GRANT ל-service_role, RLS מופעל ומדיניות policy.
- `backend/routers/contact_router.py`: הוסרה התלות המלאה ב-`tickets.json`/`consents.json` וב-`threading.Lock()`; מספור הפניות והסכמות עוברים כעת דרך Supabase, בטוח לעבודה מול מספר workers.
- נבדק: יצירת מספר פניות רצופות מוודאת מספור רץ תקין ללא כפילויות מול ה-DB האמיתי.

## 2026-08-03 — תיקון באג "דלג" בחלונית סיווג שורות לא מזוהות
- `backend/zihuy_core.py`: שורות דיווח שלא ניתן היה לחלץ מהן מספר ספק קיבלו כולן `union_key` זהה (מחרוזת ריקה), מה שגרם ללחיצה על "דלג" בשורה אחת בחלונית הסיווג הידני (`ClassifyModal.jsx`) לדלג בפועל על כל השורות חסרות-מספר-הספק — גם בתצוגה וגם בפועל בשרת (`/analyze/classify`). כעת כל שורה כזו מקבלת מפתח ייחודי (`_unresolved_{אינדקס}`) במקום מחרוזת ריקה משותפת.
- אין השפעה על שורות שזוהו לפי מספר ספק תקין, ואין שינוי בלוגיקת ההתאמה מול קובץ הכספים (`ichud`, שמחושבת בנפרד ב-`reconciler.py`).

## 2026-08-03 — תווית משך זמן במייל תיאום ישיר
- `backend/booking_logic.py`: נוספה תווית "משך" (למשל "45 דק'" / "1:30 שעות") לכל טווח פגישה במייל התיאום הישיר שנשלח לבית הספר.
