# Dev Log

## 2026-09-16 — שורת סיכום קפואה בטבלת ניהול-גבייה
- הוספת שורת סיכום קפואה בתחתית הקלף הלבן (לא רק בתחתית הטבלה עצמה) בטאב "גבייה" ב-AdminPage, שמציגה סה"כ עבור 'מחיר כולל מע"מ', 'סכום ששולם' ו'נותר לתשלום' על השורות המסוננות המוצגות
- עמודות שורת הסיכום מסונכרנות (רוחב + גלילה אופקית) עם עמודות הטבלה הראשית כך שכל סכום מופיע בדיוק מתחת לעמודה שהוא מסכם

## 2026-09-16 — בקרת ניתוב מפורשת ("יעד ליועץ מלווה") במשימות "יועץ מלווה"
- מיגרציה: `org_person_tasks.advisor_divisions` (JSONB) — NULL/ריק = ההתנהגות הישנה (ניתוב לפי service_type בפועל של כל בית ספר).
- backend/person_tasks_router.py: `_resolve_school_advisor_candidates`/`_build_check_rows`/`create_person_task`/`_try_activate_scheduled_person_task` מקבלים ומכבדים `advisor_divisions` — כשמוגדר, כל בית ספר תואם מנותב בדיוק לתחומים שנבחרו (גפן/שוטף/מחוז), בלי קשר ל-service_type בפועל שלו. פותר מקרה שבו בית ספר "גפن+שוטף" ניתב תמיד לשני היועצים גם כשרצו רק אחד.
- frontend/PersonTaskCreateWizard.jsx: נוספה בקרת צ'יפים "המשימה תישלח ליועץ המלווה בתחום:" — ממולאת אוטומטית כשנבחר סוג שירות יחיד וברור (לא "גפن+שوטף"), ונדרשת בחירה מפורשת כשיש כמה סוגי שירות נבחרים או "גפن+שوטף". הבקרה ממוקמת מעל תיבת סינון בתי הספר, עם רקע אפור זהה לרקע כרטיסיית תנאי בודד; הוסרה הודעת "מולא אוטומטית..." המיותרת; המשפט "יש לסנן..." הוחלף ברווח מפריד; וכותרת "קבוצת תנאים (וגם ביניהם)" הוחלפה ל"אילו בתי ספר ייכללו במשימה? (נא לסנן)" רק במסך הזה (נוסף prop `groupTitle` חדש ל-ConditionGroupsEditor.jsx לצורך זה, בברירת מחדל שלא משפיעה על שאר הקריאות).

