# Dev Log

## 2026-07-21 — תיקוני בדיקה נוספים ל"סוכן ניהול" שלב 2 (מקבילות כלים, תקציב תשובה, זיהוי אישור)
- Backend: `agent_router.py` — כובה שימוש מקבילי בכמה כלים בו-זמנית (`disable_parallel_tool_use`). קלוד לפעמים ניסה להפעיל שני כלים באותו תור, מה שגרם לקריאת ה-API הבאה להיכשל לחלוטין ("tool_use ids... without tool_result") — השגיאה נבלעה בשקט והוצגה כטקסט כללי מטעה במקום
- Backend: `max_tokens` הועלה מ-1024 ל-2048 בשתי קריאות ה-API — 1024 היה נמוך מדי עבור Sonnet 5 (תקציב "חשיבה" פנימית נוגס בתקציב הכולל), מה שגרם לתשובות להיקטע באמצע משפט
- Backend: הרחבת ה-system prompt — זיהוי אישור/הסכמה של המשתמש (למשל "כן"/"מתאים"/"סבבה"/"מעולה") מבוסס כעת על **משמעות**, לא על רשימת מילים ספציפית; וזיהוי "מה בדיוק המשתמש מאשר" (חלון זמנים מול שליחה בפועל) מבוסס כעת על **מצב הטיוטה בפועל** (`default_scheduling_window`/`unresolved_school_names` בתוצאת הכלי האחרון), לא על ניסיון לזכור מהשיחה מה נשאל בעבר — זה מנע מקרה שבו הסוכן שאל את אותה שאלה פעמיים או פספס אישור שליחה מפורש
- נבדקה שרשרת מלאה של 3 הודעות (בקשה → "מתאים" → "סבבה, תשלח") עד ליצירת שורת תור אמיתית ב-`meeting_booking_email_queue` (נמחקה מייד לאחר האימות — לא נשלח מייל אמיתי בבדיקה)

## 2026-07-21 — Fix: מניעת אירועי כפילות ב-Outlook + עמידות תחת עומס (סנכרון יומן)
- `backend/graph_client.py`: `_update_event_for_advisor` — 404 בעדכון אירוע כבר לא יוצר אירוע כפול אוטומטית; מוודא תחילה (קריאת GET נפרדת) שהאירוע באמת נמחק לפני שיוצר מחדש, כדי לא ליצור כפילות על תקלת רשת חולפת
- `backend/graph_client.py`: `persist_calendar_sync` (הכתיבה הקריטית ביותר בכל זרימת הסנכרון) קיבלה לוגיקת "נסה שוב עם חיבור טרי" — זו הייתה הפונקציה היחידה בשרשרת בלי הגנה, ותקלה בה גרמה בדיוק לתרחיש שבו Outlook "חושב" שהאירוע קיים אבל ה-DB שלנו לא מכיר אותו
- `backend/graph_client.py` + `backend/routers/calendar_router.py`: `get_freebusy`/`/calendar/freebusy` — כשל אמיתי בבדיקת זמינות כבר לא מוצג כ"פנוי" (סכנת בטיחות אמיתית לכלי קביעת פגישות) — מוחזר `ok:false`/`None` מפורש, והפרונט מציג "לא ניתן היה לעדכן" במקום
- `_get_app_only_token`/`_resolve_advisor_email`: אותה לוגיקת retry, לתקלות זמניות תחת עומס בו-זמני (הרבה בדיקות זמינות שקורות יחד)
- **הערה חשובה:** כל התיקונים האלה נבדקו ואומתו רק על שרת מקומי עד עכשיו — זהו הדיפלוי הראשון שלהם לפרודקשן. לא כלל שום דבר מהעבודה המקבילה על סוכן המיילים/מערכת השריון (`send_mail_as_advisor` וכו') — זו נשארה בקוד המקומי בלבד, לא הועלתה

## 2026-07-21 — תיקוני בדיקה ל"סוכן ניהול" שלב 2 (תאריכים, סינון בית ספר בודד, ניסוח תשובה)
- Backend: `agent_router.py` — הוזרק תאריך "היום" בפועל להקשר של הסוכן (לא היה קודם — הסוכן ניחש תאריכים, מה שגרם לו לחשב חודשים שגויים כמו אוגוסט 2025 במקום 2026)
- Backend: `find_schools_missing_meetings` מקבל כעת פרמטר אופציונלי `school_names` — כשהמשתמש מבקש על בית ספר ספציפי בשם, הסוכן מסנן אליו בלבד במקום לסרוק את כל בתי הספר בארגון
- Backend: תוקנה הודעת נפילה מטעה "בוצע." שהופיעה גם כשלא נשלח בפועל שום מייל (רק מידע נאסף) — כעת ההודעה משקפת את המצב האמיתי ("לא נשלח שום מייל בשלב זה" / מספר בקשות שאכן נשלחו)
- נבדק מחדש קצה-לקצה עם התרחיש המדויק שנכשל: "תשלח לבית ספר X בקשה לפגישה באוגוסט" — כעת מזהה נכון רק את בית הספר המבוקש, בחודש הנכון (2026-08), עם תשובה ברורה שלא מטעה

## 2026-07-21 — עמודות "יומן" ו"סיכום פגישה" בטבלת הפגישות (שלב 1: העלאה ידנית)
- Frontend: שתי עמודות חדשות בטבלת הפגישות המשותפת (`MeetingsTable.jsx`/`MeetingRow.jsx`, פועל בכל שלושת מקומות הפגישות — כרטיס בית ספר, אזור אישי, ניהול):
  - "יומן" — הועבר לכאן תג הסנכרון (`CalendarSyncBadge`) שהיה קודם דחוס בתוך תא "תזכורת"; מוצג רק אם לארגון יש אינטגרציית יומן מחוברת (`GET /calendar/connection`, בדיקה חד-פעמית לכל עמוד, לא per-row)
  - "סיכום פגישה" — כפתור "+" שפותח בחירה בין "לכתוב הערה" (מפנה למנגנון ההערות הקיים) לבין "להעלות הקלטה"; בזמן עיבוד ברקע מוצג "מעבד..." עד שהתוצאה מגיעה אוטומטית דרך ה-polling הקיים (`useMeetingsPolling`)
- Frontend: קומפוננטה חדשה `MeetingSummaryModal.jsx` — מודל בחירה קטן, לא מסך תצוגה עצמאי; לא שומר סיכום בעצמו
- Backend: ראוטר חדש `meeting_summary_router.py` — `POST /schools/meetings/{id}/summary/recording` (העלאת הקלטה, מחזיר תשובה מיידית ומריץ עיבוד ב-`BackgroundTasks`), `GET .../summary` (סטטוס, polling-safe), `POST .../summary/retry`
- Pipeline: תמלול באמצעות OpenAI Whisper API (`OPENAI_API_KEY` חדש), סיכום באמצעות Claude Sonnet 5 (אותו מפתח/SDK כמו ב-`agent_router.py`) — הפרומפט נשמר כקבוע יחיד ב-`meeting_summary_router.py` וטרם הושלמו הקריטריונים המדויקים לחיפוש בתמלול (TODO בקוד)
- **חשוב:** תוצאת הסיכום (ידני או אוטומטי) נכתבת ישירות לתוך שדה `notes` הקיים של הפגישה — לא לשדה נפרד. הצגתה היא דרך עמודת "הערות" הרגילה
- Supabase: 4 עמודות חדשות ב-`meetings` — `summary_status`, `summary_error`, `summary_audio_storage_key`, `summary_updated_at` (מעקב עיבוד בלבד, לא תוכן)
- היקף שלב זה: העלאת הקלטה ידנית בלבד; חיבור API חיצוני ממערכת אחרת נדחה לשלב עתידי (ידרוש תשתית API keys ברמת ארגון)

## 2026-07-21 — "סוכן ניהול" שלב 2: זיהוי פגישות חסרות + שליחת בקשת שריון דרך Outlook
- Backend: 4 tools חדשים ב-`agent_router.py` (`find_schools_missing_meetings`, `update_draft_school`, `set_booking_defaults`, `confirm_send_booking_emails`) — זרימת סוכן רב-תורית: מזהה בתי ספר חסרי פגישות בחודשים שהמשתמש מגדיר, פותר יועץ מלווה לכל בית ספר (אוטומטית אם יש אחד יחיד; שואל בעברית אם יש כמה, או אם אין בכלל), שואל על חלון זמנים (ימים/שעות/משך פגישה) לפני שממשיך — עם אפשרות לחריגה נקודתית לבית ספר בודד — ולא שולח שום מייל בלי אישור מפורש בהודעה הנוכחית
- Backend: תשתית חדשה — `booking_draft_state.py` (טיוטת אצווה רב-תורית ב-Supabase, לא בזיכרון שרת), `booking_logic.py` (זיהוי בתי ספר חסרי פגישות, בניית HTML למייל, בדיקת חיבור Outlook), `booking_token_logic.py` (לינק שריון עם טוקן, בהשראת `meeting_upload_logic.py` הקיים)
- Backend: `graph_client.py` — פונקציה חדשה `send_mail_as_advisor` ששולחת מייל אמיתי דרך Microsoft Graph (`Mail.Send`) מתיבת ה-Outlook האמיתית של היועץ (משתמשת באותו מנגנון `_resolve_advisor_email`/`calendar_sync_email` שכבר קיים לסנכרון יומן) — שום שינוי לקוד הקיים
- Backend: `meeting_booking_router.py` חדש — עמוד שריון ציבורי ללא login (`GET/POST /public/meeting-booking/{token}`), כולל בדיקת זמינות אמיתית מול Outlook ויצירת פגישה אמיתית גם ב-Supabase וגם ביומן ה-Outlook בפועל בעת שריון (מבוסס על `graph_client.sync_meeting_create` הקיים)
- Backend: `schools_router.py` — cron endpoint חדש `process-booking-email-queue` (מוגן `X-Cron-Secret`, אותו דפוס כמו `send-due-reminders` הקיים) לשליחה מדורגת של בקשות שריון בקבוצות קטנות
- Supabase: 3 טבלאות חדשות — `meeting_booking_drafts`, `meeting_booking_tokens`, `meeting_booking_email_queue` (GRANT/RLS/policy מלאים ל-service_role)
- Frontend: `AgentChatWidget.jsx` — שולח היסטוריית שיחה ו-`draft_id` בכל קריאה, מציג "סיכום אצווה" חזותי (בתי ספר → יועץ → חודשים) מתחת לתשובת הסוכן
- Frontend: `MeetingBookingPage.jsx` חדש בנתיב `/book/:token` — עמוד ציבורי (ללא Sidebar) שבו מזכירת בית הספר בוחרת מועד פנוי מתוך משבצות אמיתיות ביומן היועץ
- Frontend: `AdminIntegrationsTab.jsx` — תוקן פער שמנע re-consent (הכפתור היה ננעל לצמיתות אחרי חיבור ראשוני); כעת אפשר ללחוץ שוב לעדכון הרשאות
- Frontend: `vite.config.js` — הוספת `/public` ו-`/agent` לרשימת ה-proxy המקומית (חסרו, גרמו ל-404 בפיתוח מקומי)
- **דורש פעולה ידנית ב-Azure** לפני שהתכונה תעבוד בפועל: הוספת הרשאות Application permissions `Mail.Send` + `Mail.Read` ב-App Registration והענקת admin consent (ראה `dynamic-bouncing-cocke.md` סעיף 0 לפרטים מלאים); לאחר מכן צריך ללחוץ מחדש על "חבר יומן ארגוני" בכל ארגון שכבר חיבר בעבר
- נבדק קצה-לקצה מול נתונים אמיתיים (זיהוי 46 בתי ספר חסרי פגישות, פתרון יועץ אוטומטי/עמום, קביעת חלון זמנים, וחסימת שליחה בפועל כשיש בתי ספר לא-פתורים — ללא שום שורת תור/טוקן שנוצרה בטעות)

## 2026-07-20 — "סוכן ניהול": וידג'ט צ'אט AI לסינון טבלאות בתי ספר/פגישות בניהול
- Frontend: הוספת סינון לפי עמודה לטבלת "בתי ספר" בניהול (`AdminPage.jsx`) — כפתור סינון בכל כותרת עמודה עם popover (טקסט/מספר/בחירה מרשימה), נשמר ב-localStorage לפי משתמש, פועל יחד עם החיפוש והמיון הקיימים
- Frontend: `AdminMeetingsTab.jsx` — הרחבת ה-`useImperativeHandle` הקיים עם `applyAgentFilters` (קריאת GET בלבד — לא נוגע ביצירה/עדכון/מחיקה/סנכרון-Outlook של פגישות)
- Frontend: `frontend/src/components/AgentChatWidget.jsx` — וידג'ט צ'אט צף חדש בשם "סוכן ניהול", עיצוב והתנהגות זהים ל-`ChatWidget.jsx` (מדריך גפן), אך בפינה הנגדית ולא-streaming; מוצג רק ל-owner/manager בכל טאבי הניהול
- Backend: `backend/routers/agent_router.py` חדש — אינטגרציית Claude API (Messages API + tool use) הראשונה בפרויקט. Endpoint יחיד `POST /agent/ask`, שני tools (`filter_schools_table`, `filter_meetings_table`), מכסה של 20 הודעות למשתמש ביום
- Supabase: עמודת `agent_usage_count` חדשה על `chatbot_usage_daily` + פונקציית `increment_agent_usage` (מיררור מדויק של `increment_chatbot_usage` הקיימת)
- שדרוג `anthropic` ב-`requirements.txt` מ-0.28.0 ל-0.69.0 (נדרש ל-tool-use המודרני)
- היקף: MVP סינון/שאילתות בלבד — לא שולח מיילים, לא זוכר שיחות, לא נוגע בנתוני פגישות מעבר לסינון תצוגה

## 2026-07-20 — שמירה נדחית ליועץ מלווה + תיקוני UI ב"גישה"
- Frontend: הקצאת/הסרת יועץ מלווה בטופס עריכת בית ספר (`SchoolPage.jsx` ו-`AdminPage.jsx`) הפכה ל**נדחית** — כל שינוי מעדכן רק state מקומי (`draftAdvisorIds`), ורק בלחיצה על "שמור שינויים" מחושב ההפרש מול המצב המקורי ונשלחות רק קריאות ה-API עבור שינוי אמיתי (וממילא רק אלו יוצרות התראה). ביטול/סגירת הטופס בלי שמירה משחזר את הבחירה המקורית, ללא כל קריאת שרת
- סדר השמירה: קודם הוספות ואז הסרות, כדי שהבדיקה "לא ניתן להסיר יועץ יחיד" בבאקאנד לא תיתקל במצב-ביניים שגוי
- ולידציה חדשה: ניסיון לשמור עריכת בית ספר קיים עם 0 יועצים נבחרים חסום בצד לקוח (כמו בטופס בית ספר חדש)
- הפאנל המורחב ברשימת בתי הספר (AdminPage, "יועצים מוקצים") נשאר מיידי (אין שם כפתור שמור) אך עבר לאותה קומפוננטה
- קומפוננטה חדשה ומאוחדת: `frontend/src/components/AdvisorSearch.jsx` הפכה לרשימת checkbox מרובה עם כפתור "אישור" לסגירה (controlled: `selectedIds`/`onChange`), במקום דפדוף type-then-click בודד. הגרסה המקומית הכפולה ב-`AdminPage.jsx` נמחקה — שני העמודים מייבאים כעת מהקובץ המשותף
- תיקון `AccessSelector` ב-`SchoolPage.jsx`: הוסר `onFocus` שגרם להבהוב פתיחה/סגירה בקליק ראשון (מירוץ מול `onClick`), והוסר סינון "בעלים" ייחודי שגרם לשדה "גישה" להיראות ריק כשהיועץ היחיד הוא בעל תפקיד "בעלים" — עכשיו תואם להתנהגות התקינה שכבר הייתה קיימת ב-`AdminPage.jsx`

## 2026-07-20 — תיקון המשך: הבהוב עבר ל"יועץ מלווה" + "גישה" חוזר ריק אחרי שמירה
- `frontend/src/components/AdvisorSearch.jsx`: אותו באג ההבהוב (מירוץ `onClick`/`onFocus`) שתוקן ב-AccessSelector דלף גם לקומפוננטה החדשה של "יועץ מלווה" — קליק על תיבת החיפוש פתח וסגר מיד כי `onClick` הזיז פוקוס ידנית ל-input, מה שהפעיל `onFocus` בנפרד. הוסר הקריאה הידנית ל-`focus()` וה-`onFocus`; הפתיחה/סגירה מבוססת אך ורק על `onClick`
- `frontend/src/pages/SchoolPage.jsx` (תצוגת "גישה" במצב לא-עריכה, לא במצב עריכה): 1) הוסר סינון "בעלים" נוסף שפספסנו בתיקון הקודם (היה קיים גם כאן, לא רק ב-AccessSelector של מצב העריכה). 2) `school.restrict_access_profiles` (השדה שממנו נגזרת התצוגה כשלא נטענו `users`) לא התעדכן אחרי שמירה מקומית ולכן המשיך להציג תמונת מצב ישנה מלפני העריכה — עכשיו מתעדכן ב-`saveEdit()` יחד עם שאר השדות

## 2026-07-20 — אחידות עיצובית בין "יועץ מלווה" ל"גישה"
- `AccessSelector` הועבר מהגדרה מקומית כפולה (ב-`AdminPage.jsx` וב-`SchoolPage.jsx`) לקובץ משותף `frontend/src/components/AccessSelector.jsx` — אותו תבנית עקבית כמו `AdvisorSearch.jsx` (מונע סחיפה עתידית בין שני מימושים)
- שני השדות ("יועץ מלווה" ו"גישה") נראים ומתנהגים כעת זהה: תיבה סגורה עם צ'יפים בלבד (בלי שדה הקלדה בתיבה הסגורה עצמה), ותפריט נפתח עם שדה חיפוש ייעודי בראש + כפתור "אישור" בתחתית לסגירה
- לפני כן: ל"גישה" היה שדה חיפוש בתפריט אך בלי כפתור אישור; ל"יועץ מלווה" היה כפתור אישור אך שדה ההקלדה ישב בתיבה הסגורה עצמה (לא בתפריט) — שתי הבחירות התאחדו לתבנית אחת
- הערה: קיימות עוד שתי הגדרות מקומיות נפרדות של `AccessSelector` ב-`DashboardPage.jsx` וב-`AddSchoolPage.jsx` — לא נגענו בהן כי הן מחוץ לתחום הבקשה הנוכחית

## 2026-07-20 — מניעת הסרת היועץ המלווה האחרון מבית ספר
- Backend: `DELETE /schools/{id}/advisors/{advisor_id}` חוסם הסרה כשמדובר ביועץ היחיד המשויך לבית הספר, ומחזיר שגיאה 400 בעברית
- Frontend: `removeAdvisorFromSchool` ב-`AdminPage.jsx` וב-`SchoolPage.jsx` מציגה כעת התראה למשתמש אם ההסרה נחסמה (מכסה את שלוש הזירות: טופס עריכה, פאנל מורחב, כרטיס בית ספר)
- פתיחת בית ספר חדש כבר הייתה חסומה קודם ללא יועץ נבחר — ללא שינוי
- Frontend (`SchoolPage.jsx`): שדה "גובה הזמנה בגפן" הועבר מקארד נפרד לתוך אזור "ליווי", מתחת ל"יועץ מלווה" (גם במצב עריכה וגם במצב תצוגה) — זה גם פתר תקלת UI שבה תפריט ה"גישה" הוסתר חלקית מאחורי הקארד הנפרד (שני glass-card עוקבים יוצרים הקשרי staking נפרדים בגלל backdrop-filter, והקארד השני "בלע" חלק מהתפריט הנפתח של הקארד הראשון)

## 2026-07-19 — טבלת ניהול בתי ספר חדשה באזור "ניהול" (שלב 1)
- טבלת Supabase חדשה `school_year_admin_data` (מפתח school_id+academic_year) עם GRANT/RLS/policy מלאים
- עמודות `admin_col_order`/`admin_col_visible` חדשות ב-`profiles` (העדפות עמודות נפרדות מהדשבורד)
- Backend: endpoints חדשים ב-`schools_router.py` — GET (bulk+יחיד), PUT (upsert עם מעקב updated_by/updated_at ל-order_amount_gefen), העלאה/הורדה של קובץ חוזה ל-Storage (`check-files/contracts/...`)
- Frontend: אזור ניהול ← בתי ספר הוחלף מרשימת כרטיסים לטבלה מלאה (מיון לפי עמודה, חיפוש חופשי, "עמודות לתצוגה", עמודת שם מוסד קפואה, בורר שנת לימוד) עם עמודות זהות/חדשות: סמל, עיר, בעלות, שלב, פגישות/שעות, סוג שירות, מחיר מבוקש, אמצעי הזמנה, גובה הזמנה בגפן, שעות שהוזמנו, תעריף, תשלומים, חוזה (נשלח/התקבל/קובץ), אסמכתאות שנשלחו
- Frontend: שדה "גובה הזמנה בגפן" נוסף לטאב "פרטי בית הספר" בכרטיס בית ספר, מסונכרן דו-כיוונית עם הטבלה החדשה (משתמש בבורר שנת הלימוד הקיים בטאב), עם tooltip "מי עדכן/מתי"
- זהו שלב ראשון בתהליך איטרטיבי — עוד עמודות/לוגיקה עסקית יתווספו בהמשך לפי בקשת המשתמש

