# Dev Log

## 2026-07-06 — Feat: כפתורי "נקה בחירה" / "בחר הכל" בפאנל "עמודות להצגה"
- **Frontend (`DashboardPage.jsx`):** נוספו שני כפתורים בפינה השמאלית-תחתונה של פאנל "עמודות להצגה" (מתחת לרשת התיבות, מעל שורת הפרדה): "נקה בחירה" (מכבה את כלל 26 תיבות הסימון בבת אחת) ו-"בחר הכל" (מדליק את כולן בבת אחת, נוח כשרוצים את רוב העמודות ואז להסיר רק חלק) — שניהם שומרים את המצב החדש (`saveColPrefs`) בדיוק כמו סימון/הסרה בודדים.
- **הערה:** שמירת מצב הסימון בין מעברי עמוד/ריענון כבר הייתה קיימת מלפני כן (`localStorage` + טבלת `profiles.col_visible`/`col_order` ב-Supabase, נטענת מחדש ב-`init()`) — לא נדרש שינוי נוסף עבור זה.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק ששני הכפתורים עובדים כצפוי ושהמצב נשמר גם אחרי ריענון, לפני העלאה לאתר.

## 2026-07-06 — Feat: הקפאת עמודת "שם מוסד" + עדכוני כותרות/נתונים בטבלה הראשית
- **Frontend (`DashboardPage.jsx`):** עמודת "שם מוסד" (ותא ה-checkbox של מצב בחירה, כשמופעל) הופכה ל-`position: sticky` בכותרת ובגוף הטבלה, כך שהיא נשארת גלויה בגלילה אופקית ימינה-שמאלה; תא/כותרת הבחירה נעוצים ב-`right: 0`, ועמודת "שם מוסד" מיושרת אחריהם (`right: 2.5rem` כשה-checkbox מוצג, אחרת `right: 0`). לכל תא נעוץ בגוף הטבלה נקבע רקע אטום זהה למצב השורה (נבחרת/רגילה/ריחוף — דרך `group-hover` על ה-`tr`) כדי שתוכן שנגלל מתחתיו לא "יבצבץ".
- עמודת הסיכום "תקציב" שונתה ל-"**גובה תקציב**" (`SUMMARY_COLUMNS`).
- עמודת "חטיבה / תקציב" (מוצגת רק כשסינון מתקדם פעיל) שונתה ל-"**סוג תקציב**", וסדר הנתונים המוצגים בה הוחלף מ-[סוג חטיבה] · [סוג תקציב] ל-**[סוג תקציב] · [סוג חטיבה]**.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק גלילה אופקית עם/בלי מצב בחירה, ואת מצב הריחוף/הבחירה על השורה הנעוצה, לפני העלאה לאתר.

## 2026-07-06 — Feat: אוטומציית תזכורות מייל לפגישות (שלב א')
- **הקשר:** הכפתור "תזכורת" הקיים ליד כל פגישה (`reminder_enabled` ב-`meetings`) הבטיח למשתמש ("תישלח למשתתפים תזכורת יום לפני הפגישה") אך מעולם לא שלח בפועל שום דבר — היה רק דגל שנשמר ב-DB בלי מנגנון שקורא אותו. זהו שלב א' מתוך שני שלבים מתוכננים (שלב ב' — פורטל העלאת קבצים למזכירה — ימתין לתכנון נפרד).
- **DB (Supabase):** נוצרה טבלה חדשה `meeting_reminders` (meeting_id, school_id, recipient_email, recipient_name, status ['sent'/'failed'], error_message, sent_at) — מעקב **פרטני לכל נמען בנפרד** (לא ברמת הפגישה כולה), כדי שפגישה עם כמה משתתפים תציג בדיוק מי קיבל ומי לא. GRANT ל-service_role בלבד, RLS מופעל עם פוליסי `FOR ALL TO service_role`.
- **Backend (`schools_router.py`):** נוסף `POST /schools/meetings/send-due-reminders` — endpoint מוגן בסוד משותף בהדר (`X-Cron-Secret` מול `CRON_SECRET` מה-env, **לא** דרך ה-auth הרגיל של משתמשים) שמיועד להיקרא ע"י טריגר יומי חיצוני. מחשב "היום" לפי שעון ישראל (`Asia/Jerusalem`, לא UTC) ומאתר פגישות עם `reminder_enabled=true`+`status='scheduled'` שמועד קיומן "מגיע" (מחר; ביום חמישי גם שישי+שבת+ראשון נבלעים יחד, כי אין הרצה בשישי/שבת). לכל פגישה נשלח מייל בסיסי (בלי קישור העלאה — זה שלב ב') לכל משתתף עם כתובת מייל תקינה (`participants`, לא `advisor_ids` — תואם את ה-tooltip הקיים), ונרשמת שורה נפרדת ב-`meeting_reminders` לכל נמען (הצלחה/כישלון בנפרד) — כישלון של נמען אחד לא חוסם את השאר ולא מפיל את שאר הבקשה. פגישה שכבר טופלה (יש לה שורות ב-`meeting_reminders`) מדולגת — אידמפוטנטי, בטוח מול ריצה כפולה. נוסף גם `GET /schools/meetings/{id}/reminder-status` (מוגן auth רגיל) להצגת הסטטוס בפרונט. קוד שליחת המייל (Gmail SMTP קיים) הוכפל בכוונה לפונקציה שלישית ממוקדת ולא אוחד עם שני המקומות הקיימים (טופס יצירת קשר, הזמנה מחדש) — הוחלט במפורש לא לגעת בקוד עובד של פיצ'רים לא קשורים לשם יתרון תיאורטי בלבד, בהתאם לכלל הפרויקט נגד הפשטה מוקדמת.
- **טריגר יומי (`.github/workflows/meeting-reminders.yml`):** GitHub Actions מתוזמן, רץ רק א'-ה' (`cron: '0 3 * * 0-4'`, 03:00 UTC), עם `workflow_dispatch` להרצה ידנית. **נדרש שלב ידני** — הוספת `BACKEND_URL`/`CRON_SECRET` ל-GitHub repo secrets, והוספת `CRON_SECRET` (כבר קיים ב-`.env` המקומי) גם למשתני הסביבה ב-Render.
- **Frontend (`MeetingRow.jsx`):** ליד כפתור התזכורת, כשמופעל, מוצגת תגית "נשלח ל-X/Y" (כחול = כולם הצליחו, כתום = יש כישלון) עם tooltip שמפרט סטטוס פר-נמען.
- **בדיקות שבוצעו (קצה-לקצה, מול ה-DB האמיתי ומול Gmail האמיתי):** שרת מקומי הורם בפועל; אומת ש-401 מוחזר בלי/עם סוד שגוי; נוצרה פגישת בדיקה אמיתית למחר עם משתתף תקין (geffen360@gmail.com) ומשתתף עם כתובת פגומה — הריצה החזירה `sent:1, failed:1`, המייל האמיתי נשלח בהצלחה, ונרשמו שתי השורות הנכונות ב-`meeting_reminders` (כולל הודעת השגיאה האמיתית של Gmail על הכתובת הפגומה); ריצה חוזרת אישרה אידמפוטנטיות (`skipped_meetings:1`, לא נשלח מייל כפול); אומת ש-`reminder-status` דורש auth רגיל (403 בלי טוקן); נתוני הבדיקה נמחקו בסיום. `npx vite build` עובר ב-0 שגיאות.
- **מומלץ לפני העלאה לאתר:** להוסיף את שני ה-secrets ל-GitHub ואת `CRON_SECRET` ל-Render, ולהריץ את ה-workflow ידנית (`workflow_dispatch`) פעם אחת כדי לוודא שהוא מגיע בהצלחה ל-endpoint האמיתי בפרודקשן לפני שסומכים על התזמון האוטומטי.

## 2026-07-06 — Fix: בדיקה שכוללת קבצים רק לתקציב אחד לא תדרוס נתונים טובים של תקציב אחר ב-check_metrics
- **הבעיה:** אם בית ספר עם כמה תקציבים (למשל גפן+תקומה) מריץ בדיקה שכוללת קבצי דיווח רק עבור תקציב אחד, הקוד הקודם עדיין כתב ל-`check_metrics` נתונים ריקים/אפסיים עבור התקציב שלא נבדק הפעם — מוחק נתונים אמיתיים וטובים מבדיקה קודמת.
- **גילוי לאורך הדרך:** ניסיון ראשוני להסתמך על `pct_tanuz` הקיים כאינדיקציה ל"התקציב נבדק הפעם" התברר כלא אמין — השדה מחשב יחס אמיתי (לא null) גם עבור תקציב עם 0 שורות דיווח בפועל בבדיקה הנוכחית.
- **Backend (`analyze_router.py` — `_finalize_tikhnun_metrics`):** נוסף דגל חדש ומפורש `doch_analyzed` לכל תקציב (`True`/`False`, לפי `norm in doch_budget_norms` — הסט הפנימי שכבר קיים וסופר איזה תקציבים היו להם שורות דיווח בבדיקה הזו), מוזרם גם ל-`overview` הראשי (לבתי"ס חד-תקציביים). תוספת שדה טהורה — לא נוגעת בשום חישוב קיים.
- **Backend (`_compute_check_metrics_rows`):** השדות פוצלו לשתי קבוצות: (1) **מבוססי-תכנון** (`pct_plan`, `pct_divuach`, `budget_amount`, `planned_amount`, `fixed_gap_abs`, `flexible_remaining`, `sum_chayav`, `sum_divuach`) — תמיד מתעדכנים, מגיעים מקובץ התכנון ולא תלויים בקבצי הדיווח שהועלו הפעם; (2) **מבוססי-דיווח** (`pct_tanuz`, `rejected_*`, `no_pdf_*`, `partial_*`, `finance_not_gefen_*`, `gefen_not_finance_*`) — נכללים ב-upsert רק כש-`doch_analyzed=True` לאותו תקציב; אחרת מדולגים לגמרי (לא נכתב אליהם כלל), כך ש-Supabase משמר את הערך הקודם בעמודה (upsert חלקי לפי העמודות שנשלחות בפועל, לא מוחק עמודות שלא נשלחו).
- **בדיקות שבוצעו:** אומת ישירות מול ה-DB האמיתי בשני מבחנים נפרדים: (1) upsert גולמי עם עמודות חסרות — אושר ש-Supabase משמר את הערך הקודם בעמודות שלא נשלחו; (2) סימולציית קצה-לקצה עם `_save_check_metrics` על שני "ריצות" רצופות (בדיקה א׳ עם 2 תקציבים, בדיקה ב׳ עם תקציב אחד בלבד) — אושר שהתקציב שלא נבדק בבדיקה ב׳ שימר את `pct_tanuz`/`rejected_count` המקוריים מבדיקה א׳ (לא נדרסו לערכים המזויפים של בדיקה ב׳), בעוד ש-`pct_plan` שלו כן התעדכן כצפוי (שדה מבוסס-תכנון). `python -c "import ast; ..."` תקין.
- **הערה:** התיקון חל על בדיקות **חדשות** מעכשיו והלאה בלבד — לפי החלטה משותפת, נתונים היסטוריים שכבר נשמרו ב-`check_metrics` נשארים כמו שהם (לא בוצע backfill רטרואקטיבי, גם כי קבצים מעל 24 חודש כבר נמחקו מה-Storage וממילא לא ניתן היה לשחזר בוודאות אם תקציב נבדק בבדיקה היסטורית מסוימת).

## 2026-07-06 — Fix: זיהוי חטיבה שגוי בעמודות סיכום הדשבורד (check_metrics) לבתי ספר ללא gefen_accounts
- **הבעיה שדווחה:** בתי ספר שש-שנתיים ללא שורות `gefen_accounts` (בדיקות ישנות/גימנסיה) לא הציגו נתונים בכלל בעמודות הסיכום החדשות, ולעומת זאת בית ספר יסודי ("יאללה ביתר", שיש לו בפועל 2 חשבונות גפן אמיתיים תיכון+ביניים) הציג שורת "יסודי" מיותרת בנוסף לשתי השורות האמיתיות.
- **גורם השורש (`analyze_router.py` — `_compute_check_metrics_rows`):** כשאין `gefen_account_id` על שורת הבדיקה, הקוד הקודם נפל ישירות ל-`school.stage` — אבל (1) לבתי"ס שש-שנתיים `stage="sheshshnati"` אינו ערך חטיבה תקף, כך שלא נוצרה אף שורה, ו-(2) לבתי"ס עם כמה חשבונות גפן אמיתיים (כמו "יאללה ביתר"), נפילה ל-`stage` יצרה שורה שגויה במקום פשוט לדלג על הבדיקה שאין לה שיוך ודאי.
- **התיקון:** נוסף מקור מידע חדש שהיה קיים כל הזמן ולא נוצל — `summary.division` (חטיבה שזוהתה אוטומטית מקודי הדיווח בקובץ הגפן עצמו של אותה בדיקה, "tikkon"/"beinayim"). סדר הזיהוי החדש: (1) `gefen_account_id`→`gefen_accounts.division_type` (הכי אמין), (2) `summary.division` — **רק** אם `schools.stage` אינו "yesodi" (כי הזיהוי האוטומטי לא יודע לזהות "יסודי" בכלל — קודי יסודי חופפים לקודי ביניים ומסווגים כ"beinayim" מטבעם, ולכן הסתמכות עליו לבית ספר יסודי הייתה שגויה), (3) אם לבית הספר יש בדיוק חשבון גפן אמיתי אחד — משתמשים בחטיבה שלו, (4) אם אין לו אף חשבון — נופלים ל-`schools.stage` (רק כשזה ערך חטיבה יחיד תקף, לא "sheshshnati").
- **Backfill:** `check_metrics` נוקתה לגמרי (`DELETE FROM check_metrics`) והורצה מחדש עם הלוגיקה המתוקנת (למניעת שורות "יתומות" משגיאות קודמות שנשארות כי upsert לא מוחק). אומת ישירות מול 4 בתי ספר שדווחו + סריקה השוואתית של `stage` מול `division_type` שנוצר לכל 21 בתי הספר בארגון — כל ההתאמות הגיוניות כעת (בתי"ס חד-חטיבתיים מציגים רק את החטיבה שלהם, שש-שנתיים עם 2 חטיבות אמיתיות מציגים את שתיהן). שני מקרי קצה שנותרו ("יאללה ביתר", "שעות פרויקט גפני") הם נתוני בדיקה/הדגמה אמיתיים שבהם ה-`stage` המוצהר של בית הספר לא תואם את מה שבפועל נבדק (יש להם checks אמיתיים לחטיבות שאינן תואמות את ה-stage) — לא באג בקוד, הקוד כעת נאמן למה שבאמת נבדק.
- **Frontend:** נוסף `sheshshnati: "שש שנתי"` ל-`DIVISION_LABEL` (`DashboardPage.jsx`) כתיקון קוסמטי נלווה — אם אי-פעם תוצג שורת placeholder לבית ספר שש-שנתי ללא נתונים כלל, התווית תוצג בעברית במקום הערך הגולמי.
- **בדיקות שבוצעו:** שאילתות ישירות מול ה-DB לכל אחד מהמקרים שדווחו + שאילתת סריקה מקיפה של כל 21 בתי הספר; `python -c "import ast; ..."` תקין; `npx vite build` עובר ב-0 שגיאות.

## 2026-07-06 — Fix: ברירת מחדל מערכתית להרשאת "לאשר בקשות עריכת פרטים" עבור מנהל
- **Backend (`schools_router.py`):** נותח מלוא מבנה ההרשאות (`PERMISSION_DEFAULTS`, `permission_settings`, `user_permission_overrides`, `_check_permission`) מנקודת מבט של בעלים חדש שרק פתח חשבון. נמצאה אי-התאמה יחידה: ברירת המחדל המערכתית ל-`can_approve_update_requests` עבור מנהל הייתה `True`, בעוד CLAUDE.md מגדיר במפורש שאישור בקשות ע"י מנהל תלוי בהאצלה מפורשת מהבעלים (`delegate_approvals_to_managers`) ולא אמור להיות מופעל אוטומטית. שונתה ברירת המחדל ל-`False` (שורה 2729). שאר 11 ההרשאות נבדקו ונמצאו תואמות למה שמומלץ לבעלים חדש. השינוי משפיע רק על ארגונים חדשים ללא הגדרות `permission_settings` קיימות — לא נוגע בהגדרות/overrides שכבר הוגדרו במפורש.
- **בדיקות שבוצעו:** עדכון קוד יזום (קבוע Python בלבד, אין שינוי סכימת DB). **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לדמות ארגון בעלים חדש (ללא רשומות ב-`permission_settings`) ולוודא שטאב "הרשאות" מציג "לא" עבור מנהל בשורה "לאשר בקשות עריכת פרטים".

## 2026-07-06 — Feat: עדכון מדיניות פרטיות — התאמה לאחסון בפועל ב-Supabase Storage
- **Frontend (`PrivacyPage.jsx`):** מערך `SECTIONS` הוחלף במלואו (מ-8 ל-11 סעיפים). הטקסט הישן טען ש"אין שמירה קבועה" של קבצים ושמחיקתם אוטומטית "עם סיום הסשן" — זה כבר לא נכון מאז המעבר ל-Supabase Storage. הניסוח החדש מפרט במדויק: קבצי מקור/Excel נשמרים ב-Storage ונמחקים אוטומטית לאחר 24 חודשים (לפי `_cleanup_old_storage_files` ב-`main.py`), בעוד סיכום תוצאות הבדיקה (`check_logs`) נשמר ללא הגבלת זמן; קבצי עיבוד זמניים (`run_states`) נמחקים תוך 120 יום. נוספו גם סעיף "מטרת איסוף המידע" וסעיף "זכויות המשתמש" (עיון/תיקון/מחיקה לפי חוק הגנת הפרטיות), וסעיף אבטחת מידע הורחב עם פרטים קונקרטיים על ההגנה שמספק Supabase (מסלול Pro, 25$): הצפנת AES-256 במנוחה, TLS 1.2+ בתעבורה, גיבויים יומיים ל-7 ימים, מיקום שרתים באירופה (GDPR), והסמכת SOC 2 Type II ברמת התשתית. תאריך העדכון בראש הדף עודכן ל-6.7.26.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לצפות בעמוד `/privacy` לפני העלאה לאתר, לוודא תצוגה תקינה של כל 11 הכרטיסים.

## 2026-07-06 — Fix: 4 בעיות בעמודות הסיכום החדשות בדשבורד
- **פאנל "עמודות להצגה" חתוך:** הפאנל הורחב (`minWidth` 560→820, `maxWidth` 90vw→95vw), עוגן כעת ממורכז מתחת לכפתור (`right: 50%; transform: translateX(50%)` במקום `right: 0`, כך שהוא גדל גם ימינה וגם שמאלה מהכפתור ולא רק שמאלה), המרווח בין הטורים גדל (`gap-x-2`→`gap-x-6`), והוסר ה-`truncate` מהתווית (הוחלף ב-`whitespace-nowrap`) — כל הכותרות הארוכות נכנסות עכשיו בשורה אחת.
- **חוסר עקביות בפיצול שורות (נחקר לעומק, לא תוקן קוד — ממצא לגבי הנתונים):** "יאללה ביתר" מוגדר `stage=yesodi` אך בפועל יש לו **2 שורות `gefen_accounts`** אמיתיות (tikkon+beinayim) עם בדיקות "גפן" נפרדות לכל אחת — ולכן הוא מפוצל ל-2 שורות בצדק (זו אי-התאמה בנתוני בית הספר עצמו בין `stage` המוצהר לחשבונות בפועל, לא באג בדשבורד). "טל - שש שנתי" לעומת זאת מוגדר `sheshshnati` אך **מעולם לא רץ בו אף בדיקה** (0 שורות ב-`check_logs`) — כך שאין נתונים לפצל לפיהם, ולכן מוצגת שורת placeholder בודדת. שני המקרים נובעים מנתוני בדיקה/הדגמה חלקיים, לא מלוגיקה שגויה.
- **Fix — בתי ספר עם בדיקות אמיתיות שלא הוצג עבורם דבר (למשל קשת שער הנגב, סמל 616805):** אותר הגורם — `_compute_check_metrics_rows` (`analyze_router.py`) הסתמך אך ורק על `gefen_account_id` כדי לזהות את חטיבת הבדיקה עבור בתי ספר חד-חטיבתיים; **38 מתוך כלל שורות `check_logs`** אין להן `gefen_account_id` (כנראה בתי ספר בלי שורת `gefen_accounts` כלל — אותה בעיה בדיוק שתוקנה בעבר עבור פיצ'ר "יעדים", לפי `dev-log` מ-2026-07-05), ולכן הבדיקה נמחקה בשקט מהחישוב. נוספה נפילה חוזרת ל-`schools.stage` כשאין `gefen_account_id`/החשבון לא נמצא. **הורץ מחדש `backfill_check_metrics.py`** עם התיקון — אומת שקשת שער הנגב מקבל כעת שורת `check_metrics` אמיתית (אחוז תכנון 96.6% לתקציב גפן).
- **גלילת מקלדת + עובי פס גלילה בטבלה הראשית:** הפיצ'ר הזה (חצים במקלדת + פס גלילה עבה פי 2 + כפתורי חצים בפינות) מעולם לא מומש עבור טבלת הדשבורד הראשית — היה קיים רק בטבלת היסטוריית הבדיקות ב-`SchoolPage`. נוסף עכשיו גם כאן: מאזין `keydown` גלובלי (חצים ימינה/שמאלה, מדלג על שדות טקסט), מחלקת CSS חדשה `.dash-scroll-x` (10px, פי 2 מהגלובלי), וכפתורי חץ קטנים בפינות התחתונות של הטבלה (אותו עיצוב בדיוק כמו ב-`SchoolPage`).
- **בדיקות שבוצעו:** אימות ישיר מול ה-DB האמיתי לכל אחד מ-4 בתי הספר שדווחו (שאילתות `check_metrics`/`gefen_accounts`/`check_logs`); `python -c "import ast; ..."` על ה-router תקין; `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק את רוחב הפאנל, גלילת המקלדת, ואת "קשת שער הנגב" בדשבורד בפועל לפני העלאה לאתר.

## 2026-07-06 — Fix: טאב "יעדים" — כרטיס הטבלה נמתח מעבר לשורה האחרונה
- **Frontend (`GoalsTab.jsx`):** הוסר ה-`minHeight: calc(100vh - 240px)` הקבוע מכרטיס טבלת "יעדים" — מכיוון שמספר השורות קבוע (7), הגובה הכפוי יצר שטח ריק גדול מתחת לשורה האחרונה. הכרטיס מתאים עכשיו את גובהו לתוכן בפועל ומסתיים ישר אחרי השורה האחרונה (טבלת "תאריכים חשובים" לא הושפעה — לא היה בה מלכתחילה גובה כפוי).

## 2026-07-06 — Feat: 19 עמודות סיכום-בדיקה חדשות בדשבורד + פאנל "עמודות להצגה" כרשת
- **DB (Supabase):** נוספו 14 עמודות נומריות חדשות ל-`check_metrics` (`fixed_gap_abs`, `flexible_remaining`, `sum_chayav`, `sum_divuach`, `rejected_count/sum`, `no_pdf_count/sum`, `partial_count/sum`, `finance_not_gefen_count/sum`, `gefen_not_finance_count/sum`) — בנוסף לחמש הקיימות (`pct_plan/pct_divuach/pct_tanuz/budget_amount/planned_amount`).
- **Backend (`analyze_router.py`):** חולצה פונקציה טהורה משותפת `_compute_check_metrics_rows()` מתוך `_save_check_metrics` (המשמשת גם את הבקאנד החי וגם את סקריפט ה-backfill), שהורחבה לחשב את כל 19 השדות פר (חטיבה, תקציב): מ-`bud["overview"]` (`fixed_gap_abs`/`flexible_remaining`/`sum_chayav`/`sum_divuach` — מחושבים per-budget גם בבתי"ס רב-תקציביים), מ-`per_budget_rejected`/`per_budget_no_pdf` (כמות+סכום מפורסר מ-"סכום"), מ-`partial_rows` המסונן לפי שם תקציב, ומ-`per_combo_results` (התאמה לפי `budget`+מיפוי `stage`→`division_type` חדש `{"תיכון":"tikkon","חטיבת ביניים":"beinayim","יסודי":"yesodi"}`).
- **Backend (סקריפט חד-פעמי `backend/backfill_check_metrics.py`):** עבר על כל 500+ שורות `check_logs` הקיימות (ממוין `run_at` עולה, כך שהבדיקה האחרונה לכל שילוב מנצחת בדיוק כמו ב-upsert החי) והזין את `check_metrics` בשדות החדשים גם לבדיקות היסטוריות. הורץ בפועל: 84 שורות תרמו נתונים, 2 דולגו (ללא נתוני תכנון).
- **Backend (`schools_router.py` — `list_schools`):** ה-`select` על `check_metrics` הורחב לכלול את כל 19 השדות (היו 3 בלבד נבחרים בעבר).
- **Frontend (`DashboardPage.jsx`):** נוסף קבוע `SUMMARY_COLUMNS` (19 עמודות חדשות, כבויות כברירת מחדל) לצד 7 העמודות הקיימות ב-`MOVABLE_COLUMNS`; ערך כל עמודה נלקח מהבדיקה **האחרונה שבוצעה בפועל** (לא מושפעת מנעיצת שורות ב-`SchoolPage`), עבור תקציב "גפן" כברירת מחדל — נדרס רק כשנבחר תקציב יחיד בסינון המתקדם (`activeSummaryBudget`). הפעלת עמודת סיכום כלשהי מרחיבה את מנגנון הפיצול הקיים (`advancedFilterActive`, שקודם הופעל רק ע"י סינון יעדים) כך שבתי"ס שש-שנתיים עם 2 חטיבות (כל אחת עם "גפן" נפרד) מוצגים כשתי שורות — בלי "להתפוצץ" לשורה נפרדת לכל תקציב קיים כשלא נבחר תקציב ספציפי.
- **Frontend (פאנל "עמודות להצגה"):** הפך מרשימה אנכית לרשת של 3 טורים (`grid grid-cols-3`, פאנל רחב עד `560px`/`90vw`) כדי להכיל את 26 העמודות בנוחות; תיבת החיפוש הקיימת ממשיכה לסנן את כל הרשימה המורחבת.
- **בדיקות שבוצעו:** מיגרציית ה-DB אומתה דרך ה-Management API; הרצת ה-backfill בפועל מול ה-DB האמיתי ואימות ידני של השורות שהתקבלו (ערכים סבירים ומגוונים ל-`rejected_count`/`partial_count`/`finance_not_gefen_count` וכו'); `python -c "import ast; ..."` על שני קבצי ה-router תקין; `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק הפעלת כמה עמודות סיכום על בית ספר רגיל ועל בית ספר שש-שנתי (וידוא פיצול ל-2 שורות), ובחירת תקציב ספציפי בסינון המתקדם, לפני העלאה לאתר.

## 2026-07-06 — Feat: "תאריכים חשובים" — 20 מועדים נוספים (רשימת תשפ"ז מלאה)
- **Backend (`GOAL_DEFINITIONS`):** נוספו 20 יעדי `kind: "date"` חדשים (מתוך רשימה שהתקבלה עבור תשפ"ז — תאריך היעד נשמר כבסיס תשפ"ו, כלומר מינוס שנה בדיוק, כדי ש-`_shift_goal_date` הקיים ימשיך לחשב את תשפ"ז אוטומטית). שני פריטים עם שני תאריכים ("X / Y, בהתאמה") פוצלו לשתי שורות נפרדות עם מהות מדויקת לכל תאריך: מועדי טפסי פתיחת חשבון בנק (נובמבר/דצמבר בהתאמה), ואישור סופי שינויי תוכנית עבודה (פיקוח/רשות בהתאמה). כל 30 ההגדרות (7 יעדי תכנון/דיווח + 23 תאריכים חשובים) ממוינות כרונולוגית לפי `target_date` יחד במערך אחד.
- **בדיקות שבוצעו:** אומת ישירות מול שתי שנות הלימודים — `important_dates` מחזיר 23 פריטים ממוינים כרונולוגית עבור כל אחת, עם התאמה מדויקת בין הבסיס (תשפ"ו) לתשפ"ז (למשל `2025-06-30`→`2026-06-30`, `2027-02-01`→`2028-02-01`, תואם בדיוק את הרשימה שסופקה עבור תשפ"ז); `goals` (7 היעדים המקוריים) לא הושפעו; `npx vite build` ו-`python -m py_compile` עוברים ב-0 שגיאות.

## 2026-07-05 — Feat: נעיצת שורת בדיקה בטבלת ההיסטוריה + תיקון גלילת מקלדת גלובלית
- **DB (Supabase):** נוספה עמודה `pinned_at TIMESTAMPTZ` לטבלת `check_logs` (נגישה רק דרך ה-backend).
- **Backend (`schools_router.py`):** נוסף `PATCH /schools/{school_id}/logs/{log_id}/pin` (`{pinned: bool}`) — מוגן באותה הרשאה שכבר שולטת בעריכת שם/מחיקת בדיקה. `GET /schools/{id}/logs` ממיין כעת כך שכל השורות הנעוצות מוצגות ראשונות, ממוינות לפי `pinned_at` יורד (כך שנעיצה אחרונה = הכי עליונה), ואחריהן שאר השורות הלא-נעוצות בסדר הרגיל (`run_at` יורד).
- **Frontend (`SchoolPage.jsx` — תפריט "···"):** נוספה אפשרות "נעץ" (עם אייקון נעץ) מעל "ערוך שם בדיקה"; בשורה נעוצה האפשרות הופכת ל"בטל נעיצה" (האייקון מוצג מלא/מודגש) — לחיצה עליה מבטלת את הנעיצה ומחזירה את השורה למקומה הרגיל בהיסטוריה.
- **Fix — גלילת מקלדת:** מקשי החצים ימינה/שמאלה גללו את הטבלה רק אם המכל עצמו היה ממוקד (focus) קודם. הוחלף במאזין `keydown` ברמת ה-`document` שמופעל כל עוד טאב "בדיקות" מציג את תצוגת הטבלה (נרשם/מוסר אוטומטית עם מחזור החיים של `ChecksTab`, כלומר פעיל רק כשנמצאים בטאב הזה) — כך שהחצים גוללים תמיד, ללא צורך ללחוץ קודם על אזור הגלילה. מדלג במכוון על שדות טקסט (`INPUT`/`TEXTAREA`/`contentEditable`) כדי לא לפגוע בהזזת סמן בחלונית שינוי השם או בתיבת החיפוש של "עמודות להצגה".
- **Frontend (אינדיקציה חזותית):** בשורת בדיקה נעוצה מוצג אייקון נעץ קטן (כתום) ליד שם/מועד הבדיקה, כדי שניתן יהיה לזהות מיד אילו שורות נעוצות בלי לפתוח את תפריט "···".
- **בדיקות שבוצעו:** מיגרציית ה-DB אומתה דרך ה-Management API; `python -c "import ast; ..."` על ה-router תקין; `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק נעיצה/ביטול נעיצה עם מספר שורות, וגלילת מקלדת בלי לחיצה מוקדמת, לפני העלאה לאתר.

## 2026-07-05 — Fix: 3 תיקוני UX לטבלת היסטוריית בדיקות (המשך לפיצ'ר הקודם)
- **טולטיפ מוסתר:** הטולטיפ הצהוב (`CheckLinkTooltip`) היה מוסתר מאחורי שורות מאוחרות יותר בטבלה — נגרם כי כל תא "מועד הבדיקה" בגוף הטבלה הוא `position: sticky` (יוצר הקשר-ערימה (stacking context) נפרד לכל שורה), כך ש-`z-index` פנימי בתוך שורה אחת לא יכול "לנצח" שורה מאוחרת יותר עם אותו `z-index`. תוקן ע"י מעבר ל-`createPortal` (מ-`react-dom`) שמצייר את הטולטיפ ישירות ל-`document.body` במיקום `fixed` מחושב (`getBoundingClientRect` בעת ריחוף), כך שהוא לגמרי מחוץ להקשרי הערימה של הטבלה ותמיד צף מעל הכל; נסגר אוטומטית גם בגלילה (מאזין `scroll` ברמת ה-`document` עם `capture: true`, כדי לתפוס גם גלילה בתוך מכל הטבלה עצמו).
- **כפתורי חצים:** הוסרה השורה הנפרדת מתחת לטבלה; הכפתורים הוחזרו להיות חופפים לפינות התחתונות של אזור הגלילה עצמו (ולא בשורה נפרדת) — ממוקמים `absolute` בפינה הימנית-תחתונה והשמאלית-תחתונה, בגובה זהה לפס הגלילה עצמו, כך שהם נראים כ"תפוסים בצדי" פס הגלילה במקום מתחתיו.
- **גלילה במקלדת:** נוסף `tabIndex={0}` ו-`onKeyDown` (מטפל ב-`ArrowLeft`/`ArrowRight` עם `scrollBy`) ישירות על מכל הגלילה, כדי להבטיח שגלילה אופקית עם מקשי החצים תמיד עובדת (במקום להסתמך על מנגנון המיקוד האוטומטי המובנה של הדפדפן על אזורי גלילה, שהפסיק לפעול כצפוי אחרי השינוי הקודם).
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לבדוק את שלושת התיקונים בפועל לפני העלאה לאתר.

## 2026-07-05 — Feat: טבלת היסטוריית בדיקות — שינוי שם, פס גלילה עבה + חצים, עמודת "מועד הבדיקה" קפואה, טולטיפ
- **DB (Supabase):** נוספה עמודה `custom_name TEXT` לטבלת `check_logs` (נגישה רק דרך ה-backend, ללא GRANT/RLS נוספים).
- **Backend (`schools_router.py`):** נוסף `PATCH /schools/{school_id}/logs/{log_id}/name` — מעדכן `custom_name` לבדיקה, מוגן באותה הרשאה שכבר שולטת במחיקת בדיקה (`can_delete_own_meetings`).
- **Frontend (`SchoolPage.jsx` — תפריט "···"):** נוספה אפשרות "ערוך שם בדיקה" (מעל "מחק") הפותחת `RenameCheckModal` חדשה (בהשראת `DeleteCheckModal` הקיימת) לעריכת שם מותאם אישית. אם הוגדר שם, הוא מוצג כטקסט הקישור הראשי בעמודת "מועד הבדיקה", עם המועד המקורי כטקסט משני קטן מתחתיו; ללא שם — ההתנהגות נשארת כפי שהייתה.
- **Frontend (עמודת "מועד הבדיקה"):** קובעה (frozen) בעזרת `position: sticky; right: 0` על תא הכותרת ותאי הגוף (כולל שורת ה-pending ושורת ה-filler), כך שהעמודה נשארת גלויה גם בזמן גלילה אופקית ימינה-שמאלה בטבלה.
- **Frontend (פס גלילה):** מחלקת CSS ממוקדת חדשה `.hist-scroll-x` (ב-`index.css`) מגדילה את גובה פס הגלילה האופקי פי 2 (5px→10px) רק במכל הגלילה של טבלת ההיסטוריה (שאר פסי הגלילה באתר לא הושפעו); נוספה שורת בקרה דקה מתחת לטבלה עם שני כפתורי חץ קטנים (ימין/שמאל) שגוללים את הטבלה בלחיצה (`scrollBy`), כאלטרנטיבה לגרירת פס הגלילה.
- **Frontend (טולטיפ):** קומפוננטה חדשה `CheckLinkTooltip` (אותה שפה חזותית כמו `QuestionTooltip` הקיים — רקע צהוב, מסגרת כתומה) עוטפת את קישור השם/מועד; בריחוף מוצגת הודעה "לחץ להצגת מפורטת של תוצאות הבדיקה."
- **בדיקות שבוצעו:** מיגרציית ה-DB אומתה דרך ה-Management API (עמודה קיימת); `python -c "import ast; ...` על ה-router תקין; `npx vite build` עובר ב-0 שגיאות. **לא בוצעה בדיקה ידנית בדפדפן** (מחייבת התחברות עם משתמש אמיתי) — מומלץ לבדוק שינוי שם/גלילה/הקפאה/טולטיפ בדפדפן לפני העלאה לאתר.

## 2026-07-05 — Fix: ספינר טעינה בטבלת "בדיקות" במקום "אין בדיקות קודמות" מוקדם מדי
- **Frontend (`SchoolPage.jsx`):** נוסף state נפרד `logsLoading` (מתחיל כ-`true`) שמנוהל סביב טעינת/רענון היסטוריית הבדיקות (`load()` הראשוני ו-`reloadLogs`). כל עוד `logsLoading===true` והרשימה עדיין ריקה, שורת ה"אין בדיקות קודמות" בטבלת `ChecksTab` מוחלפת בספינר עם הטקסט "טוען בדיקות קודמות..." (`role="status"`); ההודעה "אין בדיקות קודמות" מוצגת רק אחרי שהטעינה הסתיימה בפועל ואומת שהרשימה ריקה.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות.

## 2026-07-05 — Feat: פאנל "סינון מתקדם" — מבנה accordion + סינון יעדים מרובה עם וגם/או
- **Frontend (`DashboardPage.jsx`):** פאנל "סינון מתקדם" פוצל למבנה accordion עם 7 אזורים מתקפלים, כל אחד עם כותרת שניתן ללחוץ עליה לפתיחה/סגירה (כמו "שאלות ותשובות") ותג מספרי לכמות התנאים הפעילים בו:
  - **ראשי** (פתוח כברירת מחדל) — כל שדות הסינון הקיימים (שם בית ספר עד גישה), ואחריהם **שנת לימודים** ו**סוג תקציב** (הועברו לכאן מהמקום הישן שלהם). "סוג תקציב" כולל כעת רשימת ברירת מחדל קבועה (גפן/דוקאטי/תנופה/גפן חירום/פל"ג/כללי) בנוסף לכל שם תקציב שכבר קיים בפועל ב-`check_metrics` — כך שהרשימה הנפתחת אינה ריקה גם לפני שנצברו נתוני `check_metrics` בפועל (טבלה חדשה, מתמלאת רק מבדיקות עתידיות).
  - **יעדים** (סגור כברירת מחדל) — הוחלף מ"בחירת יעד בודד + מצב" לרכיב "+ הוסף יעד לסינון" עם רשימה נפתחת לבחירה מרובה מתוך כלל היעדים (מ-`GET /schools/goal-definitions`); כל יעד שנבחר מתווסף כשורה נפרדת עם בורר מצב (עומד/לא עומד/לא סומן) וכפתור הסרה (×). מהשורה השנייה ואילך מוצג בורר **וגם/או** לחיבור לתנאי הקודם — המשתמש בוחר בעצמו את אופן הצירוף לכל תנאי נוסף (ולא AND קבוע). ההערכה מתבצעת משמאל לימין לפי סדר ההוספה, ללא סדר עדיפויות/סוגריים.
  - **אחוז תכנון / אחוז דיווח כללי / אחוז דיווח למודל תמרוץ / דיווח חסר / ללא PDF** — נוצרו ככותרות accordion ריקות (מוצג טקסט "יתווסף בהמשך") לקראת מימוש בהמשך, שלב-אחר-שלב.
  - סינון האחוז המשולב הישן ("סינון לפי אחוז תכנון/דיווח") הוסר בשלב זה — יוחלף בשלושת האזורים הייעודיים הנפרדים כשהם יתממשו.
  - כל מצב הסינון (כולל אילו אזורים פתוחים/סגורים) נשמר ב-`sessionStorage` ומשוחזר בטעינה הבאה; "ניקוי סינון" מאפס את כל התנאים (כולל תנאי היעדים וסוג התקציב, לא כולל שנת הלימודים).
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות. **לא בוצעה עדיין בדיקה ידנית בדפדפן** — מומלץ לבצע לפני העלאה לאתר, כולל בדיקת שילוב וגם/או עם 3+ תנאי יעד.

## 2026-07-05 — Feat: סינון מתקדם במסך הבית לפי בדיקות (אחוזי תכנון/דיווח) ויעדים
- **DB (Supabase):** נוצרה טבלת `check_metrics` (school_id, gefen_account_id, division_type, budget_name, academic_year, pct_plan, pct_divuach, pct_tanuz, budget_amount, planned_amount, run_id, check_log_id) עם UNIQUE(school_id, division_type, budget_name, academic_year) — מבנה שטוח שמכיל את האחוזים המחושבים העדכניים ביותר לכל שילוב חטיבה+תקציב, כדי שאפשר יהיה לסנן לפיהם ישירות בלי לפרק את ה-JSON הגדול של `check_logs.summary`. GRANT ל-service_role, RLS מופעל, ללא GRANT ל-authenticated (נגישה רק דרך FastAPI).
- **Backend (`analyze_router.py`):** נוספה `_save_check_metrics()` — נקראת (לא-קריטית, בתוך try/except נפרד) בסוף `_save_check_log()` בכל שמירת בדיקה; ממלאת שורת `check_metrics` אחת לכל תקציב (מתוך `tikhnun_result["budgets"]` לבתי ספר רב-תקציביים) או שורה יחידה בשם "כללי" (לבתי ספר עם תקציב בודד, לפי ה-overview הראשי). בבתי ספר שש-שנתיים נקבע `division_type` ישירות מ-`tikhnun_tikkon`/`tikhnun_beinayim`; בחטיבה יחידה נשלף מ-`gefen_accounts` לפי `gefen_account_id`.
- **Backend (`schools_router.py`):** `GET /schools/` מקבל כעת פרמטר `academic_year` (ברירת מחדל תשפ"ו) ומעשיר כל בית ספר בשדות חדשים `check_metrics` ו-`goal_statuses` (שאילתות שטוחות ולא-קריטיות על `check_metrics`/`school_goals`, ללא N+1). נוסף גם `GET /schools/goal-definitions` — מחזיר את `GOAL_DEFINITIONS` (key/kind/goal_number/label בלבד) עבור תפריט היעדים בפאנל הסינון, כדי שהרשימה לא תשוכפל בצד הלקוח.
- **Frontend (`DashboardPage.jsx`):** פאנל "סינון מתקדם" קיבל 4 שדות חדשים: **שנת לימודים** (ברירת מחדל תשפ"ו, בחירה גורמת לטעינה מחדש של רשימת בתי הספר עם השנה הנבחרת), **סוג תקציב** (בחירה מרובה, ברירת מחדל = הכל, אפשרויות נגזרות דינמית מתוך `check_metrics` שהתקבלו), **סינון לפי יעד** (יעד + מצב: עומד/לא עומד/לא סומן), **סינון לפי אחוז תכנון/דיווח** (מדד + אופרטור ≥/≤/בין + ערך). כשאחד משני הסינונים האחרונים פעיל, הטבלה הראשית מציגה **שורה נפרדת לכל שילוב חטיבה+תקציב** התואם (עמודה נוספת "חטיבה / תקציב" עם האחוז הרלוונטי); ללא סינון פעיל ההתנהגות נשארת בדיוק כפי שהייתה (שורה אחת לכל בית ספר). כל השדות החדשים נשמרים ב-`sessionStorage` יחד עם שאר מצב הסינון, ומתאפסים בלחיצה על "ניקוי סינון" (מלבד שנת הלימודים).
- **בדיקות שבוצעו:** אימות grants/RLS/עמודות ל-`check_metrics` דרך ה-Management API; `python -m py_compile` על שני קבצי ה-router תקין; `npx vite build` עובר ב-0 שגיאות. **לא בוצעה עדיין בדיקה ידנית מלאה בדפדפן** (כולל הרצת בדיקה אמיתית על בית ספר שש-שנתי עם 2+ תקציבים כדי לוודא שהמיזוג ל-`check_metrics` והפיצול לשורות בטבלה עובדים כצפוי) — מומלץ לבצע לפני העלאה לאתר.

## 2026-07-05 — Feat: טאב "יעדים" — פיצול ל"סוג"/"יעד" (מספר) + טבלת "תאריכים חשובים" חדשה
- **DB (Supabase):** נוספו לטבלת `school_goals` שתי עמודות חדשות — `goal_type` (TEXT: `planning`/`reporting`) ו-`goal_number` (INT: המספר מתוך היעד, למשל 40/70/90/10/25/85) — כדי שאפשר יהיה לשאול/לסנן ישירות מ-Supabase לפי סוג ומספר (לצורך שימוש עתידי בדשבורד), במקום להסתמך רק על `goal_key` שדורש את קובץ הקוד כדי לפרש. 3 השורות הקיימות (מבדיקות קודמות) עודכנו רטרואקטיבית עם הערכים הנכונים. `PATCH /schools/{id}/goals` כבר לא מקבל type/number מהלקוח — הבקאנד גוזר אותם עצמאית מ-`GOAL_DEFINITIONS` לפי `goal_key` (מונע חוסר עקביות).
- **Backend (`GOAL_DEFINITIONS`):** כל הגדרת יעד מסומנת עכשיו ב-`kind`: `"planning"` / `"reporting"` (7 יעדים — נשארים בטבלת "יעדים" עם מעקב עמידה) או `"date"` (3 יעדים — "מועד אחרון להזנת מלגות בגפ"ן", "מועד אחרון לשינויים בתוכנית העבודה", "מועד אחרון להנפקת אסמכתאות") שהועברו ל"תאריכים חשובים" בלבד, ללא מעקב עמידה. `GET /schools/{id}/goals` משנה צורה מ-array שטוח ל-`{goals: [...], important_dates: [...]}`.
- **Frontend (`GoalsTab.jsx`):** טבלת "יעדים" עודכנה ל-5 עמודות בסדר (מימין לשמאל): תאריך יעד, סוג ("תכנון"/"דיווח"), יעד (מספר בלבד, ללא %), מצב נוכחי, עמידה ביעד. "מצב נוכחי" נגזר עכשיו מ-`goal.goal_type` (planning→`pct_plan`, reporting→`pct_tanuz`) במקום משני `Set`ים קבועים של goal_key. נוספה טבלת "תאריכים חשובים" חדשה מתחת (מרווח `mt-32`, ≈128px, לפחות 4 שורות ריקות), באותו עיצוב, עם 2 עמודות (תאריך, מהות) — מוצגת פר-תקציב יחד עם טבלת היעדים באותה קריאת API.
- **בדיקות שבוצעו:** אומת ישירות מול בית ספר אמיתי (סמל 616805) — `GET` מחזיר 7 יעדים (עם `goal_type`/`goal_number` נכונים) ו-3 תאריכים חשובים; `PATCH` שומר `goal_type`/`goal_number` נכון (נבדק ואופס בחזרה ל-null אחרי הבדיקה כדי לא להשאיר מצב בדיקה); `npx vite build` ו-`python -m py_compile` עוברים ב-0 שגיאות.

## 2026-07-05 — Fix: אפשור מספר חלונות "השוואה בין בדיקות" פתוחים/ממוזערים במקביל
- **בעיה שדווחה:** ביצוע השוואה בבית ספר Y בזמן שחלון השוואה ממוזער של בית ספר X היה פתוח גרם לחלון X להיעלם — כי הקונטקסט תמך רק בהשוואה בודדת אחת (`compareData` יחיד).
- **Frontend (`CompareChecksContext.jsx`):** שונה מ-state יחיד (`compareData`) למערך `compareWindows: [{id, data, minimized}]` — כל השוואה שנפתחת מקבלת `id` ייחודי ונשמרת בנפרד; `closeCompare(id)`/`setMinimized(id, bool)`/`patchCompare(id, updater)` פועלים רק על החלון הספציפי. חתימת ה-API כלפי חוץ (`openCompare(data) → id`, `patchCompare(id, updater)`) לא השתנתה, כך שלא נדרש שינוי ב-`SchoolPage.jsx`.
- **Frontend (`CompareResultsWindow.jsx`):** פוצל ל-`CompareResultsWindow` (רנדור כל החלונות מה-array) ו-`CompareWindowItem` (מופע אחד, נשאר mounted תמיד — גם כשממוזער — כדי לשמר מיקום/גודל/תקציב נבחר ברגע השחזור). מיקום התחלתי לכל חלון "משורשר" (cascade) לפי ה-`id` שלו כדי שחלונות שנפתחים בו-זמנית לא ייפתחו בדיוק זה על זה. חלונות ממוזערים מוצגים **זה לצד זה** (רוחב קבוע 260px, מרווח 10px) בפינה השמאלית התחתונה, לפי סדר הפתיחה.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות; הרמוניה סטטית (Playwright) שחזרה בדיוק את התרחיש שדווח — פתיחת השוואה X, מיזעור, פתיחת השוואה Y (החלון של X נשאר ממוזער ולא נעלם), מיזעור גם Y (שני ה-pills מוצגים זה לצד זה, ללא חפיפה), שחזור שניהם (שני חלונות מלאים ועצמאיים, ממוקמים ב-cascade), וסגירת אחד מהם (השני נשאר פתוח ללא שינוי).

## 2026-07-05 — Fix: טאב "יעדים" — עדכון 3 תאריכי יעד (2 עדכוני בסיס + חריג חד-פעמי לתשפ"ז)
- **Backend (`GOAL_DEFINITIONS`):** `reporting_valid_10_dec` (יעד דיווח תקין 10%) עודכן מ-15/12 ל-31/12 (משפיע אוטומטית על שתי השנים דרך `_shift_goal_date`: תשפ"ו 31/12/25, תשפ"ז 31/12/26). `work_plan_changes_deadline_jun` (מועד שינויי תוכנית עבודה) עודכן מ-30/06 ל-10/07 (תשפ"ו 10/07/26, תשפ"ז 10/07/27).
- **Backend (מנגנון חדש — `date_overrides`):** נוסף שדה אופציונלי `date_overrides: {academic_year: iso_date}` על הגדרת יעד בודדת, לחריגים חד-פעמיים שלא עוקבים אחרי כלל "אותו יום/חודש, +1 שנה". שימוש ראשון: `planning_40_sep` (יעד תכנון 40%) — בתשפ"ו נשאר 15/09/25 ללא שינוי, ובתשפ"ז בלבד נדרס ל-01/09/26 (במקום 15/09/26 שהיה יוצא מההזזה האוטומטית). `_shift_goal_date` בודק קודם אם קיים override לשנה המבוקשת לפני שהוא מחשב הזזה.
- **בדיקות שבוצעו:** אומת ישירות מול שתי שנות הלימודים (תשפ"ו/תשפ"ז) ששלושת התאריכים יצאו כמבוקש, וששאר 7 היעדים לא הושפעו; `python -m py_compile` תקין.

## 2026-07-05 — Feat: טאב "יעדים" — "מצב נוכחי" ל-4 יעדי דיווח (אחוז דיווח למודל תמרוץ)
- **Frontend (`GoalsTab.jsx`):** עבור 4 היעדים "יעד דיווח: ביצוע תקין של לפחות 10%/25%/70%/85% מהתקציב" (`reporting_valid_10_dec`, `reporting_valid_25_feb`, `reporting_valid_70_may`, `reporting_valid_85_jul`), "מצב נוכחי" מציג כעת את "אחוז דיווח למודל תמרוץ" (`overview.pct_tanuz`, 2 ספרות אחרי הנקודה) מהבדיקה האחרונה שבוצעה עבור התקציב הנבחר — אותו מקור נתונים ומנגנון (`latestBudgetOverview`) שכבר משמש עבור יעדי "אחוז תכנון". אם אין דוח (`pct_tanuz` הוא `null`) מוצג "—".

## 2026-07-05 — Feat: טאב "יעדים" — פורמט תאריך, הזזת שנה, ו"מצב נוכחי" ל-3 יעדי תכנון
- **Frontend (`formatGoalDate`):** תאריך היעד מוצג כעת תמיד בפורמט `DD/MM/YY` עם אפסים מובילים (למשל `05/09/25` ולא `5/9/25`).
- **Backend (`GOAL_DEFINITIONS`):** נוסף `_shift_goal_date(iso_date, academic_year)` — מזיז את שנת היעד קדימה (יום/חודש ללא שינוי) לפי המרחק בין `academic_year` הנבחר ל-`DEFAULT_ACADEMIC_YEAR` (`ACADEMIC_YEARS.index`), כך שבתשפ"ז כל 10 היעדים מוצגים ב-+1 שנה קלנדרית לעומת תשפ"ו (אומת: `2025-09-15`→`2026-09-15`, `2026-08-31`→`2027-08-31`). גם ה-label של "מועד אחרון... גפן תשפ"ו" מוחלף דינמית לשנה הנבחרת.
- **Frontend (`GoalsTab.jsx` — "מצב נוכחי"):** עבור 3 היעדים "יעד תכנון: לפחות 40%/70% (גפ"ן)/90%" (`planning_40_sep`, `planning_70_oct`, `planning_90_jan`), "מצב נוכחי" מציג כעת את "אחוז תכנון" (`overview.pct_plan`, 2 ספרות אחרי הנקודה) מהבדיקה **האחרונה** שבוצעה עבור התקציב הנבחר — נשלף מ-`logs` בצד הלקוח (`summary.tikhnun_result.budgets`, אותו שדה שמזין את "אחוז תכנון" ב-ResultsView), ללא שינוי בבקאנד (`list_logs` כבר ממיין `run_at DESC`, כך שהרשומה הראשונה תמיד היא האחרונה). שאר היעדים עדיין מציגים "—" בינתיים.

## 2026-07-05 — Style: טאב "יעדים" — עיצוב/רוחב זהים לטבלת "פגישות"
- **Frontend (`GoalsTab.jsx`):** הוחלף העיצוב הכהה (כותרת גרדיאנט כחול, `glass-card-dark`) בעיצוב הבהיר של `MeetingsTable` — כרטיס `glass-card` עם מסגרת דקה, כותרת עמודות `bg-slate-50/80` עם טקסט אפור עדין, שורות עם `hover` ו-`border-b` דק, אותם ריווחי `py-2.5`/`text-sm`. הכרטיס מקבל `minHeight: calc(100vh - 240px)` (זהה לפגישות) כדי שהחלק התחתון של הטבלה יתקרב לתחתית העמוד ופחות שורות ידרשו גלילה.
- **Frontend (`SchoolPage.jsx`):** הוסר הרוחב המורחב (`max-w-6xl`) מטאב "יעדים" — חוזר לאותו רוחב קונטיינר (`max-w-4xl`) כמו טאב "פגישות".

## 2026-07-05 — Feat: כפתור מזעור לחלון "השוואה בין בדיקות"
- **Frontend (`CompareResultsWindow.jsx`):** נוסף כפתור מזעור (−) ליד כפתור ה-X בפינה השמאלית העליונה; לחיצה עליו ממזערת את החלון למלבן קטן קבוע בפינה השמאלית התחתונה של המסך (עם שם בית הספר), ולחיצה על המלבן משחזרת את החלון **במיקום ובגודל המדויקים** שהיו לפני המזעור.
- **Fix נלווה:** ה-`useEffect` שאיפס את בורר התקציב הפעיל (ואת מצב המזעור) התבסס בטעות על כל שינוי של `compareData` — כולל עדכוני `patchCompare` (כשנתוני "מענים" מסיימים להיטען ברקע), מה שהיה גורם לחלון לצאת ממזעור/לאפס את הטאב הנבחר בכל פעם שה"מענים" מסיים להיטען. תוקן לתלות רק ב-`compareData._token` (מזהה ייחודי שנוצר בכל פתיחה חדשה של השוואה).
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות; הרמוניה סטטית (Playwright) — גרירת החלון, מזעור, ווידוא שהמלבן הממוזער מופיע בפינה השמאלית התחתונה עם הטקסט הנכון, ושחזור מחזיר את החלון למיקום המדויק שהיה לפני המזעור.

## 2026-07-05 — Feat: טאב "יעדים" — שלד ראשוני (טבלה + מיתוג חטיבה/תקציב + שמירת עמידה ביעד)
- **DB (Supabase):** נוצרה טבלת `school_goals` (id, school_id, gefen_account_id, budget_name, goal_key, academic_year, met, updated_at) עם UNIQUE(gefen_account_id, budget_name, goal_key, academic_year) לצורך upsert; GRANT ל-service_role, RLS מופעל, פוליסי `FOR ALL TO service_role`. מחליפה בפועל את טבלת `goals` הריקה שנוצרה כתשתית ב-2026-07-03 (שלא כללה gefen_account_id/budget_name/goal_key/met ולא חוברה מעולם ל-API/UI) — טבלת `goals` הישנה נשארה בינתיים ללא שימוש, ניתן למחוק אותה אם תרצה.
- **Backend (`schools_router.py`):** קבוע חדש `GOAL_DEFINITIONS` — רשימת הגדרות יעד קבועה (זהה לכל בתי הספר, נקבעת ע"י משרד החינוך); כרגע רשומת placeholder יחידה בלבד, תוחלף ברשימה המלאה כשתפורט. `GET /schools/{id}/goals?gefen_account_id=&academic_year=` מחזיר את היעדים הרלוונטיים לחטיבה (עם met מ-`school_goals`, ו-`current_status` עדיין None). `PATCH /schools/{id}/goals` — upsert לסטטוס "עמידה ביעד" (כן/לא/לא נקבע) עבור משתמש מחובר כלשהו (לא מוגבל למנהל).
- **Frontend:** קומפוננטה חדשה `GoalsTab.jsx` — טבלה בעיצוב זהה ל"דיווח חסר" (כותרת גרדיאנט כחול כהה, שורות מפוספסות), 4 עמודות: יעד / תאריך יעד / מצב נוכחי (כרגע "—") / עמידה ביעד (כפתורי כן/לא בעיצוב זהה לטוגל בהרשאות). בורר תקציב (pills) מופיע כשיש יותר מתקציב אחד. הטאב מחובר לבורר החטיבות (תיכון/חט"ב) הקיים בשורת הטאבים העליונה של `SchoolPage.jsx`, בדיוק כמו בטאב "בדיקות".
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות; `python -m py_compile` על ה-router תקין; אימות grants/RLS/policy ב-Supabase דרך ה-Management API. **לא בוצעה בדיקה ידנית בדפדפן** — מומלץ לפני העלאה. רשימת היעדים האמיתית וחישוב "מצב נוכחי" ימתינו לפירוט נוסף.
- **Fix (אחרי צילום מסך מהמשתמש):** כשאין יעדים תואמים לחטיבה הנבחרת, הטבלה עם 4 הכותרות (יעד/תאריך יעד/מצב נוכחי/עמידה ביעד) הייתה נעלמת לגמרי והוחלפה בקופסת "אין יעדים" — לא זה מה שהתבקש (השלד הקבוע של הטבלה צריך להיות תמיד גלוי). תוקן: הכותרות תמיד מוצגות; הודעת "אין יעדים להצגה עבור חטיבה זו" מוצגת כעת כשורה בתוך הטבלה עצמה, מתחת לכותרות.
- **Feat (רשימת היעדים האמיתית לתשפ"ו — 10 יעדים, מצילומי מסך שסופקו):** `GOAL_DEFINITIONS` עודכן מ-placeholder לרשימה המלאה (תכנון 40%/15.9, תכנון 70% גפ"ן/31.10, דיווח תקין 10%/15.12, תכנון 90%/31.1, דיווח תקין 25%/28.2, מועד מלגות/31.3, דיווח תקין 70%/31.5, מועד שינויי תוכנית עבודה/30.6, דיווח תקין 85%/15.7, מועד אסמכתאות/31.8), ממוינת כרונולוגית. כל היעדים חלים על **כל חטיבה** (`division: "all"`) ומוערכים **בנפרד לכל תקציב** (גפן/דוקאטי/תנופה וכו') — לא רק "גפן". "מצב נוכחי" עדיין None לכל השורות — ימולא בשלב הבא לפי הנחייתך.
- **Backend (שינוי מבנה):** `GET /schools/{id}/goals` מקבל כעת גם `budget_name` (חובה) בנוסף ל-`gefen_account_id`/`academic_year`, ומחזיר את סטטוס העמידה עבור אותו תקציב ספציפי בלבד (לא כל התקציבים בבת אחת).
- **Frontend (`GoalsTab.jsx`):** בורר התקציב (pills) נגזר עכשיו **מהיסטוריית הבדיקות בפועל** (`logs`, `summary.tikhnun_result.budgets`) — אותו מקור נתונים בדיוק שמזין את בורר התקציב ב"בדיקות" — במקום מרשימת ההגדרות. אם לבית הספר עדיין אין אף בדיקה שמורה, לא מוצגים pills וכל היעדים מוצגים תחת "תקציב כללי" (עד לבדיקה ראשונה). תאריך היעד מוצג בפורמט `DD.M.YY` התואם לשאר האתר.
- **Fix קריטי (אחרי בדיקה בדפדפן — הטבלה הייתה ריקה לגמרי, אפילו בלי שגיאה):** אותרה הסיבה: בתי ספר רבים (כמו זה שנבדק, סמל 616805) **אין להם בכלל שורה ב-`gefen_accounts`** (אף חטיבה מעולם לא נוצרה שם עבורם, גם אם יש להם היסטוריית בדיקות). העיצוב המקורי דרש `gefen_account_id` תקין כדי לבצע את הקריאה ל-API — וכשלא היה כזה, ה-`fetch` פשוט לא בוצע כלל (0 בקשות ברשת, בלי הודעת שגיאה). **תוקן מבנית:** טבלת `school_goals` שוחזרה (הייתה ריקה, 0 שורות בפועל — אין אובדן מידע) עם `division_type TEXT` (ערכי tikkon/beinayim/yesodi/other) **במקום** `gefen_account_id UUID` (FK), כך שהמערכת כבר לא תלויה בקיום שורת `gefen_accounts`. `GET`/`PATCH /schools/{id}/goals` מקבלים עכשיו `division_type` ישירות (לא `gefen_account_id`). אומת ישירות מול בית הספר הבעייתי (סמל 616805, שלב yesodi, ללא gefen_accounts) שה-API מחזיר את כל 10 היעדים כראוי.

## 2026-07-05 — Feat: השוואה בין בדיקות — מקטע "מענים" (שלב 3)
- **Backend (חדש, `backend/plan_roster.py`):** מודול עצמאי לחלוטין מ-`backend/logic/tikhnun_processor.py` ומ-`_finalize_tikhnun_metrics` (המזינים את "דיווח חסר" הקיים — **לא נגעתי בהם כלל**). `extract_plan_roster(plan_fpath)` מחזיר את **כלל** התוכניות (מענים) מקובץ תכנון, לפי תקציב, כולל תוכניות שדווחו במלואן (לעומת "דיווח חסר" שמציג רק פער דיווח). שימוש חוזר לגיטימי ב-`zihuy_core.normalize_budget_name` (פונקציית עזר טהורה) ובלוגיקת קיבוץ/זיהוי שכפולה (לא מיובאת) מהקוד הקיים.
- **Backend (`analyze_router.py`):** נוסף `POST /analyze/compare-plans` (`{newer_log_id, older_log_id}`) — מוריד את הקבצים השמורים של שתי הבדיקות (`stored_file_paths`, אותו pattern כמו `/add-file/{log_id}`), מזהה קבצי תכנון בעזרת `identify_file` הקיים, מריץ עליהם `extract_plan_roster`, ומחזיר per-budget diff: `added` (מענים חדשים), `removed` (הוסרו), `updated` (סכום תכנון השתנה, כולל `oldAmount`/`newAmount`/`diff`). מחזיר `missing: {newer, older}` אם לא נמצא קובץ תכנון שמור לאחת הבדיקות (למשל בדיקה ישנה מ-24 חודשים שקבציה כבר נמחקו).
- **Frontend (`CompareChecksContext`):** נוסף `patchCompare(token, updater)` — ממזג נתונים לתוך ההשוואה הפתוחה כרגע (עם token guard כדי שתשובה איטית מהשרת לא תדרוס השוואה חדשה יותר שהמשתמש כבר פתח).
- **Frontend (`SchoolPage` — `onCompare`):** מיד לאחר פתיחת חלון ההשוואה (עם "כללי" מוצג מיידית), נשלחת קריאת `POST /analyze/compare-plans` ברקע; בתשובה מתמזגים נתוני ה"מענים" לתוך אותם תקציבים (`mergeMengonimIntoBudgets`).
- **Frontend (`CompareChecksModal`):** בדיקות ישנות מ-24 חודשים (`isOlderThan24Months`) מסומנות עם ⚠ אפור + tooltip ברשימות הבחירה (עדיין ניתנות לבחירה — "כללי" תמיד עובד, רק "מענים" עשוי לא להיות זמין עבורן).
- **Frontend (`CompareResultsWindow`):** מקטע "מענים" מוחלף מ-placeholder למימוש מלא — ספינר בזמן טעינה, הודעת שגיאה, הודעת "קובץ תכנון לא זמין" לכל צד חסר, ושלוש רשימות צבעוניות (חדשים/עודכנו/הוסרו) לכל תקציב פעיל.
- **בדיקות שבוצעו:** `py_compile` תקין; `npx vite build` עובר ב-0 שגיאות; נבדק ידנית (סקריפט openpyxl סינתטי + קריאה ל-`extract_plan_roster`) שזיהוי מענה חדש/מעודכן/שהוסר עובד נכון; נבדקה הרמוניה סטטית (Playwright) לכל 4 מצבי התצוגה (טעינה/שגיאה/חסר/תוצאות) כולל מעבר בין כפתורי תקציב — כל המצבים מוצגים כצפוי.

## 2026-07-05 — Feat: השוואה בין בדיקות — חלון תוצאות צף + מקטע "כללי" (שלב 2)
- **Frontend (context חדש):** `CompareChecksContext.jsx` — קונטקסט חדש (בדומה ל-`MeetingRemindersContext`) שמנוהל מעל ה-router ב-`App.jsx`, כך שהחלון נשאר על המסך גם כשעוברים בין עמודים. `openCompare(data)` / `closeCompare()`.
- **Frontend (רכיב חדש):** `CompareResultsWindow.jsx` — חלון צף, נגרר (drag מה-header) וניתן לשינוי גודל (ידית resize בפינה), ממוקם ב-`AppLayout` לצד `MeetingRemindersOverlay` כך שהוא לא נסגר בניווט; נסגר רק בלחיצה על ה-X בפינה השמאלית העליונה. **אינו מודאלי** — לא חוסם אינטראקציה עם שאר האתר (בלי `useFocusTrap`/`aria-modal`, במכוון, כדי לאפשר עבודה מקבילה).
- **Frontend (SchoolPage — `buildCompareData`):** בעת לחיצה על "בצע השוואה" בחלונית הבחירה, נבנה אובייקט השוואה **כולו מנתונים שכבר טעונים בצד הלקוח** (משורות ה-`summary.tikhnun_result.budgets` של שתי הבדיקות שנבחרו) — **ללא קריאה נוספת לשרת/Storage** בשלב זה. מוצג לכל תקציב (כפתורי pill אם יש יותר מאחד, בדיוק כמו ב"דיווח חסר"/"אסמכתאות ללא PDF" בתוך הבדיקה עצמה):
  - **כללי** — 4 שורות (הראשונה מותנית): "תקציב" (מוצג רק אם השתנה, ניסוח גדל/קטן + הפרש בש"ח), "גובה התכנון" (תמיד, הוספה/ירידה/אין שינוי בש"ח), "אחוז דיווח כולל" ו"אחוז דיווח למודל תמרוץ" (תמיד, אותו רעיון באחוזים).
  - **מענים** — placeholder זמני ("ההשוואה עבור מענים תתווסף בהמשך") — ממתין לפירוט הלוגיקה (שלב הבא, כולל שליפת קובצי תכנון מ-Storage).
- **הערה חשובה שסוכמה איתך ועדיין לא מומשה (תרשם לשלב הבא):** קובע האקסל המיוצא ייצטרך להישמר לצמיתות ב-`check_logs` (מיגרציית DB, כרגע נשמר רק זמנית ב-`run_states` ל-120 יום), וההשוואה תוגבל לבדיקות שכל אחת מהן בטווח 24 החודשים האחרונים (עדיין לא נאכף ב-UI — ייאכף כשתיבנה "מענים" שדורשת גישה ל-Storage).

## 2026-07-03 — Feat: סינון לפי שנת לימודים (תשפ"ו/תשפ"ז) בטאבי פגישות/יעדים/בדיקות
- **DB (Supabase):** נוספה עמודה `academic_year TEXT NOT NULL DEFAULT 'תשפ"ו'` לטבלאות `meetings` ו-`check_logs` (רשומות קיימות הושלמו אוטומטית לתשפ"ו); נוצרה טבלת `goals` חדשה (`id`, `school_id`, `academic_year`, `created_at`) — **תשתית בלבד**, ללא API/UI (טאב "יעדים" עדיין placeholder "בקרוב"), עם GRANT ל-service_role ו-RLS מופעל.
- **Backend:** נוספו קבועים `backend/academic_years.py` (`ACADEMIC_YEARS`, `DEFAULT_ACADEMIC_YEAR`). כל endpoints הפגישות (`list_meetings`, `list_all_meetings` בניהול, `list_my_meetings` באזור אישי, `create_meeting`) וה-בדיקות (`list_logs`) תומכים כעת בפרמטר/שדה `academic_year` לסינון ולשיוך. עריכת פגישה קיימת (`update_meeting`) לא משנה את השנה אלא אם נשלחה במפורש. זרימת ההעלאה/סיווג/ניתוח (`analyze_router.py`: `/upload`, `/save-for-account`, `/add-file`, `/classify`, `/retry-finance`) מעבירה `academic_year` דרך אותו מנגנון `_school_ctx` הקיים ל-`school_id`/`gefen_account_id`, ונשמרת ב-`check_logs` רק בעת יצירת בדיקה חדשה (לא נדרסת בעדכון בדיקה קיימת).
- **Frontend:** קומפוננטה משותפת חדשה `AcademicYearSelector.jsx` (תפריט נפתח, בסגנון תואם לשאר הבוררים באתר) וקובץ קבועים `frontend/src/constants/academicYears.js`.
  - **SchoolPage:** בורר שנה נוסף בקצה השמאלי ביותר של שורת הטאבים, מעל כל שלושת הטאבים "פגישות"/"יעדים"/"בדיקות"; הבחירה נשמרת ב-URL (`?year=...`) כך שרענון/שיתוף/ניווט קדימה-אחורה שומרים על אותה שנה; טעינת פגישות ובדיקות, וכן יצירת פגישה/בדיקה חדשה, כוללות את השנה הנבחרת.
  - **AdminMeetingsTab ("ניהול") ו-PersonalMeetingsTab ("אזור אישי"):** נוסף אותו בורר שנה בשורת הכותרת של כל דף (ברירת מחדל תשפ"ו בכל טעינה, ללא שמירה ב-URL — אלו מסכים חוצי-בתי-ספר); סינון הרשימה ויצירת פגישה חדשה כוללים את השנה הנבחרת שם.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות; קבצי ה-backend שעברו שינוי נטענים (import) ללא שגיאות; אימות סכימת Supabase (עמודות/GRANT/RLS) בוצע דרך ה-Management API. **לא בוצעה בדיקה ידנית מלאה בדפדפן** — מומלץ לבצע לפני העלאה לאתר.

## 2026-07-03 — Feat: חלונית בחירת 2 בדיקות להשוואה (שלב 1 — בחירה בלבד, ללא לוגיקת השוואה)
- **Frontend (SchoolPage):** כפתור "השוואה בין בדיקות" מחובר עכשיו — פותח `CompareChecksModal` חדשה. החלונית מציגה שני בוררים זה לצד זה (RTL: ימין = "בדיקה חדשה יותר", שמאל = "בדיקה ישנה יותר"); כל בורר פותח רשימה נפתחת עם כל הבדיקות בטווח הטאב הנוכחי (`filteredLogs` — מכבד את הפילטור לפי חטיבה בבתי"ס שש-שנתיים), מוצגות בפורמט `formatDateTime` (זהה לעמודת "זמן הבדיקה"); ברירת המחדל של הצד הימני היא הבדיקה האחרונה שבוצעה (`filteredLogs[0]`), הצד השמאלי ריק עד לבחירה; לא ניתן לבחור את אותה בדיקה משני הצדדים (מנוטרל ברשימה). כפתור "בצע השוואה" מרכזי מתחת לשני הבוררים — מנוטרל (grey-out, `disabled`) עד שנבחרו שתי בדיקות שונות, ואז הופך פעיל.
- **הערה:** לחיצה על "בצע השוואה" כרגע רק סוגרת את החלונית (`TODO` בקוד) — לוגיקת ההשוואה בפועל ואופן הצגת התוצאות טרם הוגדרו, ימתינו להנחיה נוספת.

## 2026-07-03 — Feat: שעה בטבלת היסטוריית בדיקות (SchoolPage)
- **Frontend (SchoolPage):** בטבלת "בדיקות" (היסטוריית ריצות), כותרת העמודה "תאריך" שונתה ל"זמן הבדיקה"; נוספה פונקציית `formatDateTime` המציגה תאריך+שעה בפורמט `DD.MM.YY - HH:MM` (למשל `01.08.26 - 13:42`); הוחלה על שורת הבדיקה הרצה כעת (pending) ועל כל שורות ההיסטוריה. `formatDate` (תאריך בלבד) נשאר ללא שינוי לשימוש בשאר העמוד (כגון תאריכי פגישות).

## 2026-07-02 — Feat: לשונית "פגישות" חדשה באזור הניהול
- **Refactor (ללא שינוי התנהגות):** חילוץ רכיבי הפגישות המשותפים מ-`SchoolPage.jsx` לתיקייה `frontend/src/components/meetings/` (`MeetingRow`, `MeetingsTable`, `AdvisorCell`, `MeetingTypeSelect`, `DeleteMeetingModal`, `NoParticipantsModal`, `DatePickerPopover`, `ParticipantsSelector`, `NotesModal`, `TimeInput`, `constants.js`); `SchoolPage.jsx` עבר ל-import מהמיקום המשותף. גם `AdvisorSearch` אוחד לרכיב משותף `frontend/src/components/AdvisorSearch.jsx` (בשימוש כרגע רק ב-`SchoolPage.jsx` — הגרסה ב-`AdminPage.jsx` נשארה כפי שהיא, כי יש לה חתימת פונקציה שונה (`onAdd(schoolId, id)` לעומת `onAdd(id)`) ולכן לא הוחלפה, כדי לא ליצור סיכון רגרסיה בזרימת שיוך היועצים הקיימת).
- **Backend:** נוספו `GET /schools/meetings/all` (רשימת פגישות מרוכזת לכל הארגון, עם סינון לפי סטטוס/טווח תאריכים/יועץ/בית ספר/חיפוש טקסט, owner+manager בלבד) ו-`PATCH /schools/meetings/{meeting_id}/reassign-school` (שיוך פגישה קיימת לבית ספר אחר).
- **Frontend:** לשונית "פגישות" חדשה באזור הניהול (מיד משמאל ל"משתמשים"), עמוד `AdminMeetingsTab.jsx` חדש — טבלת פגישות מרוכזת לכל הארגון עם עמודת "שם מוסד" נוספת (בין סטטוס להתחלה, עם בורר בית ספר מסוג checkbox+אישור), חיפוש חופשי, פאנל סינון מתקדם (טווח תאריכים/סטטוס/יועץ), פעולות מרובות (שיוך יועץ מרוכז + מחיקה מרוכזת), וכפתורי ייצוא Excel/PDF לנתונים המוצגים. ברירת מחדל בטעינה: פגישות `status=scheduled` עם `meeting_date<=היום`. תזכורות ופופאפים קיימים (Sidebar polling) עובדים אוטומטית ללא שינוי נוסף.
- **בדיקות שבוצעו:** `npx vite build` עובר ב-0 שגיאות; `python -m py_compile` על ה-router תקין; ה-backend נטען (import) ללא שגיאות; שרת ה-dev של Vite הגיש את כל הקבצים החדשים/שעברו חילוץ ללא שגיאות טרנספורמציה. **לא בוצעה בדיקה ידנית מלאה בדפדפן** (כניסה עם משתמש אמיתי, קליק על כל שדה, אימות סנכרון דו-כיווני בפועל מול Supabase) — מומלץ לבצע לפני העלאה לאתר.

## 2026-07-02 — Feat: ניווט לכרטיס בית ספר + שמירת סינון בלוח פגישות מנהל
- **Frontend (MeetingRow):** לחיצה על תא "שם מוסד" מנווטת ל-`/school/:schoolId`; hover בצבע כחול; אם אין `school_id` (פגישה לא משויכת) — מציג `+` שפותח את ה-picker
- **Frontend (AdminMeetingsTab):** כל מצב הסינון (פילטרים שרת, פילטרים client-side, טקסט חיפוש, נראות פאנל מתקדם) נשמר ב-`sessionStorage` תחת מפתח `admin_meetings_ui_state`; בעת חזרה לטאב — המצב משוחזר אוטומטית

## 2026-07-02 — Feat: שיפור UX לוח פגישות מנהל — פאנל סינון קבוע + הרחבת טבלה
- **Frontend (AdminMeetingsTab):** פאנל הסינון (מתאריך, עד תאריך, סטטוס, יועץ מבצע) הפך קבוע ותמיד מוצג; נוספו שדות "שם מוסד" ו"סמל מוסד" (סינון client-side); כפתור "סינון מתקדם" עכשיו פותח פאנל משני עם שדה "עיר" (client-side); `clearFilters` מנקה גם את הפילטרים המקומיים
- **Frontend (AdminPage):** הטבלה הורחבה ב-`-mx-24` (96px מכל צד ≈ 20%) כדי לנצל את השטח המת משני צדדי הקונטיינר

## 2026-07-03 — Feat: שליחת תזכורת ידנית לעדכון סטטוס פגישה (מנהל/בעלים)
- **DB:** נוצרה טבלת `meeting_status_reminders` (id, meeting_id, school_id, recipient_id, sent_by, sent_at, shown_at) — עם GRANT לservice_role, RLS מופעל, ופוליסי
- **Backend (`schools_router.py`):**
  - `POST /schools/meetings/{id}/send-status-reminder?force=` — שולח תזכורת ליועצי הפגישה (או יועצי בית-הספר אם אין); בודק כפילות ב-72 שעות; מחזיר `already_sent` + `last_sent_at` אם קיימת
  - `GET /schools/meetings/pending-status-reminders` — מחזיר תזכורות ממתינות (unshown, <72h, meeting status="scheduled") עבור המשתמש הנוכחי
  - `PATCH /schools/meetings/status-reminders/{id}/mark-shown` — מסמן תזכורת כ"הוצגה"
- **Frontend (MeetingRow):** כפתור "שלח תזכורת לעדכון סטטוס" בתפריט ה-3-נקודות של שורה, מוצג רק כאשר הפגישה מתאימה: status=scheduled + תאריך עבר + שעת סיום עברה
- **Frontend (MeetingsBulkActionBar):** כפתור ⋮ חדש בצד שמאל; תפריט עם "שלח תזכורת לעדכון סטטוס" — שולח בbulk לכל הפגישות הנבחרות המתאימות
- **Frontend (AdminMeetingsTab + SchoolPage):** toast זמני 3 שניות בצד שמאל למטה עם פרטי השליחה; מודל "תזכורת כבר נשלחה ב-DD.MM.YY בשעה HH:MM — האם לשלוח חדשה?" עם כפתורי אישור/ביטול
- **Frontend (Sidebar):** polling כולל בדיקת `pending-status-reminders` → מפעיל `addStatusReminder()` לכל תזכורת ממתינה ומסמן כ-shown מיד; מונע כפילות עם ה-deduplication הקיים בcontexxt

## 2026-07-03 — Feat: טאב "פגישות" באזור אישי (PersonalMeetingsTab)
- **Backend:** נוסף `GET /schools/meetings/my` (כל תפקיד) — מסנן לפי `advisor_ids cs [user.id]`, תומך ב-`status`/`date_from`/`date_to`; מעשיר עם `school_name`, `school_symbol`, `school_city`, `school_district`, `advisor_profiles`
- **Frontend (MeetingsTable + MeetingRow):** נוסף prop `hideAdvisorColumn` — מסתיר את עמודת "יועץ מבצע" מהכותרת ומשורות הטבלה
- **Frontend:** נוצר `PersonalMeetingsTab.jsx` — רכיב חדש המשתמש ב-MeetingsTable, מציג רק פגישות המשתמש הנוכחי, ללא שדה סינון/עמודת "יועץ מבצע"; ברירת מחדל: היום; תומך בהוספה, עריכה, מחיקה, סינון מתקדם (עיר/מחוז), סימון מרובה, ייצוא Excel/PDF, שליחת תזכורת לעדכון סטטוס
- **Frontend (ProfilePage):** מקטע "פגישות" החליף את הplaceholder "בקרוב" ב-`<PersonalMeetingsTab />`; הטופס של אזור אישי מעביר `userId` ו-`userRole` כ-props; `canDeleteMeetings=true` רק למנהל/בעלים

## 2026-07-03 — Feat: שורת פעולות מרובות בפגישות — עיצוב זהה לדשבורד + ייצוא Excel/PDF
- **Frontend (MeetingsBulkActionBar):** עיצוב הכפתורים שונה להיות זהה לדשבורד — PDF (כתום), EXCEL (ירוק), "ביטול סימון" (border), "מחק" (אדום); סדר הכפתורים זהה; כפתורי ה-count בצד ימין והכפתורים בצד שמאל
- **Frontend (AdminMeetingsTab):** נוספו פונקציות `exportSelectedToExcel` (XLSX client-side) ו-`exportSelectedToPdf` (POST ל-`/schools/export-pdf`) — מייצאות את הפגישות הסומנות עם עמודות: שם מוסד, סמל, תאריך, שעה, סטטוס, סוג פגישה, יועצים, עיר

## 2026-07-03 — Fix: כפתור 3 נקודות בפאנל סינון פגישות — מיקום + תפריט dropdown
- **Frontend (AdminMeetingsTab):** כפתור 3 הנקודות הועבר לפינה העליונה-שמאלית של תיבת הסינון (`absolute left-3 top-3`); לחיצה עליו פותחת תפריט dropdown עם האפשרות "סמן פגישות" (במקום הצגה/הסתרה ישירה של תיבות הסימון); "נקה סינון" הועבר לתוך שורת שדות הסינון (אותו קו אופקי); "בטל בחירה" מסתיר גם את תיבות הסימון (`setSelectable(false)`) בנוסף לניקוי הבחירה

## 2026-07-02 — Feat: תפריט 3 נקודות למחיקת משתמש + badge ספירת הרשאות מותאמות
- **Backend:** נוסף `GET /schools/permissions/overrides/counts` — מחזיר `{ user_id: count }` לכל המשתמשים בארגון (manager+ בלבד)
- **Frontend:** כפתור "מחק" הוחלף בתפריט 3 נקודות אופקיות שנפתח ל-"מחק" (מוצג רק לבעלים / מנהל עם `canDeleteUsers`); תפריט נסגר בלחיצה מחוץ לו; לחיצה על "מחק" פותחת אישור מחיקה כרגיל
- **Frontend:** badge כחול עם ספירת הרשאות מותאמות אישית מוצג משמאל לכפתור "הרשאות" לכל משתמש שיש לו overrides; ספירה מתעדכנת בזמן אמת לאחר כל שמירה

## 2026-07-02 — Feat: שיפור UX חלונית הרשאות אישיות למשתמש
- **Frontend:** `UserPermissionsModal` — שינויים נצברים מקומית ונשלחים לשרת רק בלחיצה על "שמור שינויים" (כפתור "ביטול" סוגר בלי שמירה); שורות עם התאמה אישית מסומנות ברקע כחול (`bg-blue-100`); כפתורי כן/לא משתנים בהתאם (מילוי מלא לשורות מותאמות, מילוי קלוש לברירת מחדל); סינון הרשאות לא-רלוונטיות ליועץ; כפתור "איפוס הכל לברירת מחדל" (מקומי, דורש שמירה)

## 2026-06-29 — Fix: עיצוב חלונית הרשאות אישיות — 2 כפתורים + טקסט שחור
- **Frontend:** `UserPermissionsModal` — החלפת 3 כפתורי pill (ברירת מחדל/מורשה/חסום) ב-2 כפתורי כן/לא בסגנון PermToggle (לא=אדום, כן=ירוק, ניטרלי=אפור); לחיצה על כפתור פעיל מאפסת לברירת מחדל; כל הטקסט שאינו כפתור עודכן ל-`text-black`
