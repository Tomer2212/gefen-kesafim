# Dev Log

## 2026-09-09 — מניעת סמלי-מוסד כפולים בכל 5 נתיבי יצירת בית ספר + אחריותיות מחיקה רכה
- **ממצא**: אין שום אילוץ ייחודיות ב-DB על `schools.symbol`, ו-3 מתוך 5 המקומות שיוצרים בית
  ספר (ייבוא בתי ספר מאקסל, טופס ההוספה המקוצר בייבוא פגישות, ה-endpoint לרשומות היסטוריות)
  יצרו כפילות אמיתית בלי שום בדיקה. גם שני מסכי ההוספה הבודדת שכן בדקו — אחד מהם
  (`AddSchoolPage.jsx`) בדק רק מול בתי ספר פעילים, לא מול בתי ספר בסל מחזור.
- **תיקון מרכזי**: פונקציית עזר חדשה `_find_existing_school_by_symbol` ב-`create_school()` —
  מחזירה `409 duplicate_symbol` (עם פרטי בית הספר הקיים) לפני כל insert. מקור אמת יחיד לכל
  הקוראים, הווה או עתידי.
- **הוספה בודדת** (`AdminPage.jsx` + `AddSchoolPage.jsx`): תפיסת 409 → הודעה חוסמת עם שם בית
  הספר הקיים (+ "(בסל מחזור)" אם רלוונטי) וקישור ישיר לכרטיס שלו. `AddSchoolPage.jsx` גם תוקן
  לטעון את רשימת הסמלים הקיימת עם `include_deleted=true` (לא רק פעילים כמו קודם).
- **ייבוא בתי ספר מאקסל**: `parseImportRow` מזהה עכשיו גם "סמל כבר קיים" כבעיה חדשה
  (`duplicate_symbol`) בתוך חלונית "בעיות" הקיימת (`SchoolImportProblemsModal.jsx`) — כרטיס
  לכל שורה מתנגשת עם כפתור "עדכן את בית הספר הקיים" (מריץ `PUT` במקום `POST`, דורס כל שדה
  שסופק בקובץ) + כפתורי "עדכן את הכל"/"דלג על הכל" מרוכזים.
- **טופס ההוספה המקוצר בייבוא פגישות** (`QuickAddSchoolForm`): תפיסת 409 → מציע לקשר את
  הפגישה לבית הספר הקיים במקום ליצור כפילות.
- **Endpoint רשומות היסטוריות** (`create_historical_schools_for_import`): בודק סמל קיים לפני
  יצירה — משתמש ברשומה הקיימת (פעילה או בסל מחזור) במקום ליצור כפילה; ייבוא כפול של אותו
  קובץ כבר לא יוצר רשומות נוספות.
- **אחריותיות מחיקה רכה**: נוספו עמודות `deleted_by`/`restored_by` ל-`schools` (מיגרציה
  הורצה ואומתה). מולאות בשלושת נתיבי המחיקה/שחזור הקיימים (`delete_school`, `restore_school`,
  אישור בקשת מחיקה ב-`review_update_request`). סוגר פער אמיתי: עד היום לא הייתה שום דרך
  לגלות בדיעבד מי מחק בית ספר (רק התראה חולפת בזמן אמת, בלי תיעוד קבוע) — נמצא תוך כדי
  חקירה, לא היה קיים קודם.
- `npx vite build` עבר ב-0 שגיאות; ה-backend נבדק ב-import מלא

## 2026-09-09 — תיקון חמור: מחיקת בית ספר הסתירה את היסטוריית הפגישות שלו + פתרונות ל-school_not_found בייבוא עבר
- **באג חמור שתוקן** (נמצא תוך כדי בדיקת הבקשה למטה, לא קשור אליה במקור): `list_all_meetings`
  (`GET /schools/meetings/all`, "ניהול → פגישות") ו-`list_my_meetings` ("אזור אישי") בנו את
  רשימת בתי הספר הרלוונטית מסוננת ל-`status="active"` בלבד, ואז שלפו פגישות רק מתוך אותה
  רשימה — כך שמחיקת בית ספר (מחיקה רכה, "סל מחזור") **הסתירה לגמרי מהתצוגה גם את כל היסטוריית
  הפגישות שתועדה איתו**, למרות שהנתונים עצמם נשארו שלמים ב-DB. הוסר סינון הסטטוס משתי
  השאילתות — `GET /schools/` (רשימת הלקוחות הפעילים, דשבורד וכו') לא נגע, נשאר מסונן כרגיל
- **"בית ספר לא נמצא" בתיעוד פגישות עבר** — endpoint חדש `POST /schools/meetings/import/
  historical-schools`: לכל שורה עם "בית ספר לא נמצא" שנבחרה, יוצר רשומת בית ספר מינימלית
  (שם+סמל בלבד, מתאם פגישות פלייסהולדר) **ומיד ממחק אותה מחיקה רכה** — מתנהגת בדיוק כמו
  בית ספר אמיתי שהארגון מחק בעבר: לא ברשימת הלקוחות הפעילים, אבל הפגישות שלו כן מופיעות
  בהיסטוריה (תודות לתיקון למעלה), וניתן לשחזר אותה ללקוח פעיל דרך כפתור השחזור הקיים
  (בעלים בלבד) אם יתחדש הקשר
- `MeetingImportProblemsModal.jsx`: כפתור מרוכז "רשום את כל בתי הספר הלא-מזוהים (N)" —
  פעולה אחת שיוצרת רשומה מינימלית אחת לכל סמל ייחודי (לא כפילות לשורות עם אותו סמל) ופותרת
  את כל השורות התואמות בבת אחת, בלי טופס. כפתור נוסף "דלג על כל בתי הספר הלא-מזוהים (N)"
  שמסמן את כולן כ"הוסר" בפעולה אחת. טופס ההוספה המלאה הקיים (`QuickAddSchoolForm`, פר-שורה)
  נשאר ללא שינוי כאפשרות שלישית
- **ייצוא לאקסל**: כפתור חדש בחלונית "בעיות" שמוציא את כל השורות עם בעיה לא-פתורה (מכל
  הסוגים) לקובץ Excel — לטיפול מחוץ למערכת ואז ייבוא מחדש
- **שש-שנתי בלי היקף פגישה**: `stage_scope_ambiguous` הפכה להיות רלוונטית רק לפגישות עתידיות
  (`effective_mode == "future"`) — בתיעוד פגישות עבר בית ספר שש-שנתי בלי היקף מצוין מיובא
  בלי בעיה חוסמת
- `npx vite build` עבר ב-0 שגיאות; ה-backend נבדק ב-import מלא

## 2026-09-09 — "מתאריך" בטאבי פגישות: ברירת מחדל לפי שנת הלימודים הנבחרת
- **הבעיה**: בורר שנת הלימודים ("ניהול → פגישות" ו"אזור אישי") נראה כאילו "לא עושה כלום" —
  כי "עד תאריך" קבוע כברירת מחדל להיום, ופגישות עתידיות ששייכות לשנה הנבחרת (למשל תשפ"ז)
  נחסמות על ידי אותה חסימה בכל שנה, ללא קשר לבחירה בבורר.
- **התיקון (לפי בקשה מפורשת)**: "מתאריך" מקבל כעת ברירת מחדל דינמית — תמיד 1 בספטמבר של
  שנת הלימודים **הנבחרת בפועל** בבורר (לא קבוע ליום ההיום). מעבר בין שנים בבורר מזיז אוטומטית
  את "מתאריך" לתחילת השנה החדשה (1.9.25 לתשפ"ו, 1.9.26 לתשפ"ז וכו'). "עד תאריך" **נשאר ללא
  שינוי** (תמיד היום) — לפי בקשה מפורשת של המשתמש שלא לגעת בו.
- `frontend/src/constants/academicYears.js`: נוספו `ACADEMIC_YEAR_START_GREGORIAN` (מראה
  מדויק של המקביל ב-backend) ופונקציית עזר `getAcademicYearStartDate(academicYear)`.
- `AdminMeetingsTab.jsx` + `PersonalMeetingsTab.jsx` (אותו דפוס בשני הקבצים): `DEFAULT_FILTERS`
  הקבוע הפך לפונקציה `buildDefaultFilters(academicYear)`; `handleYearChange`/מעבר שנה מעדכן
  גם את `filters.date_from`; `clearFilters` מחזיר לברירת המחדל של **השנה הנוכחית** (לא ערך
  קבוע ישן); תוקן גם חישוב ההדגשה הכחולה של שדה "מתאריך" (מהפיצ'ר הקודם) שהשוותה מול `null`/
  `TODAY` הישנים — עכשיו משווה מול ברירת המחדל הדינמית, כדי שההדגשה תישאר משמעותית.
- `npx vite build` עבר ב-0 שגיאות (כולל תיקון הפניה שוכחת ל-`DEFAULT_FILTERS` הישן שנשארה
  ב-`applyAgentFilters` של AdminMeetingsTab.jsx).
