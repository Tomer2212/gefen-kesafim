# Dev Log

## 2026-09-10 — בורר עמודות בייבוא: פתיחה כלפי מעלה כשאין מקום למטה
- `ImportMappingModal.jsx` — הפופאובר של בחירת העמודה נפתח כלפי מעלה (anchored ל-`bottom`) כשיש פחות מ-240px מתחת לכפתור ויותר מקום מעליו (שדות בתחתית המודל כמו "הערות"). גובה הפופאובר מוגבל למקום הפנוי בפועל והרשימה גוללת בתוכו — לא נחתך בשום כיוון.
- `npx vite build` — 0 שגיאות.

## 2026-09-10 — תיקון: שורת כותרות לא מוקפאת בטבלת "ניהול → גבייה"
- `AdminCollectionTab.jsx` — לתיבת הגלילה של הטבלה היה רק `flex-1` בלי תקרת גובה, אז כשהיו הרבה שורות הכרטיס גדל מעבר לגובה המסך והעמוד כולו גלל במקום הטבלה — וה-`<thead sticky>` נגלל איתו. נוסף `min-h-0` + `maxHeight: calc(100vh - 260px)` לתיבת הגלילה (זהה ל-`minHeight` של הכרטיס), כך שהגלילה מתרחשת בתוך הטבלה ושורת הכותרות + העמודה הראשונה נשארות מוקפאות — כמו בשאר הטבלאות.
- `npx vite build` — 0 שגיאות.
