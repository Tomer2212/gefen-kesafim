# Dev Log

## 2026-09-23 — תיקון קריטי: ניתוק חיבור נשאר חסום לצמיתות
- הסיבה: תג הדפדפן (`device_id`) לא התחלף אחרי ניתוק מרוחק, אז אותו תג נשלח שוב בכל בקשה — כולל בניסיון ההתחברות/רישום הבא — ונחסם שוב ושוב על ידי אותה בדיקה, בלי אפשרות לצאת מהמצב מאותו דפדפן (אומת מול ה-DB החי: מייל+סיסמה נכונים עברו, אבל הרישום מחדש נחסם תמיד)
- תוקן: כשמתקבלת שגיאת "החיבור נותק מרחוק", הדפדפן מוחק את התג הישן מ-localStorage (`clearDeviceId` חדש ב-`deviceSession.js`) לפני שהוא שולח לדף ההתחברות — כך שההתחברות הבאה נרשמת כחיבור חדש לגמרי, ולא נשארת חסומה לצמיתות