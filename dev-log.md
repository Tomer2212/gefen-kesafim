# Dev Log

## 2026-06-21 — תיקון לינק הזמנה + מסך ברוכים הבאים
- **באג תוקן:** לינק הזמנה שנפתח פעם אחת (מבלי להגדיר סיסמה) הציג "קישור לא תקין" בכניסה חוזרת, אף שה-session עדיין קיים בדפדפן. כעת הקוד בודק קיום session לפני הצגת שגיאת "פג תוקף"
- נוסף מסך "ברוכים הבאים!" לאחר השלמת הרישום (flow הזמנה בלבד): V ירוק + כפתור "כניסה למערכת" המעביר לדף ההתחברות
- לאחר הגדרת הסיסמה מתבצע signOut אוטומטי כדי שהמשתמש יכנס מחדש בצורה נקייה

## 2026-06-21 — עיצוב טאבים בדף ניהול
- הטאבים "בתי ספר" / "משתמשים" בדף ניהול עודכנו לסגנון קו תחתון זהה לטאבים בכרטיס בית הספר (border-b-2, text-blue-600)

## 2026-06-21 — מיון רשימת משתמשים בדף ניהול
- רשימת המשתמשים בטאב "משתמשים" ממוינת כעת לפי תפקיד: בעלים → מנהל → יועץ

## 2026-06-21 — עמוד אזור אישי
- נוסף `ProfilePage.jsx` — עמוד ייעודי עם שלושה אזורים: פרטים אישיים, פגישות (בקרוב), משימות (בקרוב)
- פרטים אישיים מוצגים אופקית: שם מלא (עם עריכה), אימייל (קריאה בלבד), עדכון סיסמה
- הסיידבר: "אזור אישי" מנווט כעת ל-`/profile` במקום לפתוח חלונית; הוסרה ה-`ProfileModal` וה-`showProfile` state
- נוסף route `/profile` ב-`App.jsx`

## 2026-06-21 — תיקון כפתור "שלח מחדש" להזמנת יועץ
- הוסף endpoint ייעודי `POST /users/{user_id}/resend-invite` בבאקאנד עם retry loop תקין (Invariant #5)
- ה-endpoint מחפש את האימייל מה-profiles, מוודא שהמשתמש עדיין pending, ומפעיל `invite_user_by_email` מחדש
- פרונטאנד: `resendInvite` קורא עכשיו ל-endpoint החדש (במקום `/users/invite` הכללי שהיה נכשל עם "User already registered")
- נוסף טיפול בשגיאות + הודעת הצלחה/שגיאה inline ליד הכפתור (נעלמת אחרי 4 שניות)

## 2026-06-21 — כפתור "הוסף בית ספר" בדף הבית פותח ישירות את הטופס
- כפתורי "הוסף בית ספר" ו"הוסף בית ספר ראשון" ב-DashboardPage מנווטים כעת ל-AdminPage עם `state: { openNewSchool: true }`
- AdminPage מזהה את הסטייט ופותח אוטומטית את טופס הוספת בית ספר חדש — המשתמש לא צריך ללחוץ פעמיים

## 2026-06-21 — שמירת מצב סינון בדף הבית + כפתור ניקוי בפאנל
- שמירת `searchQuery`, `filters`, `queries`, `showFilters` ב-`sessionStorage` (per user) — מצב הסינון נשמר גם לאחר ניווט לדף אחר וחזרה
- הוספת כפתור "ניקוי סינון" בתחתית פאנל הסינון המתקדם (צד שמאל), שמנקה את כל שדות הסינון בבת אחת
