# Dev Log

## 2026-09-10 — בורר עמודות בייבוא: חיפוש חופשי + תצוגת 2 עמודות
- `frontend/src/components/ImportMappingModal.jsx` — `ColumnSelect` הוחלף מ-`<select>` native לרכיב מותאם: כפתור + פופאובר (portal, `position: fixed`) עם תיבת חיפוש חופשי שמצמצמת את הרשימה, וכל אפשרות מוצגת כ-2 עמודות עם גבולות ברורים ("כותרת בקובץ" | "ערך לדוגמה"). סגירה ב-Escape / לחיצה בחוץ / גלילה של הרקע (לא סגירה בגלילה בתוך הרשימה עצמה).
- חל על כל זרימות הייבוא המרוכז (פגישות / בתי ספר / משתמשים) — רכיב משותף.
- `npx vite build` — 0 שגיאות.
