# Dev Log

## 2026-09-23 — זיהוי מוקדם של קובץ כספים2000 שנשמר כ-Excel בינארי (ולא TSV)
- רקע: קובץ כספים2000 שנערך/נשמר דרך Excel (במקום להישאר בפורמט הגולמי TSV) עדיין מזוהה כ-`kesafim2000` ע"י `identify_file`, אך `load_kesafim` לא יודע לקרוא אותו (`UnicodeDecodeError`) — תקלה קיימת-מראש. עד עכשיו הבדיקה המקדימה שלנו הייתה "נכשלת בעדינות" על קובץ כזה ומציגה בטעות התרעת "קובץ ריק מאסמכתאות" (כי לא הצליחה לקרוא תאריכים), במקום הבעיה האמיתית — מה שעלול היה לבלבל משתמש שיחשוב שהתעלמות מההתרעה תוביל לבדיקה תקינה.
- `backend/routers/analyze_router.py`: `_kesafim_is_unreadable_binary` — מזהה ספציפית את מקרה ה-`UnicodeDecodeError` הזה (לפני בדיקת סוג תקציב/שנה), ומחזירה `status: "unreadable_raw_file"` עם **אותה הודעה בדיוק** שכבר מוצגת היום כשהבדיקה האמיתית נכשלת על קובץ כזה (`_KESAFIM_UNREADABLE_MESSAGE`, ששימשה גם לאיחוד הניסוח בטיפול השגיאה הקיים ב-`_process`, כדי שהניסוח לא יתפצל לשני מקורות).
- `frontend/src/components/YearMismatchModal.jsx`: השורה מוצגת תחת אותה כותרת "קובץ לא תקין" יחד עם `unscoped_budget`, חוסמת לגמרי את "בצע בדיקה" (רק "החלף קובץ"/"הסר" אפשריים).
- אומת מול קובץ הבדיקה האמיתי של המשתמש (עותק Excel בינארי אמיתי) — עכשיו מתקבל `unreadable_raw_file` במקום "ריק", וקובץ TSV תקין ממשיך לעבוד בלי שינוי.

## 2026-09-22 — כפתור "הסר קובץ" בחלון חוסר ההתאמה + תיקון צבע כותרת
- `frontend/src/components/YearMismatchModal.jsx`: כותרת המשנה "קובץ לא תקין" שונתה לאותו צבע/גודל של הכותרת הראשית (`text-slate-800`, לא אדום).
- נוסף כפתור "הסר" משמאל ל-"החלף קובץ" בשורות דיווח ביצוע/כספים2000/פייסקול/סקולקאש (לא בקובץ תכנון — היחיד שאינו "קובץ רשות"). לחיצה מסמנת את השורה כמוסרת: היא נעלמת מהחלון, קבציה לא נשלחים בהרצה, והיא לא נספרת יותר לצורך נעילת "בצע בדיקה". מאפשר להריץ בדיקה עם קובץ תכנון בלבד (או כל שילוב שנשאר) בלי להיאלץ להחליף קובץ בעייתי.

## 2026-09-22 — שיפורי UX בחלון "טיב הקובץ"
- `frontend/src/components/YearMismatchModal.jsx`: הודעת "unscoped_budget" נוסחה מחדש ("הקובץ שהועלה אינו תקין. נא להוריד מחדש את הקובץ מ{פייסקול/כספים2000} ולוודא שבחרתם 'לפי סוג תקציב'...") עם שם התוכנה הספציפי דינמית. שורות "קובץ לא תקין" הופרדו מבעיות שנת הלימודים לרשימה נפרדת עם כותרת-משנה אדומה "קובץ לא תקין" ביניהן, כדי שהכותרת הראשית לא תתאר בטעות גם את בעיית סוג התקציב. הרחבת רוחב החלון (`max-w-xl`) ויישור השורות ל-`items-start` כדי להתאים להודעה הארוכה יותר. טקסט הפתיחה הכללי שונה ל"שימו לב: חלק מהקבצים שהועלו אינם תואמים את שנת הלימודים עליה אתם נמצאים במערכת."

## 2026-09-22 — בדיקת "טיב קובץ": חסימת קובץ כספים2000/פייסקול לא מסונן לפי סוג תקציב
- `backend/routers/analyze_router.py`: `_kesafim_budget_scoped`/`_payscool_budget_scoped`/`_finance_budget_scoped` — מזהות קובץ כספים "כללי" (לא מסונן לתקציב ספציפי): בכספים2000 בודקות שהערך בשדה "סוג תקציב" (שורת הבלוק הראשונה) מנורמל לאחד משמונת שמות התקציב המוכרים במערכת; בפייסקול בודקות שהגיליון הרלוונטי מתחיל בדיוק בעמודות "סעיף"/"שם ספק"/"ח.פ" (חתימת דוח "לפי סוג תקציב" התקין — דוח "פירוט הוצאות" הכללי מסודר אחרת).
- `_collect_file_year_issues` ו-`POST /analyze/detect-file-year` בודקים את זה לפני בדיקת השנה; קובץ לא מסונן מקבל שורה בחלון עם `status: "unscoped_budget"`, שתמיד חוסמת (בלי קשר לשנה).
- `frontend/src/components/YearMismatchModal.jsx`: הודעת שגיאה ייעודית (אדום, לא ענבר) לשורה כזו, וכפתור "בצע בדיקה" נעול לחלוטין כל עוד קיימת שורה כזו — האפשרות היחידה היא "החלף קובץ".
- היקף נוכחי: רק כספים2000 ופייסקול. סקולקאש לא נבדק עדיין.
- אומת מול קבצי דוגמה אמיתיים: קובץ כספים2000 תקין (מסונן לגפן), קובץ פייסקול תקין, ושני קבצי פייסקול "כלליים" אמיתיים — כולם התנהגו בדיוק כמצופה.

## 2026-09-22 — תיקון: חלון חוסר ההתאמה מציג עכשיו את כל הקבצים, לא רק את הבעייתיים
- `backend/routers/analyze_router.py`: `_collect_file_year_issues` בנויה מחדש — עכשיו בונה שורה עבור **כל** קובץ שהועלה (תכנון/דיווח ביצוע/כספים) תמיד, כולל קבצים תואמים. אם לפחות קובץ אחד באמת בעייתי — מחזירה את **כל** הרשימה (כדי שהמשתמש יראה את כל התמונה, כולל קבצים שכן תקינים, ויוכל להחליט אם להחליף גם אותם). אם הכל תואם — מחזירה רשימה ריקה כרגיל, והחלונית לא נפתחת.
- לא נדרש שינוי בפרונטאנד — הלוגיקה הקיימת (צביעת שורה ירוק/אדום לפי התאמה לבורר, ונעילת כפתור "בצע בדיקה") כבר תמכה בכך באופן טבעי.
- `backend/routers/analyze_router.py`: פונקציה חדשה `_read_finance_invoice_dates` שקוראת את עמודת התאריך (`invoice_date`/`"תאריך חשבונית"` בהתאם לסוג) מקבצי כספים2000/פייסקול/סקולקאש דרך `load_kesafim`/`load_payscool`/`load_schoolcash` הקיימים. רפקטור קל: `_dates_from_series` משותפת לגפן ולכספים. `_is_empty_gefen_suspicious` שונה שם ל-`_is_empty_file_suspicious` (הלוגיקה כבר הייתה גנרית).
- `_collect_file_year_issues` מקבלת גם `finance_paths`/`finance_type` — קובץ/י הכספים מקבלים **שורה אחת** משותפת בחלון (בניגוד לתכנון/דיווח ביצוע שכל קובץ מקבל שורה נפרדת), כי מבחינת המערכת הם כבר מיוצגים כישות אחת (`finance_file_name`). כל פריט ב-`file_year_issues` עבר מ-`filename` יחיד ל-`filenames: list[str]` בהתאם.
- `POST /analyze/detect-file-year` מרחיב את `file_role` הנתמך ל-`kesafim2000`/`payscool`/`schoolcash`.
- `frontend/src/components/YearMismatchModal.jsx` ו-`SchoolPage.jsx`: תמיכה ב-`filenames` (רשימה) במקום `filename` יחיד; "החלף קובץ" על שורת כספים מחליף את כל קבוצת הקבצים בקובץ בודד חדש.
- **סייג שהתגלה (קובץ מוגן, לא טופל):** `backend/logic/file_identifier.py` מזהה גם קובץ כספים2000 שנשמר כ-xlsx בינארי אמיתי (לא רק TSV-בתור-xls) כ-`kesafim2000` תקין, אבל `load_kesafim` יודע לקרוא רק את פורמט ה-TSV הישן וקורס (`UnicodeDecodeError`) על קובץ כזה — קיים גם היום בבדיקה האמיתית, לא נגרם על ידי השינוי הזה.
- מחוץ לתחום (לטיפול נפרד בעתיד): בדיקת "טיב" קובץ הכספים — האם הורד עם בחירת סוג תקציב ספציפי (בעיה שכבר קיימת בפייסקול/כספים2000 לדברי המשתמש).

## 2026-09-22 — זיהוי חוסר התאמה בשנת לימודים בקובץ תכנון
- `backend/academic_years.py`: פונקציה חדשה `get_academic_year_for_calendar_year` — ממפה את הערך המספרי בעמודת "שנת לימודים" בקובץ תכנון (2026/2027) לשם השנה העברית (תשפ"ו/תשפ"ז).
- `backend/routers/analyze_router.py`: פונקציה חדשה `_read_tikhnun_school_years` (קריאה עצמאית עם openpyxl, לא נוגעת ב-`backend/logic/`) שמזהה את עמודת "שנת לימודים" בגיליון "הכל" לפי טקסט הכותרת. בתוך `_process`, לפני עיבוד קובץ התכנון, נוספה בדיקה שחוסמת עם `error_code: "year_mismatch"` (כשהשנה בקובץ שונה משנת הלימודים הפעילה) או `"year_unrecognized"` (כשלא ניתן לזהות שנה מוכרת בקובץ).
- `frontend/src/components/YearMismatchModal.jsx`: חלון חדש עם כפתורי "בצע בדיקה ב-X" / "ביטול".
- `frontend/src/pages/SchoolPage.jsx` (ChecksTab): בנתיב "בדיקה חדשה" — טיפול מלא: מציג את החלון החדש ומאפשר מעבר לשנה הנכונה והרצה חוזרת אוטומטית. בנתיב "הוספת קובץ לבדיקה קיימת" — חסימה עם הודעת שגיאה רגילה בלבד (ללא כפתור מעבר שנה), כי הבדיקה כבר משויכת לשנה קבועה.
- הרצת בדיקה אוטומטית מתוך "קבצים שהתקבלו לפגישה" נשארת מוגנת ברמת השרת (תיחסם באותו אופן), אך טיפול ה-UI הייעודי עבורה נדחה לתכנון נפרד בעתיד.

## 2026-09-22 — הרחבת זיהוי שנת לימודים לקובץ דיווח ביצוע + חלון מאוחד
- `backend/routers/analyze_router.py`: פונקציות חדשות `_read_gefen_invoice_dates` (קוראת את עמודת "תאריך חשבונית" מקובץ/י דיווח ביצוע דרך `load_gefen` הקיים), `_classify_file_year` (כלל 60% מתוך 5+ שורות מתוארכות, לפי `get_academic_year_for_date`), `_is_empty_gefen_suspicious` (סף 31.5 בשנה העוקבת לתחילת שנת הלימודים). בדיקת ה-`year_mismatch` הקודמת (לקובץ תכנון בלבד) הוחלפה בבדיקה מאוחדת `_collect_file_year_issues` שבודקת גם קובצי תכנון וגם קובצי דיווח ביצוע יחד ומחזירה רשימת כל הבעיות בבת אחת (`error_code: "year_issues"`), במקום לחסום על הראשונה שנמצאה.
- נקודת קצה חדשה `POST /analyze/detect-file-year` — בדיקת שנה קלה לקובץ בודד (ללא הרצת בדיקה/שמירה), משמשת את כפתור "החלף קובץ" בחלון.
- `frontend/src/components/YearMismatchModal.jsx`: הפך לחלון מאוחד — רשימת קבצים בעייתיים (סוג + שם + שנה שזוהתה/הערת "ריק"), כפתור "החלף קובץ" לכל שורה, בורר שנת לימודים יחיד (ברירת מחדל: השנה הפעילה), וכפתור "בצע בדיקה" שנפתח ללחיצה רק כששורות עם שנה מזוהה מפורשת תואמות לבורר.
- `frontend/src/pages/SchoolPage.jsx`: חיווט מחדש ל-`error_code: "year_issues"`, פונקציית `detectFileYear` חדשה לתקשורת עם נקודת הקצה החדשה.
- בהיקף הנוכחי: רק קובץ דיווח ביצוע (מהגפן, עמודת "תאריך חשבונית") — קובץ תוכנת הכספים (כספים2000/פייסקול/סקולקאש) נשאר מחוץ לתחום, ייתכן שיטופל בעתיד.

## 2026-09-22 — תיקון: דריסת קבצים בשם זהה בהעלאה
- תקלה קיימת-מראש שהתגלתה תוך כדי בדיקת פיצ'ר שנת הלימודים: ב-`backend/routers/analyze_router.py`, כשקובץ שהועלה נשמר לדיסק לפי שמו המקורי בלבד (`dest = run_dir / uf.filename`), שני קבצים באותה העלאה עם שם זהה (למשל שני קבצים בשם "doch.xlsx" — לדוגמה חטיבת תיכון וחטיבת ביניים) גרמו לקובץ השני לדרוס בשקט את הראשון על הדיסק, לפני כל עיבוד — אחד הקבצים אבד לחלוטין בלי הודעת שגיאה.
- תוקן בשלושת המקומות שכותבים קבצים ל-`run_dir` (`POST /analyze/upload`, ושתי הלולאות ב-`POST /analyze/add-file/{log_id}`): כל קובץ נכתב עכשיו לתת-תיקייה ממוספרת משלו (`run_dir/<אינדקס>/<שם מקורי>`) במקום ישירות ל-`run_dir` השטוח — מונע דריסה לחלוטין, בלי לשנות את שם הקובץ עצמו בשום מקום אחר בקוד (הודעות שגיאה, `stored_file_paths`, שמות בחלון חוסר ההתאמה בשנת לימודים).
- וידאתי ש-`_upload_files_to_storage` לא מושפעת (כבר משתמשת במפתחות אחסון מבוססי-אינדקס, לא בשם הקובץ).
