# Dev Log

## 2026-09-07 — עמודות "סוג שירות / רשות חינוך / מגזר" בתצוגת עמודות של מסך הבית
- `DashboardPage.jsx`: נוספו 3 עמודות לקבוצת "כללי" ב"עמודות להצגה", בשורה שמעל "יועץ מלווה [גפן/שוטף/מחוז]": סוג שירות, רשות חינוך, מגזר. מוסתרות כברירת מחדל, מסומנות ידנית. תצוגה בלבד (ללא סינון/מיון), כולל ייצוא לאקסל.
- `DashboardPage.jsx`: העמודות נוספו ל-`NEWLY_ADDED_MOVABLE_COLUMNS` כדי שיופיעו גם למשתמשים עם העדפות עמודות שמורות. נוסף מיפוי `SERVICE_TYPE_LABEL`.
- `schools_router.py` (`list_schools`): הוספת העשרת `service_type` (שדה שנתי מ-`school_year_admin_data`) עם ירושה משנה קודמת דרך `resolve_inherited_year_admin`, כדי שהעמודה תתאים לכרטיס בית הספר. `education_authority` ו-`sector` כבר הוחזרו דרך `select("*")`.
- `DashboardPage.jsx`: נוספה שורה שנייה של עמודות לקבוצת "כללי" (מתחת לשורה של סוג שירות/רשות חינוך/מגזר): סטטוס לקוח, אמצעי הזמנה, מחיר כולל מע"מ. אותו עיקרון — מוסתרות כברירת מחדל, תצוגה בלבד, כולל אקסל. נוספו מיפויי `CLIENT_STATUS_LABEL` / `ORDER_METHOD_LABEL`.
- `schools_router.py` (`list_schools`): `client_status` מועשר עם ירושה (כמו `service_type`); `order_method` ו-`order_amount_gefen` מהשנה הנבחרת בלבד (ללא ירושה, בהתאם לכרטיס). נוספו ל-`select` של שאילתת ה-year-admin.
- `DashboardPage.jsx`: שינוי סדר העמודות בקבוצת "כללי" ב"עמודות להצגה" — רשות חינוך במקום תוכנת כספים, סטטוס לקוח במקום סוג שירות, סוג שירות במקום רשות חינוך, תוכנת כספים במקום סטטוס לקוח (מגזר / אמצעי הזמנה / מחיר כולל מע"מ ללא שינוי).
- `DashboardPage.jsx`: סידור מחדש של השורה מגזר/סטטוס לקוח/סוג שירות ב"עמודות להצגה" — מימין לשמאל: מגזר, סטטוס לקוח, סוג שירות.

## 2026-09-07 — סינון ומיון בכותרת לכל עמודות מסך הבית
- `DashboardPage.jsx`: הורחב מנגנון מסנן/מיון הכותרת (סגנון אקסל) כך שיתמוך גם בעמודות טקסט ורב-ערכיות, בנוסף למספריות/תאריך/enum שכבר נתמכו.
- עמודות שקיבלו כעת מסנן+מיון בכותרת: סמל מוסד, עיר, בעלות, שלב מוסד, מחוז, תוכנת כספים, רשות חינוך, מגזר, סטטוס לקוח, סוג שירות, מחיר כולל מע"מ, אמצעי הזמנה, יועץ מלווה [גפן/שוטף/מחוז].
- עמודות טקסט: תפריט "מסנני טקסט" (מכיל/לא מכיל/שווה/לא שווה/מתחיל ב/מסתיים ב) + רשימת ערכים לסימון + מיון א׳→ת׳ / ת׳→א׳ (locale עברית).
- עמודות רב-ערכיות (יועצים, אמצעי הזמנה): רשימת הערכים מפורקת לערכים בודדים; שורה מותאמת אם היא מכילה לפחות אחד מהערכים שסומנו. מיון לפי הטקסט המוצג בתא.
- לא נכללו (לפי בקשת המשתמש): הערות סגירת שנה, הערות מכתב בקרה, שמות קבצי מכתב בקרה. פאנל "סינון מתקדם" הקיים נשאר ללא שינוי (ייתכן כפילות סינון לחלק מהעמודות — בכוונה).
- ללא שינוי בבקאנד — כל הנתונים כבר נשלחים.
- תיקון: חלונית המסנן בכותרת נסגרה בכל גלילה, כולל גלילה בתוך רשימת הערכים של החלונית עצמה (מאזין `scroll` ב-capture על `document`). כעת גלילה בתוך החלונית לא סוגרת אותה; גלילת הדף/הטבלה עדיין סוגרת (החלונית fixed ומתנתקת מהכותרת).

## 2026-09-07 — תיקון סינון שגוי ברשימת הערכים של מסנן הכותרת (חיפוש)
- הבאג: ה-draft של הצ'קבוקסים אותחל לכל הערכים; הקלדה בתיבת החיפוש רק צמצמה את התצוגה, אך הערכים המוסתרים נשארו מסומנים ב-draft. בלחיצת "אישור" הבחירה יצאה ≈"הכל" = בלי סינון בפועל, ופתיחה מחדש הראתה הכל מסומן. השפיע על עיר, סמל מוסד וכל עמודות רשימת-הערכים.
- תיקון (`DashboardPage.jsx`, `ColumnHeaderFilter`):
  - כשמתחילים להקליד בחיפוש — הבחירה "נצמדת" לכל הערכים התואמים כרגע (כמו אקסל): "אישור"/Enter מסנן לתואמים; ביטול סימון מצמצם עוד. ניקוי החיפוש משחזר את הבחירה הקודמת.
  - `confirmValues`: כשיש טקסט חיפוש — מוחל בדיוק על הערכים המסומנים+נראים (ערכים מוסתרים לא נכללים). בדיקת "הכל מסומן → אין סינון" הפכה לבדיקת קבוצה במקום השוואת אורך.
  - "(ריקים)" מוצג רק כשאין טקסט חיפוש.
  - Enter בתיבת החיפוש = אישור. ניקוי טקסט החיפוש ב-`clearFilter` ובעת אישור.

## 2026-09-07 — תיקון: מונה בתי הספר וייצוא לא התחשבו במסנני הכותרת
- הבאג: שורת המונה ("סה"כ N בתי ספר מתוך M") וייצוא Excel/PDF וכפעולות גורפות התבססו על `filteredSchools`, שמשקף רק את שורת החיפוש ו"סינון מתקדם" — לא את מסנני הכותרת (שמוחלים ב-`finalDisplayRows`). לכן סינון דרך כותרת עמודה (בעלות וכו') סינן את הטבלה בפועל אך המונה הראה את המספר המלא, והייצוא כלל את כל בתי הספר.
- תיקון (`DashboardPage.jsx`): נוספו `displayedSchools` / `selectedSchools` הנגזרים מ-`finalDisplayRows` (אחרי כל הסינונים). המונה, "הורד לאקסל", ייצוא נבחרים (Excel/PDF), `BulkAccessModal` ומחיקה גורפת עברו להשתמש בהם — כך שכל אלו עקביים עם מה שמוצג בטבלה.
