# Dev Log

## 2026-09-09 — תיקון חיתוך שקט ל-1,000 שורות ברשימות פגישות
- זוהה ותוקן: `PostgREST` (שכבת ה-API של Supabase) מחזיר עד 1,000 שורות בבקשה אחת כברירת מחדל, גם אם הסינון תואם הרבה יותר — בלי שום שגיאה. זה גרם למסכי "ניהול פגישות" ו"אזור אישי" (וגם לסטטיסטיקות הלוח בקרה) להציג רק 1,000 פגישות במקום את כל התוצאות שתואמות לסינון, בשקט מוחלט
- נוסף helper משותף `_fetch_all_rows` שדופדף אוטומטית על פני כל הדפים עד לקבלת כל השורות התואמות, ללא סריקת טבלה — משמש כעת ב-`GET /schools/meetings/all`, `GET /schools/meetings/my`, `GET /schools/{id}/meetings`, `GET /schools/meetings-stats`, ובבדיקת הכפילויות של ייבוא פגישות

## 2026-09-09 — תיקון תצוגת "מיקום" ו"סוג" ריקים בפגישות מיובאות
- אותו דפוס באג שתוקן לסטטוס: `MeetingTypeSelect` (מיקום) ו-`MeetingServiceTypeSelect` (סוג) הציגו "+" ריק במקום הטקסט שיובא מהאקסל, כי הן זיהו רק את הערכים הסגורים (פיזי/מרחוק, גפן/שוטף/וכו') ולא ידעו להציג טקסט חופשי. עכשיו מוצג הטקסט המקורי כשהוא לא אחד מהערכים הסגורים — תיקון רטרואקטיבי, בלי שינוי נתונים
- אותו תיקון בוצע גם בייצוא לאקסל/PDF של הפגישות (`AdminMeetingsTab.jsx`/`PersonalMeetingsTab.jsx`) — עמודות "מיקום"/"סוג" היו מיוצאות ריקות עבור פגישות מיובאות
