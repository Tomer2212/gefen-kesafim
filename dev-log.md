# Dev Log

## 2026-09-23 — Feat: טעינה הדרגתית (infinite scroll) לעמוד התראות
- backend/routers/schools_router.py (`GET /schools/notifications`): נוספו `limit`/`offset` לעימוד אמיתי מצד השרת (`.range(...)` במקום `.limit(50)` קשיח), הוחזר `has_more`.
- מונה "לא נקראו" (`count`) ורשימת `goal_auto_updates` הופרדו לשאילתות עצמאיות שלא תלויות בעמוד הנוכחי — מתקן undercount ופופ-אפ יעדים שהוחמץ מעבר לעמוד הראשון.
- frontend/src/pages/NotificationsPage.jsx: הרשימה עברה ל-virtualization (`useRowVirtualizer`, אותו hook כמו בטבלאות אחרות) עם טעינת עמוד נוסף אוטומטית בגלילה קרוב לתחתית הרשימה.
- Sidebar.jsx (פעמון ההתראות, פולינג כל 60 שניות) לא שונה — ממשיך לקבל אותה צורת תשובה.
- **תיקון נוסף:** אחרי השינוי הראשוני עמוד ההתראות היה מוצג ריק לגמרי (בלי ספינר, בלי הודעת "אין התראות", בלי שורות) — מיכל הגלילה של הרשימה היה עם `max-h-[70vh]` בלבד וללא גובה מינימלי, כך שלפני שה-virtualizer מחליט מה לרנדר הוא נשען על הגובה של המיכל, שהיה 0 (אין עדיין תוכן) — מעגליות שגרמה למיכל להישאר ריק לצמיתות. נוסף `min-h-[160px]` למיכל כדי לשבור את המעגליות.
- **תיקון נוסף:** בגלילה נצברו שגיאות React "two children with the same key" — ה-offset לעמוד הבא חושב מתוך `items.length` בצד הלקוח, אבל הבקאנד מסנן החוצה שורות מסוג `goal_auto_updated` **אחרי** חיתוך חלון ה-`range`, כך שמספר הפריטים שחוזרים ללקוח קטן ממספר השורות שבאמת נצרכו מה-DB — מה שגרם לעמוד הבא לבקש שוב טווח שכבר נצרך וליצור כפילויות. תוקן: המעקב אחרי המיקום עבר ל-`offsetRef` נפרד שמתקדם תמיד ב-`NOTIF_PAGE_SIZE` (לא לפי `items.length`), ונוספה גם הגנת de-dupe לפי `id` בהוספת עמוד חדש כהגנת-משנה.

## 2026-09-23 — תיקון: בדיקת תכנון-בלבד עכשיו נשמרת ב-check_logs
- רקע: בדיקה עם קובץ תכנון בלבד (בלי קובץ דיווח ביצוע) הציגה תוצאה למשתמש אך מעולם לא נשמרה בפועל — שורת ה"בדיקה" נעלמה מהיסטוריית הבדיקות ברגע שעוזבים את העמוד וחוזרים. אומת בעבר ישירות מול Supabase (0 שורות check_logs שנשמרו).
- `backend/routers/analyze_router.py`: הוסר תנאי `or run.get("tikhnun_only")` מהשומר ב-`_save_check_log` שחסם שמירה של בדיקות תכנון-בלבד בכוונה. נוספה קריאה ל-`_save_check_log` בענף "Tikhnun-only run" (שני תתי-המקרים — קובץ תכנון בודד ושני קבצים לשש-שנתי), עם `tikhnun_filenames` שהיה חסר בענף הזה.
- **פער נוסף שהתגלה ותוקן במקביל:** `_save_check_log` לא היה כותב את הדגל `tikhnun_only` לתוך ה-`summary` הנשמר, ו-`logToResult` בפרונטאנד (`SchoolPage.jsx`) לא היה קורא אותו בחזרה — המשמעות: פתיחת בדיקת-תכנון-בלבד שמורה מההיסטוריה הייתה מוצגת בטעות כאילו בוצעה השוואה מלאה מול כספים/גפן ללא פערים (טבלאות ריקות אך "תקינות למראה"), במקום לציין נכון שלא בוצעה השוואה כזו. תוקן בשני הקצוות (`summary_to_save["tikhnun_only"]` בבקאנד, `tikhnun_only: s.tikhnun_only ?? false` ב-`logToResult`).
- לפני שהוחלט לבצע את השינוי, נבדקו כל הצרכנים האחרים של `check_logs` (טבלת ההיסטוריה, "הוספת קובץ", "השוואה בין בדיקות", `check_metrics`/עדכון יעדים אוטומטי) — כולם כבר בנויים נכון להתמודד עם ערכים חסרים של בדיקת תכנון-בלבד, ללא צורך בשינוי נוסף.
