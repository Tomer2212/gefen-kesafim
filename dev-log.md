# Dev Log

## 2026-09-11 — שיפורי UI ב'ניהול - גבייה'
- כפתור "סטטוס לקוח" מעוצב זהה לכפתור "עמודות לתצוגה"
- מעל הטבלה מוצג "סה"כ X בתי ספר מתוך Y" (בדומה ל'ניהול-בתי ספר')
- בכותרת "שם מוסד" נוספו סינון (שדה חופשי) ומיון, כמו בשאר העמודות

## 2026-09-15 — תיקון "תחומי ידע" באזור אישי
- תוקן באג שבו "תחומי ידע" (control_domains) ו"טלפון עבודה" (work_phone) נעלמו מתצוגת המשתמש באזור האישי לאחר יציאה וחזרה לטאב "פרטים אישיים" (למרות שנשמרו נכון ב-DB) — השדות היו חסרים משאילתת קאש הפרופיל ב-backend/auth.py
- אוחד תהליך שמירת "תחומי ידע": לחיצה על "אישור" בתפריט הנפתח שומרת מיידית לשרת, במקום לדרוש לחיצה נוספת על "שמור" נפרד; כפתור "ביטול" עבר לתוך אותו תפריט נפתח (MultiSelectChips.jsx) — שינוי אופציונלי שלא משפיע על שאר המקומות שמשתמשים ברכיב

## 2026-09-11 — בתי ספר "לא פעילים" (מידע יבש)
- נוסף סטטוס חדש `schools.status = "reference"` (בית ספר שאינו לקוח, לצד "active"/"pending_deletion" הקיימים) — לא מופיע בדשבורד הראשי או בטבלת הלקוחות הפעילים בניהול, ללא צורך במיגרציית DB (עמודת status היא טקסט חופשי ללא CHECK constraint)
- `POST /schools/` מאפשר יצירת בית ספר עם `status: "reference"` (owner/manager בלבד), פטור מדרישת "אחראי/ת לתיאום פגישות"
- `PUT /schools/{id}` מאפשר שינוי סטטוס בין active/reference (owner/manager בלבד, ורק כשהערך אכן משתנה — לא חוסם עריכה רגילה של יועץ עם הרשאת can_edit_school_directly)
- ב'ניהול → בתי ספר' נוסף אזור מקופל נפרד "בתי ספר לא פעילים — מידע יבש", מתחת לטבלה הראשית, עם טבלה וירטואלית עצמאית (@tanstack/react-virtual, כמו הטבלה הראשית) לתמיכה באלפי רשומות, כולל חיפוש וכפתורי "ערוך" / "הפוך ללקוח פעיל" לכל שורה
- ייבוא מאקסל: תיבת סימון "ייבוא כבתי ספר לא פעילים (מידע יבש)" — שורות חדשות נוצרות כ-reference בלי דרישת יועץ/אחראי תיאום; שורה שתואמת סמל מוסד של בית ספר לא-פעיל קיים מתעדכנת אוטומטית (ללא צורך באישור ידני); שורה שתואמת סמל מוסד של לקוח פעיל קיים מדולגת לגמרי ולעולם לא נכתבת — עם סיכום בתום הייבוא כמה נוצרו/עודכנו/דולגו
