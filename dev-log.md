# Dev Log

## 2026-07-22 — אינטגרציית VOICENTER (Phase 1, ארכיטקטורה מעודכנת: Pull-only)
- **חשוב:** בעקבות בירור מעמיק (כולל מציאת ה-Call Log API הרשמי של Voicenter), הוחלט **לא לשמור שום נתון שיחה ב-Supabase**. הארכיטקטורה הראשונית (webhook + טבלת `calls`) הוסרה לגמרי והוחלפה בשליפה חיה.
- טבלת `calls` נמחקה. נשארו רק שתי טבלאות שהן **המידע שלנו, לא של Voicenter**: `voicenter_integrations` (קוד חשבון/טוקן API לשליפה) ו-`voicenter_rep_mappings` (מיפוי נציג Voicenter ↔ יועץ שלנו).
- `backend/routers/voicenter_router.py` שוכתב: הוסר ה-webhook הציבורי; נוסף `GET /voicenter/calls` ששולף **בזמן אמת** מ-Call Log API הרשמי של Voicenter (`https://api.voicenter.com/hub/cdr/`) בכל בקשה, בלי לשמור דבר.
- כרטיס VOICENTER בטאב "אינטגרציות" עודכן בהתאם: שדה קוד חשבון / טוקן API (לא webhook URL).
- **נבדק ישירות מול Voicenter** עם הטוקן שנמסר: אושר שהאימות עובד נכון כשמעבירים אותו בשדה `code` בגוף הבקשה (לא כ-Authorization Bearer header — זה מחזיר שגיאת שרת אצלם).
- **חסימה נוכחית:** ה-API של Voicenter דוחה בקשות מכתובות IP לא מורשות (קיבלנו `ERROR_NUMBER: 4, "IP address ... is not trusted"`). צריך לפנות ל-Voicenter כדי לאשר את כתובת ה-IP של השרת שלנו (מקומי לבדיקות: `85.65.71.19`; ובהמשך גם את כתובת ה-IP הקבועה של Render לסביבת הייצור).
- שדה סיכום AI (סיכום שיחה/תמלול/רגשות) **אינו זמין** דרך ה-Call Log API לפי התיעוד — אבל **התגלה שדה `customdata.AiExists`** בתשובה האמיתית שמראה לכל שיחה אם קיים לה סיכום AI אצל Voicenter (לא את התוכן עצמו). התשתית לעמודה בטבלה בנויה מוכנה (`ai_summary_available`), אך התוכן עצמו יוצג כ"לא זמין" עד שיימצא/יתועד ערוץ שליפה נוסף.

## 2026-07-23 — פריצת דרך: החיבור ל-Call Log API עובד! 🎉
- **הבעיה נפתרה.** אחרי בירור מול תמיכת Voicenter, התגלו שתי בעיות נפרדות: (1) הטוקן הראשון שנמסר היה **פגום** (חסרה לו חתימת JWT) — Voicenter הפיקו טוקן חדש ותקין; (2) **הדרך הנכונה לאימות היא `Authorization: Bearer <token>`, לא שדה `code` בגוף הבקשה** — מסלול ה-`code` חסום ע"י רשימת IP מורשים ברמת החשבון (שהדמו לא נמצא בה), אך מסלול ה-Bearer **לא בודק IP בכלל** ועובד גם מכתובת לא מאושרת, כל עוד הטוקן שלם וחתום.
- `backend/routers/voicenter_router.py` עודכן: שולח כעת `Authorization: Bearer` (לא `code`), ותוקנו שמות השדות בפענוח התשובה (Voicenter מחזירים lowercase — `callernumber`, `dialstatus` וכו' — בניגוד ל-PascalCase שבתיעוד ה-PDF).
- **נבדק מקצה לקצה בהצלחה** מול נתונים אמיתיים מחשבון הדמו: 4 שיחות אמיתיות חזרו עם כל השדות תקינים (כיוון, מספר טלפון, משך, סטטוס, קישור הקלטה, שם/קוד נציג).
- **Phase 2 נבנה:** טאב "שיחות" חדש נוסף לפאנל הניהול (בין "פגישות" ל"הרשאות", גלוי לבעלים+מנהלים) — `frontend/src/pages/AdminCallsTab.jsx` + `components/calls/CallsTable.jsx` + `CallRow.jsx`, בסגנון זהה לטבלת הפגישות. כולל: כל העמודות שהוגדרו (כיוון, מספר צד שני, יועץ, תאריך, שעת התחלה, משך, שעת סיום מחושבת, סטטוס, נגן הקלטה, סיכום AI [כרגע רק דגל "קיים"], מחלקה), פילטרים (תאריכים עם ברירת מחדל 7 ימים אחרונים, יועץ, כיוון), וה-checkbox "הצג פערים בין שיחות" עם שורות gap בעובי לוגריתמי.
- `npx vite build` עובר נקי (0 שגיאות). **נבדק בדפדפן חי בהצלחה** — הטאב מציג שיחות אמיתיות מהדמו.
- תוקנה בעיית proxy: `frontend/vite.config.js` היה חסר ערך `/voicenter` ברשימת ה-proxy לשרת המקומי (8000) — זו הייתה הסיבה ל-404 שראינו בהתחלה. גם טופל ניקוי תהליכי `npm run dev`/`vite` כפולים שרצו ברקע (אותה תופעה שתועדה בעבר עם uvicorn).
- שופרה חלונית ההגדרות של Voicenter: הוסר שדה "קוד חשבון" (לא בשימוש בפועל, רק הטוקן קובע), נוספה הודעת הצלחה עם סגירה אוטומטית לאחר שמירה, ונוסף כפתור "ניתוק חיבור".
- תוקן פורמט מספר טלפון בעמודת "מספר צד שני": מספרים ישראליים (972...) מוצגים כ-0XX-XXXXXXX.

## 2026-07-23 (המשך) — Phase 3: שמירת סיכום AI + תמלול
- **אימות סופי:** ה-Call Log API (pull) לא חושף בשום צורה תוכן AI (נבדק ישירות עם ניסיון שמות שדות שונים) — סיכום/תמלול קיימים **רק** ב-CDR Notification (ה-webhook החד-פעמי). לכן נדרש קילוט מוגבל ומכוון של שדה זה בלבד.
- **חושב נפח אחסון** (25 יועצים × 6 שעות שיחה/יום, שנתיים): סיכום בלבד ~1-3GB; + תמלול מלא ~13-35GB נוספים. הוחלט לפצל: סיכום (קטן) ← טבלת Database רגילה; תמלול (כבד) ← קובץ ב-Supabase **Storage** (מכסה נפרדת של 100GB, לא ה-8GB של ה-Database) — כך שני הצדדים בטוחים בתוך מסלול Pro ($25), בלי צורך במסלול Team ($599).
- טבלה חדשה `voicenter_call_ai` (org_id, call_id, summary, transcript_path) + Storage bucket פרטי `voicenter-transcripts` (גישה רק דרך signed URL).
- `voicenter_router.py`: הוחזר `POST /voicenter/webhook/{org_id}/{secret}` (ציבורי) — אך **מוגבל בכוונה**: קולט רק `ivruniqueid` + `aiData.insights.summary` + `aiData.transcript`, לא שאר פרטי השיחה (אלה נשארים Pull-only). נוסף `GET /voicenter/calls/{call_id}/transcript` (מנפיק signed URL). הורחב `GET /voicenter/calls` עם enrichment non-fatal שמצליב סיכום/תמלול מ-`voicenter_call_ai` לפי `call_id`.
- Frontend: `CallRow.jsx` — תא "סיכום AI" הפך ללחיץ, פותח `CallSummaryModal.jsx` חדש עם הסיכום + כפתור "הצג תמלול מלא" (טוען דרך signed URL).
- **נבדק מקצה לקצה בהצלחה** מול שיחה אמיתית מהדמו: webhook סינתטי → נשמר ב-DB + Storage → הופיע נכון ב-`GET /voicenter/calls` → transcript נשלף נכון דרך signed URL. נתוני הבדיקה נוקו בסיום.
- `npx vite build` — 0 שגיאות.
- **טרם נבדק:** קליטת webhook אמיתי משיחה חיה מ-Voicenter (צריך להדביק את כתובת ה-webhook החדשה בפאנל שלהם ולבצע שיחת בדיקה), ואימות שה-`ivruniqueid` שמגיע ב-webhook תואם בדיוק ל-`CallID` שרואים ב-Call Log API לאותה שיחה.
